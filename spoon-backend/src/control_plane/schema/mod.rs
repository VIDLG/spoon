//! Embedded SQL migration files for the control-plane schema.
//!
//! Individual migration files are `include_str!`-ed from [`migrations.rs`](super::migrations).
//! This module exists only so Rust recognises the `schema/` directory as a module tree.

---
phase: 10-scoop-legacy-path-and-state-cleanup
plan: 3
type: execute
wave: 2
depends_on:
  - 10-01
  - 10-02
files_modified:
  - spoon/src/config/paths.rs
  - spoon/src/status/discovery/probe.rs
  - spoon-backend/src/status.rs
  - spoon-backend/src/scoop/query.rs
autonomous: true
requirements:
  - SLEG-04
must_haves:
  truths:
    - "Adjacent app/backend path helpers stop reintroducing the old Scoop path worldview."
    - "Spillover cleanup improves readability instead of leaving deprecated path aliases as the public story."
  artifacts:
    - path: "spoon/src/config/paths.rs"
      provides: "reduced app-side Scoop path legacy"
      contains: "narrower deprecated helper surface or explicit layout-aligned access"
---

<objective>
Clean the direct spillover outside the Scoop backend that would otherwise preserve the old path model.

Purpose: keep the backend cleanup coherent by reducing adjacent deprecated path aliases and read-path leftovers.
Output: app/backend spillover aligns with the layout-first Scoop model.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Reduce app-side deprecated Scoop path aliases that conflict with the new model</name>
  <files>
spoon/src/config/paths.rs
spoon/src/status/discovery/probe.rs
  </files>
  <read_first>
spoon/src/config/paths.rs
spoon/src/status/discovery/probe.rs
spoon-backend/src/layout.rs
  </read_first>
  <action>Remove, narrow, or clearly isolate app-side Scoop path helpers so they do not keep the old backend path worldview alive after the backend cleanup.</action>
  <verify>
    <automated>cargo check -p spoon-backend -p spoon</automated>
  </verify>
  <acceptance_criteria>
- App-side Scoop path access no longer undermines the layout-first backend cleanup.
  </acceptance_criteria>
  <done>Backend and app path stories are coherent again.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten representative status/query outputs if legacy path assumptions or low-value redundancy remain coupled to this cleanup</name>
  <files>
spoon-backend/src/status.rs
spoon-backend/src/scoop/query.rs
  </files>
  <read_first>
spoon-backend/src/status.rs
spoon-backend/src/scoop/query.rs
  </read_first>
  <action>Use the Scoop cleanup to remove any obviously stale path assumptions or trivial redundancy that directly block a cleaner layout-owned read model.</action>
  <verify>
    <automated>cargo test -p spoon --test status_backend_flow -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- Representative status/query outputs stay aligned with the cleaned Scoop path model.
  </acceptance_criteria>
  <done>Path cleanup does not leave read-model spillover behind.</done>
</task>

</tasks>

<verification>
Run `cargo check -p spoon-backend -p spoon` and `cargo test -p spoon --test status_backend_flow -- --nocapture`.
</verification>

<output>
After completion, create `.planning/phases/10-scoop-legacy-path-and-state-cleanup/10-03-SUMMARY.md`
</output>

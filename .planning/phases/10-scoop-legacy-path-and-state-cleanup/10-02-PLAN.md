---
phase: 10-scoop-legacy-path-and-state-cleanup
plan: 2
type: execute
wave: 1
depends_on:
  - 10-01
files_modified:
  - spoon-backend/src/scoop/doctor.rs
  - spoon-backend/src/scoop/tests/state.rs
  - spoon-backend/src/control_plane
autonomous: true
requirements:
  - SLEG-01
  - SLEG-04
must_haves:
  truths:
    - "Scoop doctoring no longer preserves a dedicated legacy JSON-state worldview."
    - "The codebase stops treating old flat JSON state as a supported sub-model."
  artifacts:
    - path: "spoon-backend/src/scoop/doctor.rs"
      provides: "forward-only Scoop doctor behavior"
      contains: "current control-plane and runtime diagnostics without legacy JSON scaffolding"
---

<objective>
Remove dedicated legacy JSON-state handling from Scoop diagnostics.

Purpose: stop preserving the old Scoop control-plane worldview inside doctoring and adjacent cleanup logic.
Output: doctor and related backend logic align with the current control-plane reality only.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Delete legacy JSON-state doctor surfaces</name>
  <files>
spoon-backend/src/scoop/doctor.rs
spoon-backend/src/control_plane
  </files>
  <read_first>
.planning/phases/10-scoop-legacy-path-and-state-cleanup/10-CONTEXT.md
spoon-backend/src/scoop/doctor.rs
  </read_first>
  <action>Remove dedicated legacy JSON-state issue modeling and any doctor/control-plane glue that exists only to preserve the old flat JSON state worldview.</action>
  <verify>
    <automated>cargo check -p spoon-backend</automated>
  </verify>
  <acceptance_criteria>
- Scoop doctor no longer maintains dedicated legacy JSON-state diagnostics as a subsystem.
  </acceptance_criteria>
  <done>Scoop diagnostics are current-model-only instead of half current and half migration support.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite or remove tests that encode the legacy JSON-state worldview</name>
  <files>
spoon-backend/src/scoop/tests/state.rs
  </files>
  <read_first>
spoon-backend/src/scoop/tests/state.rs
spoon-backend/src/scoop/doctor.rs
  </read_first>
  <action>Update the targeted Scoop state/doctor tests so they no longer pin behavior around legacy JSON-state recognition as a supported concern.</action>
  <verify>
    <automated>cargo test -p spoon-backend --lib scoop::tests::state -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- Scoop state/doctor regressions reflect the forward-only backend model.
  </acceptance_criteria>
  <done>Tests stop preserving the deleted legacy worldview.</done>
</task>

</tasks>

<verification>
Run `cargo test -p spoon-backend --lib scoop::tests::state -- --nocapture` and `cargo check -p spoon-backend`.
</verification>

<output>
After completion, create `.planning/phases/10-scoop-legacy-path-and-state-cleanup/10-02-SUMMARY.md`
</output>

---
phase: 10-scoop-legacy-path-and-state-cleanup
plan: 4
type: execute
wave: 3
depends_on:
  - 10-01
  - 10-02
  - 10-03
files_modified:
  - spoon-backend/src/scoop/tests/runtime.rs
  - spoon-backend/src/scoop/tests/state.rs
  - spoon/tests/cli/scoop_flow.rs
  - .planning/phases/10-scoop-legacy-path-and-state-cleanup
autonomous: true
requirements:
  - SLEG-01
  - SLEG-04
must_haves:
  truths:
    - "The aggressive forward cleanup is protected by focused Scoop regressions."
    - "Phase 10 ships with explicit verification evidence."
  artifacts:
    - path: "spoon/tests/cli/scoop_flow.rs"
      provides: "thin app-shell regression confirmation after Scoop cleanup"
      contains: "user-visible Scoop flow protection after path/state cleanup"
---

<objective>
Refresh focused verification around the Scoop legacy path/state cleanup and close the phase cleanly.

Purpose: ensure the forward cleanup remains safe and recorded.
Output: targeted backend/app verification plus phase summaries.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Tighten Scoop backend/app regressions around the cleaned path/state model</name>
  <files>
spoon-backend/src/scoop/tests/runtime.rs
spoon-backend/src/scoop/tests/state.rs
spoon/tests/cli/scoop_flow.rs
  </files>
  <read_first>
spoon-backend/src/scoop/tests/runtime.rs
spoon-backend/src/scoop/tests/state.rs
spoon/tests/cli/scoop_flow.rs
  </read_first>
  <action>Adjust or add the smallest useful Scoop backend/app regressions needed to lock the cleaned path/state model close to the changed logic.</action>
  <verify>
    <automated>cargo test -p spoon --test scoop_flow -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- Focused regressions protect the cleaned Scoop path/state contract.
  </acceptance_criteria>
  <done>The cleanup is protected by targeted tests rather than memory.</done>
</task>

<task type="auto">
  <name>Task 2: Record phase verification and summaries</name>
  <files>
.planning/phases/10-scoop-legacy-path-and-state-cleanup
  </files>
  <read_first>
.planning/phases/10-scoop-legacy-path-and-state-cleanup/10-VALIDATION.md
  </read_first>
  <action>Write the four plan summaries and the phase verification file with the commands actually used and the remaining risk profile.</action>
  <verify>
    <automated>cargo check -p spoon-backend -p spoon</automated>
  </verify>
  <acceptance_criteria>
- Phase 10 has explicit summaries and verification evidence.
  </acceptance_criteria>
  <done>Phase 10 is documented as a complete, verifiable cleanup pass.</done>
</task>

</tasks>

<verification>
Run `cargo test -p spoon --test scoop_flow -- --nocapture` and `cargo check -p spoon-backend -p spoon`.
</verification>

<output>
After completion, create `.planning/phases/10-scoop-legacy-path-and-state-cleanup/10-04-SUMMARY.md` and `10-VERIFICATION.md`
</output>

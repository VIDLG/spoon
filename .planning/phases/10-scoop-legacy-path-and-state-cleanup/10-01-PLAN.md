---
phase: 10-scoop-legacy-path-and-state-cleanup
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - spoon-backend/src/layout.rs
  - spoon-backend/src/scoop/paths.rs
  - spoon-backend/src/scoop/mod.rs
  - spoon-backend/src/scoop/query.rs
  - spoon-backend/src/scoop/runtime
autonomous: true
requirements:
  - SLEG-01
  - SLEG-04
must_haves:
  truths:
    - "RuntimeLayout and ScoopLayout become the path source of truth for active Scoop code."
    - "JSON-era free-function path APIs no longer shape active runtime behavior."
  artifacts:
    - path: "spoon-backend/src/layout.rs"
      provides: "authoritative Scoop path model"
      contains: "layout-owned Scoop path semantics"
---

<objective>
Converge active Scoop path usage onto the authoritative layout model.

Purpose: make active Scoop code read directly from `RuntimeLayout` / `ScoopLayout` rather than through a legacy helper worldview.
Output: active backend/runtime/read-model code uses layout-owned path truth.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Remove JSON-era Scoop path helpers from active backend path flow</name>
  <files>
spoon-backend/src/layout.rs
spoon-backend/src/scoop/paths.rs
spoon-backend/src/scoop/mod.rs
  </files>
  <read_first>
.planning/phases/10-scoop-legacy-path-and-state-cleanup/10-CONTEXT.md
.planning/phases/10-scoop-legacy-path-and-state-cleanup/10-RESEARCH.md
spoon-backend/src/layout.rs
spoon-backend/src/scoop/paths.rs
  </read_first>
  <action>Delete or collapse JSON-era Scoop path helpers and make the layout model the canonical path API. Retain only layout-owned package path semantics if they materially improve readability.</action>
  <verify>
    <automated>cargo check -p spoon-backend -p spoon</automated>
  </verify>
  <acceptance_criteria>
- Active Scoop path truth comes from `RuntimeLayout` / `ScoopLayout`.
- Legacy helper APIs for old package-state or bucket-registry paths are removed from active usage.
  </acceptance_criteria>
  <done>Active Scoop code is layout-first rather than helper-first.</done>
</task>

<task type="auto">
  <name>Task 2: Update active runtime and read-model callers to the layout-first path style</name>
  <files>
spoon-backend/src/scoop/query.rs
spoon-backend/src/scoop/runtime
  </files>
  <read_first>
spoon-backend/src/scoop/query.rs
spoon-backend/src/scoop/runtime
  </read_first>
  <action>Update representative runtime and read-model callers so their path usage reflects the new layout-first design instead of lingering helper indirection.</action>
  <verify>
    <automated>cargo test -p spoon-backend runtime_writes_canonical_scoop_state -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- Representative Scoop callers follow the new path model directly.
  </acceptance_criteria>
  <done>Layout-first path ownership is visible in real Scoop code paths, not just in helper definitions.</done>
</task>

</tasks>

<verification>
Run `cargo check -p spoon-backend -p spoon` and `cargo test -p spoon-backend runtime_writes_canonical_scoop_state -- --nocapture`.
</verification>

<output>
After completion, create `.planning/phases/10-scoop-legacy-path-and-state-cleanup/10-01-SUMMARY.md`
</output>

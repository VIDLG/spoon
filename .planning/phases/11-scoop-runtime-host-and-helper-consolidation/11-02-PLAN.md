---
phase: 11-scoop-runtime-host-and-helper-consolidation
plan: 2
type: execute
wave: 1
depends_on:
  - 11-01
files_modified:
  - spoon-backend/src/scoop/lifecycle
  - spoon-backend/src/scoop/planner.rs
  - spoon-backend/src/scoop/state
autonomous: true
requirements:
  - SLEG-02
must_haves:
  truths:
    - "`lifecycle/` contains only true lifecycle stage modules."
    - "Planning and state glue no longer live under the lifecycle directory."
  artifacts:
    - path: "spoon-backend/src/scoop/lifecycle/mod.rs"
      provides: "pure lifecycle stage layout"
      contains: "only stage-oriented lifecycle modules"
---

<objective>
Purify the lifecycle layer.

Purpose: keep lifecycle as the true stage-oriented business layer by moving non-stage concerns out of it.
Output: cleaner lifecycle directory and clearer planner/state boundaries.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Move planner/state glue out of lifecycle</name>
  <files>
spoon-backend/src/scoop/lifecycle
spoon-backend/src/scoop/planner.rs
spoon-backend/src/scoop/state
  </files>
  <read_first>
spoon-backend/src/scoop/lifecycle/mod.rs
spoon-backend/src/scoop/lifecycle/planner.rs
spoon-backend/src/scoop/lifecycle/state.rs
spoon-backend/src/scoop/planner.rs
spoon-backend/src/scoop/state/mod.rs
  </read_first>
  <action>Relocate lifecycle-planner and lifecycle-state glue into clearer non-lifecycle homes so `lifecycle/` reads as a pure stage directory.</action>
  <verify>
    <automated>cargo check -p spoon-backend</automated>
  </verify>
  <acceptance_criteria>
- Non-stage modules are no longer housed under `lifecycle/`.
  </acceptance_criteria>
  <done>The lifecycle layer is semantically pure again.</done>
</task>

<task type="auto">
  <name>Task 2: Re-verify canonical Scoop install/update state behavior after the boundary move</name>
  <files>
spoon-backend/src/scoop/tests/runtime.rs
spoon-backend/src/scoop/tests/state.rs
  </files>
  <read_first>
spoon-backend/src/scoop/tests/runtime.rs
spoon-backend/src/scoop/tests/state.rs
  </read_first>
  <action>Update targeted backend tests so the lifecycle boundary cleanup remains tied to real Scoop state behavior.</action>
  <verify>
    <automated>cargo test -p spoon-backend runtime_writes_canonical_scoop_state -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- Canonical Scoop state behavior remains stable after lifecycle boundary cleanup.
  </acceptance_criteria>
  <done>Lifecycle purity did not destabilize actual Scoop state behavior.</done>
</task>

</tasks>

<verification>
Run `cargo check -p spoon-backend` and `cargo test -p spoon-backend runtime_writes_canonical_scoop_state -- --nocapture`.
</verification>

<output>
After completion, create `.planning/phases/11-scoop-runtime-host-and-helper-consolidation/11-02-SUMMARY.md`
</output>

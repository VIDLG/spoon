---
phase: 11-scoop-runtime-host-and-helper-consolidation
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - spoon-backend/src/scoop/runtime
  - spoon-backend/src/scoop/host
  - spoon-backend/src/scoop/actions.rs
  - spoon-backend/src/scoop/package_source.rs
  - spoon-backend/src/scoop/mod.rs
autonomous: true
requirements:
  - SLEG-02
must_haves:
  truths:
    - "The old `runtime` name no longer exists as the main Scoop edge-layer name."
    - "Operation entry and package source models live at the Scoop domain root."
  artifacts:
    - path: "spoon-backend/src/scoop/actions.rs"
      provides: "root Scoop operation entry"
      contains: "package action entrypoints owned by the domain root"
---

<objective>
Establish the new Scoop topology.

Purpose: rename the old runtime layer, move the real operation entry to the domain root, and relocate the package source model to a clearer home.
Output: Scoop topology that matches actual responsibilities.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Rename runtime to host and move root-worthy modules out of it</name>
  <files>
spoon-backend/src/scoop/runtime
spoon-backend/src/scoop/host
spoon-backend/src/scoop/actions.rs
spoon-backend/src/scoop/package_source.rs
spoon-backend/src/scoop/mod.rs
  </files>
  <read_first>
.planning/phases/11-scoop-runtime-host-and-helper-consolidation/11-CONTEXT.md
.planning/phases/11-scoop-runtime-host-and-helper-consolidation/11-RESEARCH.md
spoon-backend/src/scoop/mod.rs
spoon-backend/src/scoop/runtime/mod.rs
  </read_first>
  <action>Rename the current runtime layer to `host`, move operation entry into `actions.rs`, move the source model into `package_source.rs`, and update imports/exports accordingly.</action>
  <verify>
    <automated>cargo check -p spoon-backend -p spoon</automated>
  </verify>
  <acceptance_criteria>
- `runtime` no longer remains the primary directory/module name for the Scoop edge layer.
- `actions` and `package_source` live at the Scoop domain root.
  </acceptance_criteria>
  <done>The Scoop topology now matches the intended architectural story.</done>
</task>

<task type="auto">
  <name>Task 2: Keep representative package-action entry behavior intact after the topology move</name>
  <files>
spoon-backend/src/scoop
spoon-backend/src/scoop/tests/runtime.rs
  </files>
  <read_first>
spoon-backend/src/scoop/tests/runtime.rs
  </read_first>
  <action>Update representative runtime tests and imports so the new root action entry and host layer remain wired correctly.</action>
  <verify>
    <automated>cargo test -p spoon-backend scoop_action_contract_uses_context -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- Scoop action entry still works through the new topology.
  </acceptance_criteria>
  <done>Topology renaming did not break the domain entry contract.</done>
</task>

</tasks>

<verification>
Run `cargo check -p spoon-backend -p spoon` and `cargo test -p spoon-backend scoop_action_contract_uses_context -- --nocapture`.
</verification>

<output>
After completion, create `.planning/phases/11-scoop-runtime-host-and-helper-consolidation/11-01-SUMMARY.md`
</output>

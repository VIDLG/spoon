---
phase: 11-scoop-runtime-host-and-helper-consolidation
plan: 4
type: execute
wave: 3
depends_on:
  - 11-01
  - 11-02
  - 11-03
files_modified:
  - spoon-backend/src/scoop/tests/runtime.rs
  - spoon/tests/cli/scoop_flow.rs
  - .planning/phases/11-scoop-runtime-host-and-helper-consolidation
autonomous: true
requirements:
  - SLEG-02
  - BECT-05
must_haves:
  truths:
    - "The structural Scoop refactor is backed by targeted verification evidence."
    - "Phase 11 ends in a state that cleanly hands off to Phase 12."
  artifacts:
    - path: ".planning/phases/11-scoop-runtime-host-and-helper-consolidation/11-VERIFICATION.md"
      provides: "phase verification record"
      contains: "actual commands run and remaining risk"
---

<objective>
Verify and document the Phase 11 structural refactor.

Purpose: close the phase with focused evidence and a clean handoff to the read-model cleanup phase.
Output: plan summaries and verification record.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Re-run representative Scoop backend/app regressions after the structural refactor</name>
  <files>
spoon-backend/src/scoop/tests/runtime.rs
spoon/tests/cli/scoop_flow.rs
  </files>
  <read_first>
spoon-backend/src/scoop/tests/runtime.rs
spoon/tests/cli/scoop_flow.rs
  </read_first>
  <action>Adjust any targeted tests needed so the new Scoop topology remains protected by representative backend/app coverage.</action>
  <verify>
    <automated>cargo test -p spoon --test scoop_flow -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- Representative Scoop regressions still pass after the structural refactor.
  </acceptance_criteria>
  <done>The structural refactor is tied to concrete regression evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Record summaries and phase verification</name>
  <files>
.planning/phases/11-scoop-runtime-host-and-helper-consolidation
  </files>
  <read_first>
.planning/phases/11-scoop-runtime-host-and-helper-consolidation/11-VALIDATION.md
  </read_first>
  <action>Write plan summaries and the phase verification file with the commands actually used and the remaining risk profile.</action>
  <verify>
    <automated>cargo check -p spoon-backend -p spoon</automated>
  </verify>
  <acceptance_criteria>
- Phase 11 is documented with explicit execution and verification evidence.
  </acceptance_criteria>
  <done>Phase 11 is closed as a verifiable structural refactor rather than an open-ended reorg.</done>
</task>

</tasks>

<verification>
Run `cargo test -p spoon --test scoop_flow -- --nocapture` and `cargo check -p spoon-backend -p spoon`.
</verification>

<output>
After completion, create `.planning/phases/11-scoop-runtime-host-and-helper-consolidation/11-04-SUMMARY.md` and `11-VERIFICATION.md`
</output>

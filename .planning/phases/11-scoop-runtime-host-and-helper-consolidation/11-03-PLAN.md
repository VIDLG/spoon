---
phase: 11-scoop-runtime-host-and-helper-consolidation
plan: 3
type: execute
wave: 2
depends_on:
  - 11-01
  - 11-02
files_modified:
  - spoon-backend/src/scoop/mod.rs
  - spoon-backend/src/scoop/host
  - spoon-backend/src/scoop/query.rs
  - spoon-backend/src/scoop/info.rs
  - spoon-backend/src/scoop/projection.rs
autonomous: true
requirements:
  - SLEG-02
  - BECT-05
must_haves:
  truths:
    - "The host layer is thinner and the root facade is less catch-all."
    - "Required adjacent naming cleanup happens without turning Phase 11 into the full Phase 12 read-model pass."
  artifacts:
    - path: "spoon-backend/src/scoop/mod.rs"
      provides: "clearer Scoop domain boundary"
      contains: "narrower exports matching the new topology"
---

<objective>
Thin the host layer and root facade after the structural moves.

Purpose: make the new Scoop structure legible by narrowing exports and keeping the host edge focused.
Output: clearer public surface and less helper sprawl.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Shrink host and root exports to match the new structure</name>
  <files>
spoon-backend/src/scoop/mod.rs
spoon-backend/src/scoop/host
  </files>
  <read_first>
spoon-backend/src/scoop/mod.rs
spoon-backend/src/scoop/host/mod.rs
  </read_first>
  <action>Reduce over-broad root-module and host exports so the new topology stays visible and helper duplication does not immediately creep back in.</action>
  <verify>
    <automated>cargo check -p spoon-backend -p spoon</automated>
  </verify>
  <acceptance_criteria>
- The Scoop root module reads more like a domain boundary than a maximal re-export bag.
  </acceptance_criteria>
  <done>The new structure is visible in the public surface, not hidden behind broad exports.</done>
</task>

<task type="auto">
  <name>Task 2: Make only the minimum adjacent naming/read-model adjustment needed to keep the structural cleanup coherent</name>
  <files>
spoon-backend/src/scoop/query.rs
spoon-backend/src/scoop/info.rs
spoon-backend/src/scoop/projection.rs
  </files>
  <read_first>
spoon-backend/src/scoop/query.rs
spoon-backend/src/scoop/info.rs
spoon-backend/src/scoop/projection.rs
  </read_first>
  <action>Keep query/info split intact, but reduce or internalize any projection/helper naming that now obviously fights the new structural model.</action>
  <verify>
    <automated>cargo test -p spoon --test status_backend_flow -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- The structural refactor does not leave an obviously misleading helper/projection public story behind.
  </acceptance_criteria>
  <done>Phase 11 finishes structurally clean without overreaching into the full Phase 12 redundancy sweep.</done>
</task>

</tasks>

<verification>
Run `cargo check -p spoon-backend -p spoon` and `cargo test -p spoon --test status_backend_flow -- --nocapture`.
</verification>

<output>
After completion, create `.planning/phases/11-scoop-runtime-host-and-helper-consolidation/11-03-SUMMARY.md`
</output>

---
phase: 08-shared-backend-contract-hardening
plan: 2
type: execute
wave: 2
depends_on:
  - 08-01-PLAN.md
files_modified:
  - spoon-backend/src/error.rs
  - spoon-backend/src/msvc
  - spoon-backend/src/scoop
autonomous: true
requirements:
  - BECT-02
must_haves:
  truths:
    - "Backend errors distinguish domain, infrastructure, and user-action-needed semantics more clearly."
    - "High-value generic fallback cases are reduced without turning the error contract into an oversized framework."
  artifacts:
    - path: "spoon-backend/src/error.rs"
      provides: "tightened backend error contract"
      contains: "clearer error categories and reduced broad fallback usage"
---

<objective>
Tighten the shared backend error contract with forward design and controlled scope.

Purpose: reduce broad fallback drift and make backend failure semantics more legible to events/outcomes/doctor.
Output: clearer backend error categories without a maximal churn-heavy rewrite.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Tighten backend error categories and recurring fallback cases</name>
  <files>
spoon-backend/src/error.rs
spoon-backend/src/msvc
spoon-backend/src/scoop
  </files>
  <read_first>
.planning/phases/08-shared-backend-contract-hardening/08-CONTEXT.md
spoon-backend/src/error.rs
.planning/todos/pending/2026-03-31-tighten-backend-error-contract.md
  </read_first>
  <action>Refine the backend error contract so recurring high-value domain/infrastructure/user-action-needed cases are no longer broadly flattened into generic fallback variants where clearer categories would help.</action>
  <verify>
    <automated>cargo check -p spoon-backend</automated>
  </verify>
  <acceptance_criteria>
- Important recurring backend failures have clearer contract representation.
- The change remains a controlled hardening pass rather than a maximal rewrite.
  </acceptance_criteria>
  <done>Error handling is materially clearer without becoming overdesigned.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run nearby backend test coverage after error contract tightening</name>
  <files>
spoon-backend/src/msvc
spoon-backend/src/scoop
  </files>
  <read_first>
spoon-backend/src/msvc
spoon-backend/src/scoop
  </read_first>
  <action>Adjust nearby backend tests only as needed so the tighter error contract does not leave lifecycle/state/diagnostic coverage behind.</action>
  <verify>
    <automated>cargo test -p spoon-backend --lib -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- Backend test coverage remains coherent after error-contract tightening.
  </acceptance_criteria>
  <done>Error contract hardening is protected by nearby backend coverage.</done>
</task>

</tasks>

<verification>
Run `cargo check -p spoon-backend` and `cargo test -p spoon-backend --lib -- --nocapture`.
</verification>

<output>
After completion, create `.planning/phases/08-shared-backend-contract-hardening/08-02-SUMMARY.md`
</output>

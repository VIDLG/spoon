---
phase: 08-shared-backend-contract-hardening
plan: 3
type: execute
wave: 3
depends_on:
  - 08-02-PLAN.md
files_modified:
  - spoon-backend/src/fsx.rs
  - spoon-backend/src/archive.rs
  - spoon-backend/src/download.rs
  - spoon-backend/src/scoop
  - spoon-backend/src/msvc
autonomous: true
requirements:
  - BECT-03
must_haves:
  truths:
    - "`fsx` stays filesystem-primitive-focused."
    - "Shared archive/download (and maybe cache) primitives are extracted without collapsing domain workflows together."
  artifacts:
    - path: "spoon-backend/src/fsx.rs"
      provides: "narrow filesystem primitive boundary"
      contains: "filesystem-only helpers after primitive extraction"
---

<objective>
Extract shared backend IO primitives without turning them into one giant runtime abstraction.

Purpose: reduce duplicated download/extract/cache behavior across Scoop and MSVC while keeping domain workflows distinct.
Output: clearer shared primitive modules and a narrower `fsx`.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared archive/download primitives and narrow `fsx`</name>
  <files>
spoon-backend/src/fsx.rs
spoon-backend/src/archive.rs
spoon-backend/src/download.rs
spoon-backend/src/scoop
spoon-backend/src/msvc
  </files>
  <read_first>
.planning/phases/08-shared-backend-contract-hardening/08-CONTEXT.md
.planning/todos/pending/2026-03-31-consolidate-remaining-fsx-helpers.md
spoon-backend/src/fsx.rs
spoon-backend/src/scoop/extract.rs
spoon-backend/src/scoop/runtime/download.rs
spoon-backend/src/msvc/mod.rs
  </read_first>
  <action>Extract shared archive/download primitives where duplication is now clear, and keep `fsx` focused on filesystem primitives instead of becoming a backend junk drawer.</action>
  <verify>
    <automated>cargo check -p spoon-backend</automated>
  </verify>
  <acceptance_criteria>
- Shared IO primitives are extracted where duplication justifies it.
- `fsx` remains clearly filesystem-focused.
  </acceptance_criteria>
  <done>Cross-domain primitive reuse improves without collapsing domain workflows into one abstraction.</done>
</task>

<task type="auto">
  <name>Task 2: Revalidate representative MSVC/Scoop flow coverage after primitive extraction</name>
  <files>
spoon/tests/tui/tui_msvc_download_flow.rs
spoon/tests/cli/scoop_flow.rs
  </files>
  <read_first>
spoon/tests/tui/tui_msvc_download_flow.rs
spoon/tests/cli/scoop_flow.rs
  </read_first>
  <action>Re-run and adjust representative flow regressions only as needed so the shared primitive extraction does not break app-visible MSVC/Scoop behavior.</action>
  <verify>
    <automated>cargo test -p spoon --test tui_msvc_download_flow -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- Representative app-visible flow coverage remains green after primitive extraction.
  </acceptance_criteria>
  <done>Shared primitive refactors remain safe for both runtime domains.</done>
</task>

</tasks>

<verification>
Run `cargo check -p spoon-backend` and `cargo test -p spoon --test tui_msvc_download_flow -- --nocapture`.
</verification>

<output>
After completion, create `.planning/phases/08-shared-backend-contract-hardening/08-03-SUMMARY.md`
</output>

---
phase: 08-shared-backend-contract-hardening
plan: 4
type: execute
wave: 4
depends_on:
  - 08-03-PLAN.md
files_modified:
  - spoon-backend/src/ports.rs
  - spoon-backend/src/layout.rs
  - spoon-backend/src/scoop/runtime/execution.rs
  - spoon-backend/src/scoop/runtime/surface.rs
  - spoon/src/service/mod.rs
autonomous: true
requirements:
  - BECT-03
  - BECT-04
must_haves:
  truths:
    - "Port boundaries are clearer and no longer carry obvious overlap or stray path-source duties."
    - "Targeted JSON-era layout/path leftovers are removed or downgraded."
  artifacts:
    - path: "spoon-backend/src/ports.rs"
      provides: "narrower host-system contract"
      contains: "host mutation capability without stray path-source responsibilities"
---

<objective>
Clean up shared port boundaries and targeted JSON-era layout/path leftovers.

Purpose: remove stale control-plane-era concepts and make shared runtime/path contracts easier to reason about.
Output: narrower host ports, cleaner layout contracts, and reduced JSON-era path residue.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Narrow `SystemPort` and reduce `ScoopRuntimeHost` overlap</name>
  <files>
spoon-backend/src/ports.rs
spoon-backend/src/scoop/runtime/execution.rs
spoon/src/service/mod.rs
  </files>
  <read_first>
.planning/phases/08-shared-backend-contract-hardening/08-CONTEXT.md
.planning/todos/pending/2026-04-01-simplify-system-port-and-runtime-host-boundaries.md
spoon-backend/src/ports.rs
spoon-backend/src/scoop/runtime/execution.rs
spoon/src/service/mod.rs
  </read_first>
  <action>Remove `home_dir()` from `SystemPort`, narrow the generic host-system contract to genuine environment mutation, and reduce duplicate generic capability definitions inside `ScoopRuntimeHost`.</action>
  <verify>
    <automated>cargo check -p spoon-backend -p spoon</automated>
  </verify>
  <acceptance_criteria>
- `SystemPort` becomes narrower and easier to reason about.
- Generic host-system capability overlap is reduced.
  </acceptance_criteria>
  <done>Shared host/port boundaries are cleaner and less redundant.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep targeted JSON-era layout/path leftovers</name>
  <files>
spoon-backend/src/layout.rs
spoon-backend/src/scoop
spoon/src/cli
spoon/src/service
  </files>
  <read_first>
.planning/phases/08-shared-backend-contract-hardening/08-CONTEXT.md
.planning/todos/pending/2026-04-01-audit-derive-not-store-fields.md
spoon-backend/src/layout.rs
  </read_first>
  <action>Do a targeted cleanup of JSON-era layout/path concepts such as `package_state_root`, `bucket_registry_path`, and related stale helpers or assumptions that no longer match the SQLite-backed control plane.</action>
  <verify>
    <automated>cargo test -p spoon --test scoop_flow -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- JSON-era path/layout leftovers are materially reduced without turning into a repo-wide path refactor.
  </acceptance_criteria>
  <done>Shared path contracts match the current SQLite-backed backend reality more closely.</done>
</task>

</tasks>

<verification>
Run `cargo check -p spoon-backend -p spoon` and `cargo test -p spoon --test scoop_flow -- --nocapture`.
</verification>

<output>
After completion, create `.planning/phases/08-shared-backend-contract-hardening/08-04-SUMMARY.md`
</output>

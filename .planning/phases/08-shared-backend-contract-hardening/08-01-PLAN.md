---
phase: 08-shared-backend-contract-hardening
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - spoon-backend/src/event.rs
  - spoon/src/service/mod.rs
  - spoon-backend/src/msvc
  - spoon-backend/src/scoop
autonomous: true
requirements:
  - BECT-01
must_haves:
  truths:
    - "Backend event semantics are forward-designed and no longer rely on weak stringly progress categories as the core contract."
    - "App/backend/tests move together with no backward-compatibility shim layer."
  artifacts:
    - path: "spoon-backend/src/event.rs"
      provides: "reset shared backend event contract"
      contains: "stronger event typing and clearer event categories"
---

<objective>
Reset the shared backend event contract using forward design.

Purpose: replace the current overloaded event shape with a clearer shared contract for backend/app/tests.
Output: stronger event taxonomy used consistently by Scoop, MSVC, app translation, and tests.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Redesign backend events around clearer semantic categories</name>
  <files>
spoon-backend/src/event.rs
spoon-backend/src/msvc
spoon-backend/src/scoop
  </files>
  <read_first>
.planning/phases/08-shared-backend-contract-hardening/08-CONTEXT.md
.planning/phases/08-shared-backend-contract-hardening/08-RESEARCH.md
spoon-backend/src/event.rs
  </read_first>
  <action>Replace the weak/overloaded backend event shape with a stronger forward-designed contract that separates stage/progress/notice/finish semantics and avoids weak stringly categories as the central API.</action>
  <verify>
    <automated>cargo check -p spoon-backend -p spoon</automated>
  </verify>
  <acceptance_criteria>
- Backend events use clearer semantic categories and stronger typing.
- There is no backward-compatibility shim layer preserving the old event contract.
  </acceptance_criteria>
  <done>The backend event contract is reset onto a stronger forward-designed shape.</done>
</task>

<task type="auto">
  <name>Task 2: Align app translation and MSVC/Scoop flow tests to the new event contract</name>
  <files>
spoon/src/service/mod.rs
spoon/tests/cli/msvc_flow.rs
spoon/tests/cli/scoop_flow.rs
  </files>
  <read_first>
spoon/src/service/mod.rs
spoon/tests/cli/msvc_flow.rs
spoon/tests/cli/scoop_flow.rs
  </read_first>
  <action>Update app translation and representative flow regressions so the reset event contract is consumed directly rather than adapted through compatibility glue.</action>
  <verify>
    <automated>cargo test -p spoon --test msvc_flow -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- App/test surfaces consume the new event contract directly.
  </acceptance_criteria>
  <done>Backend and app now agree on the stronger event contract.</done>
</task>

</tasks>

<verification>
Run `cargo check -p spoon-backend -p spoon` and `cargo test -p spoon --test msvc_flow -- --nocapture`.
</verification>

<output>
After completion, create `.planning/phases/08-shared-backend-contract-hardening/08-01-SUMMARY.md`
</output>

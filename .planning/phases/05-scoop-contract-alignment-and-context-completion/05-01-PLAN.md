---
phase: 05-scoop-contract-alignment-and-context-completion
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - spoon-backend/src/scoop/tests/runtime.rs
  - spoon-backend/src/scoop/state/store.rs
  - spoon-backend/src/control_plane/sqlite.rs
autonomous: true
requirements:
  - TEST-03
must_haves:
  truths:
    - "Backend Scoop regressions no longer expect removed JSON package-state files."
    - "Runtime canonical-state tests verify the actual SQLite-backed control-plane contract."
  artifacts:
    - path: "spoon-backend/src/scoop/tests/runtime.rs"
      provides: "Backend stale regression alignment with SQLite/canonical state"
      contains: "runtime_writes_canonical_scoop_state"
---

<objective>
Migrate stale backend Scoop regressions onto the current SQLite-backed canonical contract.

Purpose: close the backend-side audit blocker where tests still expect removed JSON package-state persistence.
Output: backend runtime/state regressions that assert the current control-plane reality.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite stale backend runtime regression to assert SQLite control-plane persistence</name>
  <files>
spoon-backend/src/scoop/tests/runtime.rs
spoon-backend/src/scoop/state/store.rs
spoon-backend/src/control_plane/sqlite.rs
  </files>
  <read_first>
.planning/phases/05-scoop-contract-alignment-and-context-completion/05-CONTEXT.md
.planning/phases/05-scoop-contract-alignment-and-context-completion/05-RESEARCH.md
spoon-backend/src/scoop/tests/runtime.rs
spoon-backend/src/scoop/state/store.rs
spoon-backend/src/control_plane/sqlite.rs
  </read_first>
  <action>Update `runtime_writes_canonical_scoop_state` and any nearby stale backend assertions so they verify SQLite-backed canonical persistence, control-plane pathing, and typed store reads instead of looking for `packages/*.json` files.</action>
  <verify>
    <automated>cargo test -p spoon-backend runtime_writes_canonical_scoop_state -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- `runtime_writes_canonical_scoop_state` passes.
- No backend runtime regression in this area still assumes flat JSON package-state persistence.
  </acceptance_criteria>
  <done>Backend runtime verification now matches the shipped SQLite/canonical state contract.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run nearby backend runtime coverage to ensure stale assumptions are gone</name>
  <files>
spoon-backend/src/scoop/tests/runtime.rs
  </files>
  <read_first>
spoon-backend/src/scoop/tests/runtime.rs
  </read_first>
  <action>Run and adjust nearby runtime regressions only as needed so the backend runtime test surface around canonical state and lifecycle no longer mixes JSON-era assumptions with current control-plane behavior.</action>
  <verify>
    <automated>cargo test -p spoon-backend --lib scoop::tests::runtime -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- The backend runtime test module passes after the stale contract migration.
  </acceptance_criteria>
  <done>Backend-local Scoop regression coverage is internally consistent with SQLite/canonical state.</done>
</task>

</tasks>

<verification>
Run `cargo test -p spoon-backend runtime_writes_canonical_scoop_state -- --nocapture` and `cargo test -p spoon-backend --lib scoop::tests::runtime -- --nocapture`.
</verification>

<output>
After completion, create `.planning/phases/05-scoop-contract-alignment-and-context-completion/05-01-SUMMARY.md`
</output>


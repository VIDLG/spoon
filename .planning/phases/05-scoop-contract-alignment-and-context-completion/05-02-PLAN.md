---
phase: 05-scoop-contract-alignment-and-context-completion
plan: 2
type: execute
wave: 1
depends_on:
  - 05-01-PLAN.md
files_modified:
  - spoon/tests/cli/scoop_flow.rs
  - spoon/tests/common/setup.rs
  - spoon/tests/common/scoop.rs
autonomous: true
requirements:
  - TEST-02
must_haves:
  truths:
    - "CLI Scoop regression flows no longer seed removed JSON package-state files."
    - "App-shell regressions verify backend read models and canonical state instead of legacy flat JSON artifacts."
  artifacts:
    - path: "spoon/tests/cli/scoop_flow.rs"
      provides: "App stale regression alignment with SQLite/canonical state"
      contains: "scoop_status_lists_buckets_and_installed_packages"
---

<objective>
Migrate stale CLI Scoop regressions onto the current backend contract.

Purpose: close the app-side audit blocker where CLI tests still seed the removed JSON package-state world.
Output: CLI regressions that assert the same SQLite/canonical read model the product now uses.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Update status/list Scoop CLI regressions to seed canonical state through backend APIs</name>
  <files>
spoon/tests/cli/scoop_flow.rs
spoon/tests/common/setup.rs
spoon/tests/common/scoop.rs
  </files>
  <read_first>
.planning/phases/05-scoop-contract-alignment-and-context-completion/05-CONTEXT.md
.planning/phases/05-scoop-contract-alignment-and-context-completion/05-RESEARCH.md
spoon/tests/cli/scoop_flow.rs
spoon/tests/cli/status_backend_flow.rs
spoon/tests/common/setup.rs
spoon/tests/common/scoop.rs
  </read_first>
  <action>Migrate stale CLI Scoop status/list regressions so setup goes through backend bucket/state APIs and assertions validate the SQLite-backed canonical contract rather than `scoop/state/packages/*.json` files.</action>
  <verify>
    <automated>cargo test -p spoon --test scoop_flow scoop_status_lists_buckets_and_installed_packages -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- `scoop_status_lists_buckets_and_installed_packages` passes.
- `scoop_list_lists_installed_packages` passes.
  </acceptance_criteria>
  <done>Core stale CLI regressions are aligned with the current backend contract.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run the full CLI Scoop flow suite and clean nearby stale setup assumptions</name>
  <files>
spoon/tests/cli/scoop_flow.rs
  </files>
  <read_first>
spoon/tests/cli/scoop_flow.rs
  </read_first>
  <action>Run the full `scoop_flow` suite and clean any remaining stale contract assumptions in this file only where needed to make it consistent with the shipped SQLite/canonical design.</action>
  <verify>
    <automated>cargo test -p spoon --test scoop_flow -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- The full `scoop_flow` suite passes.
  </acceptance_criteria>
  <done>App-side Scoop flow regressions no longer depend on removed JSON package-state behavior.</done>
</task>

</tasks>

<verification>
Run `cargo test -p spoon --test scoop_flow scoop_status_lists_buckets_and_installed_packages -- --nocapture`, `cargo test -p spoon --test scoop_flow scoop_list_lists_installed_packages -- --nocapture`, and `cargo test -p spoon --test scoop_flow -- --nocapture`.
</verification>

<output>
After completion, create `.planning/phases/05-scoop-contract-alignment-and-context-completion/05-02-SUMMARY.md`
</output>


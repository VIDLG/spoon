---
phase: 03-scoop-lifecycle-split-and-app-thinning
plan: 5
type: execute
wave: 5
depends_on:
  - 03-01
  - 03-02
  - 03-03
  - 03-04
files_modified:
  - spoon/src/service/scoop/actions.rs
  - spoon/src/service/scoop/runtime.rs
  - spoon/src/service/scoop/mod.rs
  - spoon/src/actions/execute/mod.rs
  - spoon/tests/cli/scoop_runtime_flow.rs
  - spoon/tests/cli/status_backend_flow.rs
autonomous: true
requirements:
  - SCLF-01
  - SCLF-02
  - SCLF-03
  - SCLF-04
must_haves:
  truths:
    - "App-side Scoop service code only translates requests, events, and outcomes."
    - "Backend stage semantics are authoritative and app code does not invent a parallel lifecycle model."
  artifacts:
    - path: "spoon/src/service/scoop/actions.rs"
      provides: "Translation-only app adapter"
      contains: "command_result_from_scoop_package_outcome"
    - path: "spoon/src/service/scoop/runtime.rs"
      provides: "Event translation over backend lifecycle semantics"
      contains: "stream_chunk_from_backend_event"
---

<objective>
Thin the app shell so it only translates backend lifecycle semantics instead of orchestrating Scoop behavior.

Purpose: satisfy the app-shell side of SCLF-01 through SCLF-04 by making backend lifecycle semantics authoritative.
Output: translation-only app adapters and regressions proving the app consumes backend semantics rather than inventing its own.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Reduce app-side Scoop service code to request/event/outcome translation only</name>
  <files>
spoon/src/service/scoop/actions.rs
spoon/src/service/scoop/runtime.rs
spoon/src/service/scoop/mod.rs
spoon/src/actions/execute/mod.rs
  </files>
  <read_first>
.planning/phases/03-scoop-lifecycle-split-and-app-thinning/03-CONTEXT.md
.planning/phases/03-scoop-lifecycle-split-and-app-thinning/03-RESEARCH.md
spoon/src/service/scoop/actions.rs
spoon/src/service/scoop/runtime.rs
spoon/src/actions/execute/mod.rs
spoon-backend/src/event.rs
  </read_first>
  <action>Remove any remaining lifecycle knowledge from `spoon/src/service/scoop/*` and related action wiring so the app only builds requests/contexts, translates structured backend events to UI/CLI output, maps backend outcomes, and handles cancellation. The app must not invent stage names, infer lifecycle order, or reconstruct backend state semantics.</action>
  <verify>
    <automated>cargo test -p spoon --test scoop_runtime_flow -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- `Select-String -Path spoon/src/service/scoop/runtime.rs -Pattern 'BackendEvent'` returns a match.
- `Select-String -Path spoon/src/service/scoop/actions.rs -Pattern 'command_result_from_scoop_package_outcome'` returns a match.
- `Select-String -Path spoon/src/service/scoop/actions.rs -Pattern 'refresh_current_entry|write_shims|write_shortcuts|restore_persist_entries_into_root'` returns no matches.
  </acceptance_criteria>
  <done>The app shell translates backend lifecycle semantics instead of orchestrating them.</done>
</task>

<task type="auto">
  <name>Task 2: Add app-shell regressions proving backend stage semantics stay authoritative</name>
  <files>
spoon/tests/cli/scoop_runtime_flow.rs
spoon/tests/cli/status_backend_flow.rs
  </files>
  <read_first>
.planning/phases/03-scoop-lifecycle-split-and-app-thinning/03-CONTEXT.md
.planning/phases/03-scoop-lifecycle-split-and-app-thinning/03-RESEARCH.md
spoon/tests/cli/scoop_runtime_flow.rs
spoon/tests/cli/status_backend_flow.rs
  </read_first>
  <action>Add or update regressions so CLI/TUI-facing flows prove the app is consuming backend lifecycle semantics rather than inventing a shadow model. Keep `json_status_uses_backend_read_models` green and add a runtime-flow assertion that install/update/uninstall progress still renders from backend events after the service layer is thinned.</action>
  <verify>
    <automated>cargo test -p spoon --test status_backend_flow json_status_uses_backend_read_models -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- `Select-String -Path spoon/tests/cli/status_backend_flow.rs -Pattern 'json_status_uses_backend_read_models'` returns a match.
- `Select-String -Path spoon/tests/cli/scoop_runtime_flow.rs -Pattern 'install|update|uninstall'` returns a match in the regression area updated for backend-stage rendering.
  </acceptance_criteria>
  <done>The app shell boundary is pinned by tests to translation-only behavior.</done>
</task>

</tasks>

<verification>
Run `cargo test -p spoon --test scoop_runtime_flow -- --nocapture` and `cargo test -p spoon --test status_backend_flow json_status_uses_backend_read_models -- --nocapture`.
</verification>

<output>
After completion, create `.planning/phases/03-scoop-lifecycle-split-and-app-thinning/03-05-SUMMARY.md`
</output>

---
phase: 03-scoop-lifecycle-split-and-app-thinning
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - spoon-backend/src/event.rs
  - spoon-backend/src/control_plane/journal_store.rs
  - spoon-backend/src/scoop/runtime/actions.rs
  - spoon-backend/src/scoop/tests/runtime.rs
  - spoon-backend/src/tests/control_plane.rs
autonomous: true
requirements:
  - SCLF-01
  - SCLF-04
must_haves:
  truths:
    - "Backend lifecycle stages are a stable contract, not just free-form progress labels."
    - "Journal writes and backend events reflect the same stage semantics."
  artifacts:
    - path: "spoon-backend/src/event.rs"
      provides: "Structured lifecycle stage event contract"
      contains: "stage"
    - path: "spoon-backend/src/control_plane/journal_store.rs"
      provides: "Journal stage persistence aligned with lifecycle contract"
      contains: "set_operation_stage"
---

<objective>
Define the formal lifecycle stage/event/journal contract for Scoop lifecycle execution before deeper module extraction.

Purpose: lock the Phase 3 stage vocabulary so backend orchestration, journaling, doctor semantics, and UI translation all share the same contract.
Output: structured lifecycle event shape, journal stage semantics, and focused regressions.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add structured lifecycle stage semantics to backend events and journal helpers</name>
  <files>
spoon-backend/src/event.rs
spoon-backend/src/control_plane/journal_store.rs
spoon-backend/src/scoop/runtime/actions.rs
  </files>
  <read_first>
.planning/phases/03-scoop-lifecycle-split-and-app-thinning/03-CONTEXT.md
.planning/phases/03-scoop-lifecycle-split-and-app-thinning/03-RESEARCH.md
spoon-backend/src/event.rs
spoon-backend/src/control_plane/journal_store.rs
spoon-backend/src/scoop/runtime/actions.rs
  </read_first>
  <action>Add explicit lifecycle stage semantics to `BackendEvent` / progress signaling so backend orchestration can emit authoritative stage information instead of only generic labels. Extend `journal_store.rs` so stage updates use the same stable stage names agreed in Phase 3 discuss. Update `runtime/actions.rs` enough to emit those stage semantics for install/update/uninstall/reapply entry points at the appropriate boundaries, without yet performing the full module split.</action>
  <verify>
    <automated>cargo test -p spoon-backend install_lifecycle_emits_stage_contract -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- `Select-String -Path spoon-backend/src/event.rs -Pattern 'stage'` returns a match for structured lifecycle semantics.
- `Select-String -Path spoon-backend/src/control_plane/journal_store.rs -Pattern 'set_operation_stage'` returns a match.
- `Select-String -Path spoon-backend/src/scoop/runtime/actions.rs -Pattern 'planned|acquiring|materializing|persist_restoring|surface_applying|state_committing'` returns matches.
  </acceptance_criteria>
  <done>Stage names become a formal backend contract shared by events and journal writes.</done>
</task>

<task type="auto">
  <name>Task 2: Add regressions for install and uninstall stage contract emission</name>
  <files>
spoon-backend/src/scoop/tests/runtime.rs
spoon-backend/src/tests/control_plane.rs
  </files>
  <read_first>
.planning/phases/03-scoop-lifecycle-split-and-app-thinning/03-CONTEXT.md
.planning/phases/03-scoop-lifecycle-split-and-app-thinning/03-RESEARCH.md
spoon-backend/src/scoop/tests/runtime.rs
spoon-backend/src/tests/control_plane.rs
spoon-backend/src/event.rs
  </read_first>
  <action>Add tests named `install_lifecycle_emits_stage_contract` and `uninstall_lifecycle_emits_stage_contract` that prove the agreed stage names are emitted and/or journaled in order for install and uninstall paths. The tests should fail if lifecycle code falls back to free-form message-only semantics.</action>
  <verify>
    <automated>cargo test -p spoon-backend uninstall_lifecycle_emits_stage_contract -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- `Select-String -Path spoon-backend/src/scoop/tests/runtime.rs -Pattern 'install_lifecycle_emits_stage_contract'` returns a match.
- `Select-String -Path spoon-backend/src/scoop/tests/runtime.rs -Pattern 'uninstall_lifecycle_emits_stage_contract'` returns a match.
  </acceptance_criteria>
  <done>The lifecycle stage contract is pinned by backend tests before deeper module extraction.</done>
</task>

</tasks>

<verification>
Run `cargo test -p spoon-backend install_lifecycle_emits_stage_contract -- --nocapture` and `cargo test -p spoon-backend uninstall_lifecycle_emits_stage_contract -- --nocapture`.
</verification>

<output>
After completion, create `.planning/phases/03-scoop-lifecycle-split-and-app-thinning/03-01-SUMMARY.md`
</output>

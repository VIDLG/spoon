---
phase: 03-scoop-lifecycle-split-and-app-thinning
plan: 4
type: execute
wave: 4
depends_on:
  - 03-01
  - 03-03
files_modified:
  - spoon-backend/src/scoop/lifecycle/uninstall.rs
  - spoon-backend/src/scoop/lifecycle/reapply.rs
  - spoon-backend/src/scoop/runtime/hooks.rs
  - spoon-backend/src/scoop/runtime/actions.rs
  - spoon-backend/src/scoop/tests/runtime.rs
autonomous: true
requirements:
  - SCLF-03
  - SCLF-04
  - SCLF-05
must_haves:
  truths:
    - "Uninstall and reapply are explicit lifecycle entry points over the shared lifecycle contract."
    - "Hooks remain centralized, and hook failure policy matches the discuss decisions."
  artifacts:
    - path: "spoon-backend/src/scoop/lifecycle/uninstall.rs"
      provides: "Explicit uninstall lifecycle entry point"
      contains: "uninstall"
    - path: "spoon-backend/src/scoop/lifecycle/reapply.rs"
      provides: "Explicit reapply lifecycle entry point"
      contains: "reapply"
---

<objective>
Extract uninstall and reapply as explicit lifecycle entry points that reuse the shared lifecycle contract and centralized hooks module.

Purpose: satisfy SCLF-03, SCLF-04, and SCLF-05 without letting reapply collapse into reinstall or hooks scatter back into entry files.
Output: explicit uninstall/reapply lifecycle modules and regressions for hook policy and reapply behavior.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Extract uninstall and reapply lifecycle modules over the shared back-half contract</name>
  <files>
spoon-backend/src/scoop/lifecycle/uninstall.rs
spoon-backend/src/scoop/lifecycle/reapply.rs
spoon-backend/src/scoop/runtime/actions.rs
spoon-backend/src/scoop/runtime/hooks.rs
  </files>
  <read_first>
.planning/phases/03-scoop-lifecycle-split-and-app-thinning/03-CONTEXT.md
.planning/phases/03-scoop-lifecycle-split-and-app-thinning/03-RESEARCH.md
spoon-backend/src/scoop/runtime/actions.rs
spoon-backend/src/scoop/runtime/hooks.rs
spoon-backend/src/scoop/runtime/integration.rs
spoon-backend/src/scoop/runtime/surface.rs
  </read_first>
  <action>Create explicit lifecycle modules for `uninstall` and `reapply`. Route `uninstall` through the agreed stages `pre_uninstall_hooks -> uninstalling -> persist_syncing -> surface_removing -> state_removing -> post_uninstall_hooks`, with `post_uninstall_hooks` warning-only. Route `reapply` through `persist_restoring -> surface_applying -> integrating -> state_committing`, and do not let `reapply` invoke install/uninstall hooks. Keep `hooks.rs` centralized as the shared execution engine for hook rendering and execution.</action>
  <verify>
    <automated>cargo test -p spoon-backend uninstall_and_reapply_use_shared_lifecycle_contract -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- `Select-String -Path spoon-backend/src/scoop/lifecycle/uninstall.rs -Pattern 'uninstall'` returns a match.
- `Select-String -Path spoon-backend/src/scoop/lifecycle/reapply.rs -Pattern 'reapply'` returns a match.
- `Select-String -Path spoon-backend/src/scoop/runtime/hooks.rs -Pattern 'execute_hook_scripts'` returns a match.
  </acceptance_criteria>
  <done>Uninstall and reapply become explicit lifecycle entries without duplicating hook execution logic.</done>
</task>

<task type="auto">
  <name>Task 2: Add regressions for uninstall/reapply contract and warning-only post-uninstall hooks</name>
  <files>
spoon-backend/src/scoop/tests/runtime.rs
  </files>
  <read_first>
.planning/phases/03-scoop-lifecycle-split-and-app-thinning/03-CONTEXT.md
.planning/phases/03-scoop-lifecycle-split-and-app-thinning/03-RESEARCH.md
spoon-backend/src/scoop/tests/runtime.rs
  </read_first>
  <action>Add backend regressions named `uninstall_and_reapply_use_shared_lifecycle_contract` and `post_uninstall_hook_is_warning_only`. The first should prove uninstall/reapply use the agreed stage contract and do not regress into ad hoc behavior. The second should prove `post_uninstall` failure is recorded as warning-only rather than making uninstall unsuccessful.</action>
  <verify>
    <automated>cargo test -p spoon-backend post_uninstall_hook_is_warning_only -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- `Select-String -Path spoon-backend/src/scoop/tests/runtime.rs -Pattern 'uninstall_and_reapply_use_shared_lifecycle_contract'` returns a match.
- `Select-String -Path spoon-backend/src/scoop/tests/runtime.rs -Pattern 'post_uninstall_hook_is_warning_only'` returns a match.
  </acceptance_criteria>
  <done>Uninstall/reapply behavior and warning-only post-uninstall hooks are pinned by tests.</done>
</task>

</tasks>

<verification>
Run `cargo test -p spoon-backend uninstall_and_reapply_use_shared_lifecycle_contract -- --nocapture` and `cargo test -p spoon-backend post_uninstall_hook_is_warning_only -- --nocapture`.
</verification>

<output>
After completion, create `.planning/phases/03-scoop-lifecycle-split-and-app-thinning/03-04-SUMMARY.md`
</output>

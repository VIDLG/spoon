---
phase: 03-scoop-lifecycle-split-and-app-thinning
plan: 2
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - spoon-backend/src/scoop/lifecycle/planner.rs
  - spoon-backend/src/scoop/lifecycle/acquire.rs
  - spoon-backend/src/scoop/lifecycle/materialize.rs
  - spoon-backend/src/scoop/runtime/actions.rs
  - spoon-backend/src/scoop/runtime/mod.rs
  - spoon-backend/src/scoop/tests/runtime.rs
  - spoon/tests/cli/scoop_runtime_flow.rs
autonomous: true
requirements:
  - SCLF-01
  - SCLF-02
  - SCLF-05
must_haves:
  truths:
    - "Install and update share the same front-half lifecycle modules instead of ad hoc inline orchestration."
    - "The monolith in `runtime/actions.rs` no longer owns planner/acquire/materialize logic directly."
  artifacts:
    - path: "spoon-backend/src/scoop/lifecycle/planner.rs"
      provides: "Explicit lifecycle planner module"
      contains: "plan"
    - path: "spoon-backend/src/scoop/lifecycle/acquire.rs"
      provides: "Explicit payload acquisition module"
      contains: "acquire"
    - path: "spoon-backend/src/scoop/lifecycle/materialize.rs"
      provides: "Explicit materialization module"
      contains: "materialize"
---

<objective>
Extract the install/update front half into explicit planner/acquire/materialize lifecycle modules.

Purpose: satisfy the install/update phase structure half of SCLF-01, SCLF-02, and SCLF-05.
Output: explicit front-half lifecycle modules and install/update orchestration routed through them.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Extract planner, acquire, and materialize modules for install/update</name>
  <files>
spoon-backend/src/scoop/lifecycle/planner.rs
spoon-backend/src/scoop/lifecycle/acquire.rs
spoon-backend/src/scoop/lifecycle/materialize.rs
spoon-backend/src/scoop/runtime/actions.rs
spoon-backend/src/scoop/runtime/mod.rs
  </files>
  <read_first>
.planning/phases/03-scoop-lifecycle-split-and-app-thinning/03-CONTEXT.md
.planning/phases/03-scoop-lifecycle-split-and-app-thinning/03-RESEARCH.md
spoon-backend/src/scoop/runtime/actions.rs
spoon-backend/src/scoop/runtime/source.rs
spoon-backend/src/scoop/runtime/download.rs
spoon-backend/src/scoop/extract.rs
  </read_first>
  <action>Create lifecycle modules for `planner`, `acquire`, and `materialize` and move the corresponding logic out of `runtime/actions.rs`. Install/update entry points should orchestrate through those modules instead of owning that logic inline. Keep behavior and stage order stable while extracting.</action>
  <verify>
    <automated>cargo test -p spoon-backend install_update_share_front_half_lifecycle_modules -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- `Select-String -Path spoon-backend/src/scoop/lifecycle/planner.rs -Pattern 'plan'` returns a match.
- `Select-String -Path spoon-backend/src/scoop/lifecycle/acquire.rs -Pattern 'acquire'` returns a match.
- `Select-String -Path spoon-backend/src/scoop/lifecycle/materialize.rs -Pattern 'materialize'` returns a match.
  </acceptance_criteria>
  <done>Install/update front-half lifecycle work is explicitly modularized.</done>
</task>

<task type="auto">
  <name>Task 2: Add regressions proving install/update share the same front-half lifecycle shape</name>
  <files>
spoon-backend/src/scoop/tests/runtime.rs
spoon/tests/cli/scoop_runtime_flow.rs
  </files>
  <read_first>
.planning/phases/03-scoop-lifecycle-split-and-app-thinning/03-CONTEXT.md
.planning/phases/03-scoop-lifecycle-split-and-app-thinning/03-RESEARCH.md
spoon-backend/src/scoop/tests/runtime.rs
spoon/tests/cli/scoop_runtime_flow.rs
  </read_first>
  <action>Add `install_update_share_front_half_lifecycle_modules` in backend runtime tests and keep the existing CLI install/update flow green to prove the extraction did not reintroduce app-side orchestration gaps.</action>
  <verify>
    <automated>cargo test -p spoon --test scoop_runtime_flow spoon_scoop_package_cli_handles_install_update_uninstall_with_local_bucket_source -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- `Select-String -Path spoon-backend/src/scoop/tests/runtime.rs -Pattern 'install_update_share_front_half_lifecycle_modules'` returns a match.
- `Select-String -Path spoon/tests/cli/scoop_runtime_flow.rs -Pattern 'spoon_scoop_package_cli_handles_install_update_uninstall_with_local_bucket_source'` returns a match.
  </acceptance_criteria>
  <done>Install/update front-half extraction is protected at backend and CLI boundaries.</done>
</task>

</tasks>

<verification>
Run `cargo test -p spoon-backend install_update_share_front_half_lifecycle_modules -- --nocapture` and `cargo test -p spoon --test scoop_runtime_flow spoon_scoop_package_cli_handles_install_update_uninstall_with_local_bucket_source -- --nocapture`.
</verification>

<output>
After completion, create `.planning/phases/03-scoop-lifecycle-split-and-app-thinning/03-02-SUMMARY.md`
</output>

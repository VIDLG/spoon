---
phase: 03-scoop-lifecycle-split-and-app-thinning
plan: 3
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - spoon-backend/src/scoop/lifecycle/persist.rs
  - spoon-backend/src/scoop/lifecycle/surface.rs
  - spoon-backend/src/scoop/lifecycle/integrate.rs
  - spoon-backend/src/scoop/lifecycle/state.rs
  - spoon-backend/src/scoop/runtime/actions.rs
  - spoon-backend/src/scoop/tests/runtime.rs
autonomous: true
requirements:
  - SCLF-01
  - SCLF-04
  - SCLF-05
must_haves:
  truths:
    - "The back half of install/update is explicitly modularized and ordered as persist -> surface -> integrate -> state."
    - "State commit remains last, after integrations, matching the agreed lifecycle semantics."
  artifacts:
    - path: "spoon-backend/src/scoop/lifecycle/persist.rs"
      provides: "Dedicated persist module"
      contains: "persist"
    - path: "spoon-backend/src/scoop/lifecycle/surface.rs"
      provides: "Dedicated surface module"
      contains: "surface"
    - path: "spoon-backend/src/scoop/lifecycle/state.rs"
      provides: "Dedicated state-commit module"
      contains: "state"
---

<objective>
Extract the shared install/update back half into explicit persist/surface/integrate/state modules.

Purpose: satisfy the shared back-half portion of SCLF-01, SCLF-04, and SCLF-05.
Output: explicit back-half lifecycle modules and regressions for the agreed ordering.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Extract persist, surface, integrate, and state modules</name>
  <files>
spoon-backend/src/scoop/lifecycle/persist.rs
spoon-backend/src/scoop/lifecycle/surface.rs
spoon-backend/src/scoop/lifecycle/integrate.rs
spoon-backend/src/scoop/lifecycle/state.rs
spoon-backend/src/scoop/runtime/actions.rs
  </files>
  <read_first>
.planning/phases/03-scoop-lifecycle-split-and-app-thinning/03-CONTEXT.md
.planning/phases/03-scoop-lifecycle-split-and-app-thinning/03-RESEARCH.md
spoon-backend/src/scoop/runtime/actions.rs
spoon-backend/src/scoop/runtime/persist.rs
spoon-backend/src/scoop/runtime/surface.rs
spoon-backend/src/scoop/runtime/integration.rs
spoon-backend/src/scoop/state/store.rs
  </read_first>
  <action>Create dedicated lifecycle modules for `persist`, `surface`, `integrate`, and `state` and move the corresponding logic out of `runtime/actions.rs`. Keep the order aligned with the discuss decisions: `persist_restoring` before `surface_applying`, `integrating` before `state_committing`, and never mark completion before state commit succeeds.</action>
  <verify>
    <automated>cargo test -p spoon-backend install_lifecycle_orders_persist_surface_integrate_state -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- `Select-String -Path spoon-backend/src/scoop/lifecycle/persist.rs -Pattern 'persist'` returns a match.
- `Select-String -Path spoon-backend/src/scoop/lifecycle/surface.rs -Pattern 'surface'` returns a match.
- `Select-String -Path spoon-backend/src/scoop/lifecycle/integrate.rs -Pattern 'integrat'` returns a match.
- `Select-String -Path spoon-backend/src/scoop/lifecycle/state.rs -Pattern 'state'` returns a match.
  </acceptance_criteria>
  <done>The install/update back half is explicitly modularized in the agreed order.</done>
</task>

<task type="auto">
  <name>Task 2: Add regressions for back-half ordering and reapply semantics</name>
  <files>
spoon-backend/src/scoop/tests/runtime.rs
  </files>
  <read_first>
.planning/phases/03-scoop-lifecycle-split-and-app-thinning/03-CONTEXT.md
.planning/phases/03-scoop-lifecycle-split-and-app-thinning/03-RESEARCH.md
spoon-backend/src/scoop/tests/runtime.rs
  </read_first>
  <action>Add backend regressions named `install_lifecycle_orders_persist_surface_integrate_state` and `reapply_runs_without_hooks_and_reuses_back_half_modules`. The first should prove the back-half order is preserved. The second should prove `reapply` uses the agreed back-half stages without invoking install/uninstall hooks.</action>
  <verify>
    <automated>cargo test -p spoon-backend reapply_runs_without_hooks_and_reuses_back_half_modules -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- `Select-String -Path spoon-backend/src/scoop/tests/runtime.rs -Pattern 'install_lifecycle_orders_persist_surface_integrate_state'` returns a match.
- `Select-String -Path spoon-backend/src/scoop/tests/runtime.rs -Pattern 'reapply_runs_without_hooks_and_reuses_back_half_modules'` returns a match.
  </acceptance_criteria>
  <done>Back-half ordering and reapply semantics are pinned before uninstall/app thinning work begins.</done>
</task>

</tasks>

<verification>
Run `cargo test -p spoon-backend install_lifecycle_orders_persist_surface_integrate_state -- --nocapture` and `cargo test -p spoon-backend reapply_runs_without_hooks_and_reuses_back_half_modules -- --nocapture`.
</verification>

<output>
After completion, create `.planning/phases/03-scoop-lifecycle-split-and-app-thinning/03-03-SUMMARY.md`
</output>

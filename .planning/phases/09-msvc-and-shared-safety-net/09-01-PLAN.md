---
phase: 09-msvc-and-shared-safety-net
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - spoon-backend/src/msvc/tests/root.rs
  - spoon-backend/src/msvc/tests/official.rs
  - spoon-backend/src/msvc/tests/context.rs
autonomous: true
requirements:
  - TEST-04
must_haves:
  truths:
    - "Managed and official MSVC failure boundaries are protected close to backend ownership."
    - "Canonical-state and reconcile-sensitive failures do not silently slip past the test net."
  artifacts:
    - path: "spoon-backend/src/msvc/tests/root.rs"
      provides: "managed failure-boundary regressions"
      contains: "download/extract/materialize/validate/state-boundary coverage"
---

<objective>
Add focused backend MSVC failure-boundary regressions.

Purpose: protect the riskiest managed/official lifecycle breakpoints close to the backend state machine.
Output: deterministic backend regressions for the highest-value MSVC failure scenarios.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add managed lifecycle/state failure-boundary regressions</name>
  <files>
spoon-backend/src/msvc/tests/root.rs
spoon-backend/src/msvc/tests/context.rs
  </files>
  <read_first>
.planning/phases/09-msvc-and-shared-safety-net/09-CONTEXT.md
spoon-backend/src/msvc/tests/root.rs
spoon-backend/src/msvc/tests/context.rs
  </read_first>
  <action>Add or tighten backend tests for the highest-risk managed breakpoints: download/extract/materialize failures, validation failures, and canonical-state boundary behavior.</action>
  <verify>
    <automated>cargo test -p spoon-backend --lib msvc::tests::root -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- Managed backend failure boundaries are explicitly tested.
  </acceptance_criteria>
  <done>Managed lifecycle/state regressions are pinned close to backend ownership.</done>
</task>

<task type="auto">
  <name>Task 2: Add official bootstrapper/reconcile failure-boundary regressions</name>
  <files>
spoon-backend/src/msvc/tests/official.rs
  </files>
  <read_first>
spoon-backend/src/msvc/tests/official.rs
spoon-backend/src/msvc/official.rs
  </read_first>
  <action>Add or tighten backend tests for official bootstrapper failure, detect/reconcile mismatch, and canonical-state-sensitive official failure behavior.</action>
  <verify>
    <automated>cargo test -p spoon-backend --lib msvc::tests::official -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- Official backend failure boundaries are explicitly tested.
  </acceptance_criteria>
  <done>Official failure modes are covered close to the backend strategy implementation.</done>
</task>

</tasks>

<verification>
Run `cargo test -p spoon-backend --lib msvc::tests::root -- --nocapture` and `cargo test -p spoon-backend --lib msvc::tests::official -- --nocapture`.
</verification>

<output>
After completion, create `.planning/phases/09-msvc-and-shared-safety-net/09-01-SUMMARY.md`
</output>

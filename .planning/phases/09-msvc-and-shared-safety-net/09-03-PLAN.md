---
phase: 09-msvc-and-shared-safety-net
plan: 3
type: execute
wave: 3
depends_on:
  - 09-02-PLAN.md
files_modified:
  - spoon/tests/cli/msvc_flow.rs
  - spoon/tests/tui/tui_msvc_download_flow.rs
  - spoon/tests/cli/scoop_flow.rs
autonomous: true
requirements:
  - TEST-05
must_haves:
  truths:
    - "App-shell tests remain thin-shell and still cover the highest-value user-visible regressions."
    - "Shared contract hardening and MSVC lifecycle work are both reflected in app-visible regression coverage."
  artifacts:
    - path: "spoon/tests/cli/msvc_flow.rs"
      provides: "MSVC app-shell regression coverage"
      contains: "translation/orchestration-focused MSVC flow checks"
---

<objective>
Strengthen the thin-shell app regression net for MSVC and shared contracts.

Purpose: keep app-facing confidence high without moving backend semantics into app tests.
Output: focused CLI/TUI regressions for MSVC/shared-contract user-visible behavior.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Tighten MSVC CLI translation/orchestration regressions</name>
  <files>
spoon/tests/cli/msvc_flow.rs
  </files>
  <read_first>
.planning/phases/09-msvc-and-shared-safety-net/09-CONTEXT.md
spoon/tests/cli/msvc_flow.rs
  </read_first>
  <action>Add or tighten the highest-value MSVC CLI regressions so backend lifecycle/state/shared-contract changes still surface through the same thin-shell app behavior.</action>
  <verify>
    <automated>cargo test -p spoon --test msvc_flow -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- MSVC CLI shell behavior is protected against recent backend changes.
  </acceptance_criteria>
  <done>MSVC app-shell regressions remain aligned with backend-owned semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten TUI/representative shared-shell regressions</name>
  <files>
spoon/tests/tui/tui_msvc_download_flow.rs
spoon/tests/cli/scoop_flow.rs
  </files>
  <read_first>
spoon/tests/tui/tui_msvc_download_flow.rs
spoon/tests/cli/scoop_flow.rs
  </read_first>
  <action>Update or tighten a small set of representative TUI/shared-shell regressions so the app still presents backend progress/results correctly after the shared contract changes.</action>
  <verify>
    <automated>cargo test -p spoon --test tui_msvc_download_flow -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- Representative TUI/shared-shell regressions stay green.
  </acceptance_criteria>
  <done>The app shell remains a stable consumer of the backend contract.</done>
</task>

</tasks>

<verification>
Run `cargo test -p spoon --test msvc_flow -- --nocapture` and `cargo test -p spoon --test tui_msvc_download_flow -- --nocapture`.
</verification>

<output>
After completion, create `.planning/phases/09-msvc-and-shared-safety-net/09-03-SUMMARY.md`
</output>

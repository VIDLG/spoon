---
phase: 09-msvc-and-shared-safety-net
plan: 2
type: execute
wave: 2
depends_on:
  - 09-01-PLAN.md
files_modified:
  - spoon-backend/src/tests/event.rs
  - spoon-backend/src/msvc/tests/context.rs
  - spoon/src/service/mod.rs
  - spoon/tests/cli/status_backend_flow.rs
autonomous: true
requirements:
  - TEST-04
  - TEST-05
must_haves:
  truths:
    - "Shared backend contract regressions are protected close to the modules changed in Phase 8."
    - "App translation remains thin while still catching contract drift."
  artifacts:
    - path: "spoon-backend/src/tests/event.rs"
      provides: "shared event contract regression coverage"
      contains: "backend-local contract assertions after the reset"
---

<objective>
Protect the shared backend contract changes from Phase 8 with layered regressions.

Purpose: ensure event/error/path/port cleanup cannot silently drift after the architectural hardening pass.
Output: backend-local and app-shell translation regressions that pin the shared contract.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Tighten backend-local shared-contract regressions</name>
  <files>
spoon-backend/src/tests/event.rs
spoon-backend/src/msvc/tests/context.rs
  </files>
  <read_first>
.planning/phases/09-msvc-and-shared-safety-net/09-CONTEXT.md
spoon-backend/src/tests/event.rs
spoon-backend/src/msvc/tests/context.rs
  </read_first>
  <action>Expand backend-local regression coverage around the shared event/error/path/port contracts where Phase 8 changed the underlying contract surface.</action>
  <verify>
    <automated>cargo test -p spoon-backend --lib event -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- Backend-local shared-contract regressions are stronger than Phase 8 left them.
  </acceptance_criteria>
  <done>Shared contract drift is caught closer to backend ownership.</done>
</task>

<task type="auto">
  <name>Task 2: Reconfirm app-shell translation after shared contract changes</name>
  <files>
spoon/src/service/mod.rs
spoon/tests/cli/status_backend_flow.rs
  </files>
  <read_first>
spoon/src/service/mod.rs
spoon/tests/cli/status_backend_flow.rs
  </read_first>
  <action>Update or tighten app-shell translation regressions only as needed so the app continues to consume the shared backend contract without recreating backend logic locally.</action>
  <verify>
    <automated>cargo test -p spoon --test status_backend_flow -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- App-shell translation remains thin and stable after shared contract hardening.
  </acceptance_criteria>
  <done>Shared contract changes are protected on the app shell boundary too.</done>
</task>

</tasks>

<verification>
Run `cargo test -p spoon-backend --lib event -- --nocapture` and `cargo test -p spoon --test status_backend_flow -- --nocapture`.
</verification>

<output>
After completion, create `.planning/phases/09-msvc-and-shared-safety-net/09-02-SUMMARY.md`
</output>

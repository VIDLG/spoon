---
phase: 04-refactor-safety-net
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - spoon-backend/src/scoop/tests/runtime.rs
  - spoon-backend/src/scoop/runtime/actions.rs
  - spoon-backend/src/scoop/runtime/hooks.rs
autonomous: true
requirements:
  - TEST-01
  - TEST-03
must_haves:
  truths:
    - "Fatal lifecycle failures stop before backend state claims success."
    - "Warning-only uninstall tail cleanup does not invalidate the main uninstall result."
  artifacts:
    - path: "spoon-backend/src/scoop/tests/runtime.rs"
      provides: "Focused lifecycle failure regressions near backend runtime ownership"
      contains: "hook_failures_stop_before_state_commit"
---

<objective>
Add focused backend failure-boundary tests close to the lifecycle runtime code.

Purpose: lock the most dangerous fail-hard and warning-only lifecycle boundaries without broadening app-side test ownership.
Output: backend-local regressions for hook failure semantics and pre-commit failure stopping points.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add install/update failure-boundary regressions near lifecycle runtime code</name>
  <files>
spoon-backend/src/scoop/tests/runtime.rs
spoon-backend/src/scoop/runtime/actions.rs
spoon-backend/src/scoop/runtime/hooks.rs
  </files>
  <read_first>
.planning/phases/04-refactor-safety-net/04-CONTEXT.md
.planning/phases/04-refactor-safety-net/04-RESEARCH.md
spoon-backend/src/scoop/tests/runtime.rs
spoon-backend/src/scoop/runtime/actions.rs
spoon-backend/src/scoop/runtime/hooks.rs
  </read_first>
  <action>Add backend tests that force fatal install/update lifecycle failures at agreed mutation boundaries and prove execution stops before backend state is committed. Favor focused helpers/fakes over broad CLI flows.</action>
  <verify>
    <automated>cargo test -p spoon-backend hook_failures_stop_before_state_commit -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- `Select-String -Path spoon-backend/src/scoop/tests/runtime.rs -Pattern 'hook_failures_stop_before_state_commit'` returns a match.
- Tests assert that failure occurs before the lifecycle reaches a committed success state.
  </acceptance_criteria>
  <done>Fatal lifecycle stop points are pinned by backend-local regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Add warning-only uninstall tail regression</name>
  <files>
spoon-backend/src/scoop/tests/runtime.rs
spoon-backend/src/scoop/runtime/actions.rs
  </files>
  <read_first>
.planning/phases/04-refactor-safety-net/04-CONTEXT.md
spoon-backend/src/scoop/tests/runtime.rs
spoon-backend/src/scoop/runtime/actions.rs
  </read_first>
  <action>Add a regression that proves warning-only uninstall tail behavior remains stable: `post_uninstall` failure is surfaced diagnostically but does not undo the main uninstall result.</action>
  <verify>
    <automated>cargo test -p spoon-backend warning_only_uninstall_tail_preserves_main_result -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- `Select-String -Path spoon-backend/src/scoop/tests/runtime.rs -Pattern 'warning_only_uninstall_tail_preserves_main_result'` returns a match.
  </acceptance_criteria>
  <done>Warning-only uninstall semantics are covered where the backend owns them.</done>
</task>

</tasks>

<verification>
Run `cargo test -p spoon-backend hook_failures_stop_before_state_commit -- --nocapture` and `cargo test -p spoon-backend warning_only_uninstall_tail_preserves_main_result -- --nocapture`.
</verification>

<output>
After completion, create `.planning/phases/04-refactor-safety-net/04-01-SUMMARY.md`
</output>


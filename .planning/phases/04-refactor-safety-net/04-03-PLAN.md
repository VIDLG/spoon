---
phase: 04-refactor-safety-net
plan: 3
type: execute
wave: 2
depends_on:
  - 04-01-PLAN.md
  - 04-02-PLAN.md
files_modified:
  - spoon/tests/cli/status_backend_flow.rs
  - spoon/tests/cli/scoop_runtime_flow.rs
  - spoon/tests/tui/
autonomous: true
requirements:
  - TEST-02
  - TEST-03
must_haves:
  truths:
    - "App tests protect request/event/outcome translation without re-owning backend lifecycle correctness."
    - "CLI/TUI regressions remain focused on shell behavior and presentation contracts."
  artifacts:
    - path: "spoon/tests/cli/status_backend_flow.rs"
      provides: "Structured backend-event translation regressions"
      contains: "backend_stage_events_drive_app_stream_translation"
---

<objective>
Protect the app shell without letting it become a second backend oracle.

Purpose: keep CLI/TUI safety coverage focused on translation, routing, and orchestration-shell behavior after the backend lifecycle split.
Output: app-shell regressions that consume backend contracts rather than re-encoding backend internals.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Strengthen CLI translation regressions for lifecycle events and outcomes</name>
  <files>
spoon/tests/cli/status_backend_flow.rs
spoon/tests/cli/scoop_runtime_flow.rs
spoon/src/service/mod.rs
  </files>
  <read_first>
.planning/phases/04-refactor-safety-net/04-CONTEXT.md
spoon/tests/cli/status_backend_flow.rs
spoon/tests/cli/scoop_runtime_flow.rs
spoon/src/service/mod.rs
  </read_first>
  <action>Add or tighten CLI tests so backend lifecycle stages, warnings, and final outcome lines remain correctly translated into app-visible stream/output behavior. Avoid restating backend stage-order logic here.</action>
  <verify>
    <automated>cargo test -p spoon --test status_backend_flow -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- `cargo test -p spoon --test status_backend_flow -- --nocapture` passes.
- CLI tests assert app-visible translation behavior rather than backend-internal state mutation details.
  </acceptance_criteria>
  <done>CLI translation contracts stay protected without duplicating backend lifecycle ownership.</done>
</task>

<task type="auto">
  <name>Task 2: Add or tighten TUI-orchestration shell regressions only where they consume backend contracts</name>
  <files>
spoon/tests/tui/
spoon/src/tui/
  </files>
  <read_first>
.planning/phases/04-refactor-safety-net/04-CONTEXT.md
spoon/tests/tui/
AGENTS.md
  </read_first>
  <action>Add or refine a small number of Harness-driven TUI regressions only where Phase 3 lifecycle events/results cross the app shell. Keep these tests focused on orchestration and rendering, not backend correctness.</action>
  <verify>
    <automated>cargo test -p spoon --test scoop_runtime_flow -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- New or updated app tests remain Harness/CLI oriented and do not attempt to re-check backend journal/state semantics.
  </acceptance_criteria>
  <done>App-shell regressions stay thin and correctly weighted.</done>
</task>

</tasks>

<verification>
Run `cargo test -p spoon --test status_backend_flow -- --nocapture` and `cargo test -p spoon --test scoop_runtime_flow -- --nocapture`.
</verification>

<output>
After completion, create `.planning/phases/04-refactor-safety-net/04-03-SUMMARY.md`
</output>


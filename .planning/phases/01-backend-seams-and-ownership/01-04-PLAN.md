---
phase: 01-backend-seams-and-ownership
plan: 4
type: execute
wave: 3
depends_on:
  - 01-02
  - 01-03
files_modified:
  - spoon-backend/src/status.rs
  - spoon-backend/src/lib.rs
  - spoon/src/status/mod.rs
  - spoon/src/status/discovery/probe.rs
  - spoon/src/cli/json.rs
  - spoon/src/tui/background.rs
  - spoon/Cargo.toml
  - spoon/tests/cli/status_backend_flow.rs
autonomous: true
requirements:
  - BNDR-05
  - LAY-02
must_haves:
  truths:
    - "Status and runtime views consume backend read models instead of rereading backend state files in the app."
    - "CLI JSON and TUI background refresh use the same backend-derived status snapshot."
  artifacts:
    - path: "spoon-backend/src/status.rs"
      provides: "Backend status snapshot for app consumption"
      contains: "pub struct BackendStatusSnapshot"
    - path: "spoon/src/status/mod.rs"
      provides: "App presentation over backend status snapshot"
      contains: "build_status_details"
    - path: "spoon/tests/cli/status_backend_flow.rs"
      provides: "Regression test for backend-driven status JSON"
      contains: "json_status_uses_backend_read_models"
  key_links:
    - from: "spoon/src/status/mod.rs"
      to: "spoon-backend/src/status.rs"
      via: "status assembly consumes backend snapshot"
      pattern: "BackendStatusSnapshot"
    - from: "spoon/src/cli/json.rs"
      to: "spoon/src/status/mod.rs"
      via: "JSON status uses backend-driven status details"
      pattern: "build_status_details"
    - from: "spoon/src/tui/background.rs"
      to: "spoon/src/status/mod.rs"
      via: "background refresh uses same backend-derived statuses"
      pattern: "collect_statuses"
---

<objective>
Replace app-side backend state probing with backend-owned status read models.

Purpose: Satisfy BNDR-05 and LAY-02 for status/runtime surfaces before the final detail/config cleanup.
Output: A backend status snapshot API and app status/json/TUI code that renders it.
</objective>

<execution_context>
@C:/Users/vision/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/vision/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-backend-seams-and-ownership/01-CONTEXT.md
@.planning/phases/01-backend-seams-and-ownership/01-RESEARCH.md
@AGENTS.md
@spoon-backend/src/lib.rs
@spoon-backend/src/scoop/query.rs
@spoon-backend/src/msvc/status.rs
@spoon/src/status/mod.rs
@spoon/src/status/discovery/probe.rs
@spoon/src/cli/json.rs
@spoon/src/tui/background.rs
@spoon/tests/cli/json_flow.rs

<interfaces>
From spoon-backend/src/scoop/query.rs:
```rust
pub struct ScoopStatus {
    pub kind: &'static str,
    pub success: bool,
    pub runtime: ScoopRuntimeStatus,
    pub buckets: Vec<ScoopBucketEntry>,
    pub installed_packages: Vec<ScoopInstalledPackageEntry>,
    pub paths: ScoopPaths,
}
```

From spoon/src/status/mod.rs:
```rust
pub struct StatusDetails {
    pub include_update_info: bool,
    pub overview: StatusSummary,
    pub runtime: RuntimeStatus,
    pub tools: Vec<ToolStatusDetails>,
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a backend-owned status snapshot surface for app consumers</name>
  <files>
spoon-backend/src/status.rs
spoon-backend/src/lib.rs
  </files>
  <read_first>
.planning/phases/01-backend-seams-and-ownership/01-CONTEXT.md
.planning/phases/01-backend-seams-and-ownership/01-RESEARCH.md
spoon-backend/src/lib.rs
spoon-backend/src/scoop/query.rs
spoon-backend/src/msvc/status.rs
spoon-backend/src/context.rs
  </read_first>
  <action>Create `spoon-backend/src/status.rs` as a thin aggregate read-model module that composes existing backend query surfaces without changing persisted Scoop state per D-07 and D-08. Export concrete snapshot types such as `BackendStatusSnapshot`, `BackendRuntimeRoots`, and any per-tool runtime summary needed by the app. The snapshot must come from backend queries (`scoop::runtime_status`, installed package state, bucket inventory, MSVC status, layout derivation) rather than app-local file IO. Re-export the new status module from `spoon-backend/src/lib.rs`.</action>
  <verify>
    <automated>cargo test -p spoon-backend --lib status -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- `Select-String -Pattern 'pub struct BackendStatusSnapshot' spoon-backend/src/status.rs` returns a match.
- `Select-String -Pattern 'pub mod status;' spoon-backend/src/lib.rs` returns a match.
- `Select-String -Pattern 'runtime_status' spoon-backend/src/status.rs` returns a match.
- `Select-String -Pattern 'msvc::status' spoon-backend/src/status.rs` returns a match.
  </acceptance_criteria>
  <done>The backend exposes a serializable status snapshot that centralizes runtime roots and backend state facts for the app shell.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire status, JSON, and TUI refresh code to backend read models only</name>
  <files>
spoon/src/status/mod.rs
spoon/src/status/discovery/probe.rs
spoon/src/cli/json.rs
spoon/src/tui/background.rs
spoon/Cargo.toml
spoon/tests/cli/status_backend_flow.rs
  </files>
  <read_first>
.planning/phases/01-backend-seams-and-ownership/01-CONTEXT.md
.planning/phases/01-backend-seams-and-ownership/01-RESEARCH.md
spoon/src/status/mod.rs
spoon/src/status/discovery/probe.rs
spoon/src/cli/json.rs
spoon/src/tui/background.rs
spoon/tests/cli/json_flow.rs
spoon-backend/src/status.rs
  </read_first>
  <action>Switch `spoon/src/status/mod.rs` and `spoon/src/status/discovery/probe.rs` to consume the backend snapshot from Task 1 instead of reading Scoop state JSON files or deriving backend layout locally per D-07, D-08, and D-11. Keep the app responsible for presentation labels, action policy, and TUI/CLI formatting only. Update `spoon/src/cli/json.rs` and `spoon/src/tui/background.rs` so both surfaces use the backend-driven status path. Add `spoon/tests/cli/status_backend_flow.rs`, register it in `spoon/Cargo.toml`, and add a test named `json_status_uses_backend_read_models` that fails if the JSON status path falls back to app-side state-file parsing.</action>
  <verify>
    <automated>cargo test -p spoon --test status_backend_flow json_status_uses_backend_read_models -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- `Select-String -Pattern 'BackendStatusSnapshot' spoon/src/status/mod.rs` returns a match.
- `Select-String -Pattern 'scoop_state_root_from' spoon/src/status/discovery/probe.rs` returns no matches.
- `Select-String -Pattern 'read_to_string\\(' spoon/src/status/discovery/probe.rs` returns no matches.
- `Select-String -Pattern 'json_status_uses_backend_read_models' spoon/tests/cli/status_backend_flow.rs` returns a match.
- `Select-String -Pattern 'status_backend_flow' spoon/Cargo.toml` returns a match.
  </acceptance_criteria>
  <done>Status/runtime app surfaces render backend read models and do not reopen backend state files from `spoon/src/status` anymore.</done>
</task>

</tasks>

<verification>
Run `cargo test -p spoon --test json_flow` and `cargo test -p spoon --test status_backend_flow` after both tasks.
</verification>

<success_criteria>
The app status subsystem, JSON status output, and TUI background refresh all use one backend-derived status source and no longer parse backend state files directly.
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-seams-and-ownership/01-04-SUMMARY.md`
</output>

---
phase: 01-backend-seams-and-ownership
plan: 3
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - spoon-backend/src/msvc/mod.rs
  - spoon-backend/src/msvc/official.rs
  - spoon-backend/src/msvc/status.rs
  - spoon-backend/src/msvc/validation.rs
  - spoon-backend/src/msvc/tests/context.rs
  - spoon-backend/src/msvc/tests/mod.rs
  - spoon/src/service/msvc/mod.rs
autonomous: true
requirements:
  - BNDR-03
  - LAY-03
must_haves:
  truths:
    - "Managed and official MSVC operations no longer depend on mutable global runtime config."
    - "The app invokes MSVC detect, install, update, validate, and uninstall through explicit backend context requests."
  artifacts:
    - path: "spoon-backend/src/msvc/mod.rs"
      provides: "Context-driven MSVC runtime entry points"
      contains: "BackendContext"
    - path: "spoon-backend/src/msvc/tests/context.rs"
      provides: "MSVC explicit-context tests"
      contains: "msvc_context_drives_status_and_install"
    - path: "spoon/src/service/msvc/mod.rs"
      provides: "Thin MSVC adapter without global config bridge"
      contains: "command_result_from_msvc_outcome"
  key_links:
    - from: "spoon/src/service/msvc/mod.rs"
      to: "spoon-backend/src/context.rs"
      via: "app builds explicit MSVC backend context"
      pattern: "BackendContext"
    - from: "spoon-backend/src/msvc/official.rs"
      to: "spoon-backend/src/msvc/mod.rs"
      via: "official runtime consumes request-scoped config"
      pattern: "msvc"
---

<objective>
Replace the MSVC global runtime bridge with explicit backend context.

Purpose: Close the highest-risk hidden dependency in Phase 1 and satisfy BNDR-03 plus LAY-03.
Output: Context-driven MSVC backend APIs, updated app adapter, and nearby regression tests.
</objective>

<execution_context>
@C:/Users/vision/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/vision/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-backend-seams-and-ownership/01-CONTEXT.md
@.planning/phases/01-backend-seams-and-ownership/01-RESEARCH.md
@AGENTS.md
@spoon-backend/src/context.rs
@spoon-backend/src/msvc/mod.rs
@spoon-backend/src/msvc/official.rs
@spoon-backend/src/msvc/status.rs
@spoon-backend/src/msvc/validation.rs
@spoon-backend/src/msvc/tests/mod.rs
@spoon/src/service/msvc/mod.rs

<interfaces>
From spoon-backend/src/msvc/mod.rs:
```rust
pub struct MsvcRuntimeConfig {
    pub proxy: String,
    pub command_profile: String,
    pub selected_target_arch: String,
    pub test_mode: bool,
    pub configured_tool_root: Option<PathBuf>,
}
```

From spoon-backend/src/msvc/mod.rs:
```rust
pub struct MsvcOperationOutcome {
    pub kind: &'static str,
    pub runtime: MsvcRuntimeKind,
    pub operation: MsvcOperationKind,
    pub status: CommandStatus,
    pub title: String,
    pub output: Vec<String>,
    pub streamed: bool,
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace backend MSVC global runtime config with request-scoped context</name>
  <files>
spoon-backend/src/msvc/mod.rs
spoon-backend/src/msvc/official.rs
spoon-backend/src/msvc/status.rs
spoon-backend/src/msvc/validation.rs
spoon-backend/src/msvc/tests/context.rs
spoon-backend/src/msvc/tests/mod.rs
  </files>
  <read_first>
.planning/phases/01-backend-seams-and-ownership/01-CONTEXT.md
.planning/phases/01-backend-seams-and-ownership/01-RESEARCH.md
spoon-backend/src/context.rs
spoon-backend/src/msvc/mod.rs
spoon-backend/src/msvc/official.rs
spoon-backend/src/msvc/status.rs
spoon-backend/src/msvc/validation.rs
spoon-backend/src/msvc/tests/mod.rs
  </read_first>
  <action>Remove `MsvcRuntimeConfig`, `set_runtime_config`, `runtime_config()`, and the `OnceLock<RwLock<...>>` global path from `spoon-backend/src/msvc/mod.rs` per D-03 and D-10. Replace them with explicit request-scoped inputs coming from `BackendContext` or a dedicated MSVC request type that is created from `BackendContext`. Update `spoon-backend/src/msvc/official.rs`, `spoon-backend/src/msvc/status.rs`, and `spoon-backend/src/msvc/validation.rs` to read proxy, target arch, command profile, test mode, and configured root from that explicit request object. Add `spoon-backend/src/msvc/tests/context.rs` with tests named `msvc_context_drives_status_and_install` and `explicit_context_required_for_runtime_ops`, and wire it from `spoon-backend/src/msvc/tests/mod.rs`.</action>
  <verify>
    <automated>cargo test -p spoon-backend msvc_context_drives_status_and_install -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- `Select-String -Pattern 'pub fn set_runtime_config' spoon-backend/src/msvc/mod.rs` returns no matches.
- `Select-String -Pattern 'static RUNTIME_CONFIG' spoon-backend/src/msvc/mod.rs` returns no matches.
- `Select-String -Pattern 'BackendContext' spoon-backend/src/msvc/mod.rs` returns a match.
- `Select-String -Pattern 'BackendContext' spoon-backend/src/msvc/official.rs` returns a match.
- `Select-String -Pattern 'msvc_context_drives_status_and_install' spoon-backend/src/msvc/tests/context.rs` returns a match.
  </acceptance_criteria>
  <done>MSVC backend behavior is driven by explicit request context instead of a mutable process-global singleton.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the app-side MSVC runtime-config bridge</name>
  <files>
spoon/src/service/msvc/mod.rs
  </files>
  <read_first>
.planning/phases/01-backend-seams-and-ownership/01-CONTEXT.md
.planning/phases/01-backend-seams-and-ownership/01-RESEARCH.md
spoon/src/service/msvc/mod.rs
spoon/src/service/mod.rs
spoon-backend/src/context.rs
spoon-backend/src/msvc/mod.rs
  </read_first>
  <action>Delete `apply_runtime_config()` from `spoon/src/service/msvc/mod.rs` and update every MSVC adapter function to construct the explicit backend context/request at the app boundary per D-02 and D-03. Keep `spoon` responsible for app-owned config loading and event mapping only; do not leave any call to `configured_tool_root()`, `load_backend_config()`, or `test_mode_enabled()` hidden behind a backend global setter.</action>
  <verify>
    <automated>cargo test -p spoon --test msvc_flow -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- `Select-String -Pattern 'apply_runtime_config' spoon/src/service/msvc/mod.rs` returns no matches.
- `Select-String -Pattern 'set_runtime_config' spoon/src/service/msvc/mod.rs` returns no matches.
- `Select-String -Pattern 'BackendContext' spoon/src/service/msvc/mod.rs` returns a match.
- `Select-String -Pattern 'load_backend_config' spoon/src/service/msvc/mod.rs` returns no matches.
  </acceptance_criteria>
  <done>The app MSVC adapter is reduced to explicit request construction and backend outcome mapping, with no hidden runtime mutation step.</done>
</task>

</tasks>

<verification>
Run `cargo test -p spoon-backend --lib` and `cargo test -p spoon --test msvc_flow` after both tasks.
</verification>

<success_criteria>
MSVC status and lifecycle operations run through explicit backend context and neither crate depends on a mutable global runtime config path anymore.
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-seams-and-ownership/01-03-SUMMARY.md`
</output>

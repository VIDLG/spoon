---
phase: 01-backend-seams-and-ownership
plan: 7
type: execute
wave: 5
depends_on:
  - 01-05
  - 01-06
files_modified:
  - spoon/src/service/mod.rs
  - spoon/src/settings.rs
  - spoon/Cargo.toml
autonomous: true
requirements:
  - GIT-01
  - LAY-02
must_haves:
  truths:
    - "The Spoon app crate no longer directly depends on `gix`."
    - "Legacy backend-path helper re-exports are removed once backend-derived app surfaces no longer need them."
  artifacts:
    - path: "spoon/src/service/mod.rs"
      provides: "App service layer without dead backend path re-exports"
      contains: "pub mod scoop;"
    - path: "spoon/src/settings.rs"
      provides: "Settings surface without backend runtime path derivation exports"
      contains: "pub struct Settings"
    - path: "spoon/Cargo.toml"
      provides: "App manifest without direct gix dependency"
      contains: "name = \"spoon\""
  key_links:
    - from: "spoon/src/service/mod.rs"
      to: "spoon-backend/src/layout.rs"
      via: "service layer stops re-exporting app-owned backend path helpers"
      pattern: "BackendContext"
    - from: "spoon/src/settings.rs"
      to: "spoon/src/view/config.rs"
      via: "settings keep app config semantics only"
      pattern: "Settings"
    - from: "spoon/Cargo.toml"
      to: "spoon-backend/src/gitx.rs"
      via: "Git implementation remains backend-only"
      pattern: "gix"
---

<objective>
Finish the small terminal cleanup after the app-shell contract moves are complete.

Purpose: Make GIT-01 true and delete the last dead backend-path helper exports without mixing that cleanup into larger behavior changes.
Output: A smaller app manifest and service/settings modules that no longer expose deprecated backend runtime-path seams.
</objective>

<execution_context>
@C:/Users/vision/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/vision/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-backend-seams-and-ownership/01-CONTEXT.md
@.planning/phases/01-backend-seams-and-ownership/01-RESEARCH.md
@AGENTS.md
@spoon/src/service/mod.rs
@spoon/src/settings.rs
@spoon/Cargo.toml
@spoon-backend/src/gitx.rs
@spoon-backend/src/layout.rs

<interfaces>
From spoon/src/service/mod.rs:
```rust
pub mod scoop;
pub mod msvc;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead backend-path re-exports from app service and settings modules</name>
  <files>
spoon/src/service/mod.rs
spoon/src/settings.rs
  </files>
  <read_first>
.planning/phases/01-backend-seams-and-ownership/01-CONTEXT.md
.planning/phases/01-backend-seams-and-ownership/01-RESEARCH.md
spoon/src/service/mod.rs
spoon/src/settings.rs
spoon/src/view/config.rs
spoon/src/service/scoop/actions.rs
spoon/src/service/scoop/bucket.rs
  </read_first>
  <action>Delete dead backend-path re-exports from `spoon/src/service/mod.rs` and `spoon/src/settings.rs` once Plans 01-05 and 01-06 no longer use them. Remove legacy exports such as `shims_root_from`, `msvc_toolchain_root_from`, `msvc_root_from`, `scoop_root_from`, and `official_msvc_root_from`, but keep app-owned config semantics that are unrelated to backend runtime layout. Do not reintroduce fallback app-side backend path derivation to ease the cleanup.</action>
  <verify>
    <automated>cargo test -p spoon --test json_flow config_json_prints_structured_view_model -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- `Select-String -Pattern 'shims_root_from|msvc_toolchain_root_from|msvc_root_from' spoon/src/service/mod.rs` returns no matches.
- `Select-String -Pattern 'scoop_root_from|msvc_root_from|official_msvc_root_from|shims_root_from' spoon/src/settings.rs` returns no matches.
  </acceptance_criteria>
  <done>The app shell no longer re-exports deprecated backend runtime-path helpers after the consuming surfaces have moved off them.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the direct app `gix` dependency and verify backend-only Git ownership</name>
  <files>
spoon/Cargo.toml
  </files>
  <read_first>
.planning/phases/01-backend-seams-and-ownership/01-CONTEXT.md
.planning/phases/01-backend-seams-and-ownership/01-RESEARCH.md
spoon/Cargo.toml
spoon-backend/src/gitx.rs
  </read_first>
  <action>Remove the direct `gix` dependency from `spoon/Cargo.toml` per GIT-01. Do not touch `spoon-backend`'s `gix` dependency or `spoon-backend/src/gitx.rs`; Phase 1 requires Git to remain backend-internal, not removed entirely.</action>
  <verify>
    <automated>powershell -NoProfile -Command "if ((cargo tree -p spoon -e normal --depth 1 | Select-String ' gix v').Count -eq 0) { Write-Output 'PASS' } else { Write-Error 'spoon still directly depends on gix'; exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
- `Select-String -Pattern '^gix\\s*=' spoon/Cargo.toml` returns no matches.
  </acceptance_criteria>
  <done>The app manifest no longer directly declares `gix`, and backend-only Git ownership is enforced by dependency audit.</done>
</task>

</tasks>

<verification>
Run the config JSON regression from Task 1 and the `cargo tree -p spoon -e normal --depth 1` audit from Task 2.
</verification>

<success_criteria>
The last dead app-side backend path exports are removed and `spoon` no longer directly depends on `gix`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-seams-and-ownership/01-07-SUMMARY.md`
</output>

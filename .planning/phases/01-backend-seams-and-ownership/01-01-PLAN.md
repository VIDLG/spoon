---
phase: 01-backend-seams-and-ownership
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - spoon-backend/src/context.rs
  - spoon-backend/src/layout.rs
  - spoon-backend/src/ports.rs
  - spoon-backend/src/lib.rs
  - spoon-backend/src/scoop/paths.rs
  - spoon-backend/src/msvc/paths.rs
  - spoon-backend/src/tests/context.rs
  - spoon-backend/src/tests/mod.rs
autonomous: true
requirements:
  - BNDR-04
  - LAY-01
  - LAY-03
must_haves:
  truths:
    - "Backend code derives Scoop, MSVC, shim, cache, and state paths from one backend-owned runtime layout."
    - "Backend operations receive explicit runtime inputs through a backend context contract instead of scattered app helpers."
  artifacts:
    - path: "spoon-backend/src/context.rs"
      provides: "BackendContext and backend runtime inputs"
      contains: "pub struct BackendContext"
    - path: "spoon-backend/src/layout.rs"
      provides: "RuntimeLayout root-derived path model"
      contains: "pub struct RuntimeLayout"
    - path: "spoon-backend/src/ports.rs"
      provides: "Split system and package integration ports"
      contains: "pub trait SystemPort"
    - path: "spoon-backend/src/tests/context.rs"
      provides: "Contract tests for explicit context and layout derivation"
      contains: "runtime_layout_derives_from_root"
  key_links:
    - from: "spoon-backend/src/context.rs"
      to: "spoon-backend/src/layout.rs"
      via: "BackendContext carries RuntimeLayout built from root"
      pattern: "layout: RuntimeLayout"
    - from: "spoon-backend/src/scoop/paths.rs"
      to: "spoon-backend/src/layout.rs"
      via: "path helpers delegate to RuntimeLayout"
      pattern: "RuntimeLayout::from_root"
    - from: "spoon-backend/src/msvc/paths.rs"
      to: "spoon-backend/src/layout.rs"
      via: "MSVC path helpers delegate to RuntimeLayout"
      pattern: "RuntimeLayout::from_root"
---

<objective>
Define the Phase 1 backend seam contracts before any domain adapter moves.

Purpose: Make `spoon-backend` the single owner of runtime context and root-derived layout per D-01, D-02, D-03, D-10, and D-11.
Output: Public backend context/layout/port modules plus focused contract tests.
</objective>

<execution_context>
@C:/Users/vision/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/vision/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-backend-seams-and-ownership/01-CONTEXT.md
@.planning/phases/01-backend-seams-and-ownership/01-RESEARCH.md
@AGENTS.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@spoon-backend/src/lib.rs
@spoon-backend/src/scoop/paths.rs
@spoon-backend/src/msvc/paths.rs
@spoon-backend/src/tests/mod.rs

<interfaces>
From spoon-backend/src/scoop/paths.rs:
```rust
pub fn scoop_root(tool_root: &Path) -> PathBuf;
pub fn shims_root(tool_root: &Path) -> PathBuf;
pub fn scoop_state_root(tool_root: &Path) -> PathBuf;
pub fn scoop_cache_root(tool_root: &Path) -> PathBuf;
```

From spoon-backend/src/msvc/paths.rs:
```rust
pub fn msvc_root(tool_root: &Path) -> PathBuf;
pub fn msvc_state_root(tool_root: &Path) -> PathBuf;
pub fn msvc_cache_root(tool_root: &Path) -> PathBuf;
pub fn msvc_toolchain_root(tool_root: &Path) -> PathBuf;
pub fn official_msvc_root(tool_root: &Path) -> PathBuf;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create backend context, layout, and split-port contracts</name>
  <files>
spoon-backend/src/context.rs
spoon-backend/src/layout.rs
spoon-backend/src/ports.rs
spoon-backend/src/lib.rs
spoon-backend/src/tests/context.rs
spoon-backend/src/tests/mod.rs
  </files>
  <read_first>
.planning/phases/01-backend-seams-and-ownership/01-CONTEXT.md
.planning/phases/01-backend-seams-and-ownership/01-RESEARCH.md
AGENTS.md
spoon-backend/src/lib.rs
spoon-backend/src/scoop/paths.rs
spoon-backend/src/msvc/paths.rs
spoon-backend/src/tests/mod.rs
  </read_first>
  <action>Create `spoon-backend/src/context.rs`, `spoon-backend/src/layout.rs`, and `spoon-backend/src/ports.rs` as the new public runtime seam per D-01, D-02, D-03, D-04, D-05, and D-06. `BackendContext` must expose concrete runtime inputs needed by existing backend call sites: `root: PathBuf`, `layout: RuntimeLayout`, `proxy: Option<String>`, `test_mode: bool`, `msvc_target_arch: String`, `msvc_command_profile: String`, and a generic `ports` payload. `RuntimeLayout` must include root-derived sub-layouts for Scoop, managed MSVC, official MSVC, shims, cache, and state roots. `ports.rs` must define separate `SystemPort` and `PackageIntegrationPort` traits; do not keep the current mixed host shape under a renamed trait. Re-export these modules from `spoon-backend/src/lib.rs`. Add `spoon-backend/src/tests/context.rs` and wire it from `spoon-backend/src/tests/mod.rs` with test names `runtime_layout_derives_from_root` and `explicit_context_required_for_runtime_ops`.</action>
  <verify>
    <automated>cargo test -p spoon-backend runtime_layout_derives_from_root -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- `Select-String -Pattern 'pub struct BackendContext' spoon-backend/src/context.rs` returns a match.
- `Select-String -Pattern 'pub struct RuntimeLayout' spoon-backend/src/layout.rs` returns a match.
- `Select-String -Pattern 'pub trait SystemPort' spoon-backend/src/ports.rs` returns a match.
- `Select-String -Pattern 'pub trait PackageIntegrationPort' spoon-backend/src/ports.rs` returns a match.
- `Select-String -Pattern 'mod context;' spoon-backend/src/tests/mod.rs` returns a match.
- `Select-String -Pattern 'runtime_layout_derives_from_root' spoon-backend/src/tests/context.rs` returns a match.
  </acceptance_criteria>
  <done>Backend has one exported context contract, one exported runtime layout contract, split runtime ports, and a nearby test module proving the seam exists.</done>
</task>

<task type="auto">
  <name>Task 2: Rebase backend path helpers onto RuntimeLayout without changing persisted paths</name>
  <files>
spoon-backend/src/layout.rs
spoon-backend/src/scoop/paths.rs
spoon-backend/src/msvc/paths.rs
spoon-backend/src/tests/context.rs
  </files>
  <read_first>
.planning/phases/01-backend-seams-and-ownership/01-CONTEXT.md
.planning/phases/01-backend-seams-and-ownership/01-RESEARCH.md
spoon-backend/src/layout.rs
spoon-backend/src/scoop/paths.rs
spoon-backend/src/msvc/paths.rs
spoon-backend/src/tests/context.rs
  </read_first>
  <action>Update `spoon-backend/src/scoop/paths.rs` and `spoon-backend/src/msvc/paths.rs` so every exported helper delegates to `RuntimeLayout::from_root(...)` per D-10 and D-11. Keep the existing function names and the existing directory names (`scoop`, `scoop/state`, `shims`, `msvc/managed`, `msvc/official`, `toolchain`, `cache`) so Phase 1 stays a contract refactor and does not change persisted Scoop state schema. Extend `spoon-backend/src/tests/context.rs` to assert the layout covers Scoop, managed MSVC, official MSVC, shims, state, and cache paths from one root.</action>
  <verify>
    <automated>cargo test -p spoon-backend explicit_context_required_for_runtime_ops -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- `Select-String -Pattern 'RuntimeLayout::from_root' spoon-backend/src/scoop/paths.rs` returns a match.
- `Select-String -Pattern 'RuntimeLayout::from_root' spoon-backend/src/msvc/paths.rs` returns a match.
- `Select-String -Pattern 'official_msvc' spoon-backend/src/tests/context.rs` returns a match.
- `Select-String -Pattern 'scoop_state' spoon-backend/src/tests/context.rs` returns a match.
- `Select-String -Pattern 'tool_root.join\\(\"scoop\"\\)' spoon-backend/src/scoop/paths.rs` returns no matches.
- `Select-String -Pattern 'tool_root.join\\(\"msvc\"\\)' spoon-backend/src/msvc/paths.rs` returns no matches.
  </acceptance_criteria>
  <done>All backend path derivation funnels through one runtime layout implementation and the contract tests pin the expected path tree.</done>
</task>

</tasks>

<verification>
Run `cargo test -p spoon-backend --lib` after both tasks. Do not move any app-side adapters yet.
</verification>

<success_criteria>
Phase 1 has a public backend-owned `BackendContext`, a public `RuntimeLayout`, split runtime ports, and backend path helpers that derive from the same layout implementation.
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-seams-and-ownership/01-01-SUMMARY.md`
</output>

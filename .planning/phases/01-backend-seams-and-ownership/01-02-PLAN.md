---
phase: 01-backend-seams-and-ownership
plan: 2
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - spoon-backend/src/scoop/mod.rs
  - spoon-backend/src/scoop/runtime/execution.rs
  - spoon-backend/src/scoop/runtime/actions.rs
  - spoon-backend/src/scoop/buckets.rs
  - spoon-backend/src/scoop/tests/contracts.rs
  - spoon-backend/src/scoop/tests/mod.rs
autonomous: true
requirements:
  - BNDR-01
  - BNDR-02
  - GIT-02
  - GIT-03
must_haves:
  truths:
    - "Backend Scoop install, update, uninstall, and bucket operations consume explicit backend context rather than app-provided runtime path arguments."
    - "Bucket clone and sync progress is emitted from backend Git code and remains hidden behind backend events and outcomes."
  artifacts:
    - path: "spoon-backend/src/scoop/runtime/actions.rs"
      provides: "Context-driven Scoop lifecycle entry points"
      contains: "BackendContext"
    - path: "spoon-backend/src/scoop/buckets.rs"
      provides: "Backend-owned bucket clone and sync contracts"
      contains: "RepoSyncOutcome"
    - path: "spoon-backend/src/scoop/tests/contracts.rs"
      provides: "Contract tests for Scoop context and bucket Git ownership"
      contains: "scoop_action_contract_uses_context"
  key_links:
    - from: "spoon-backend/src/scoop/runtime/actions.rs"
      to: "spoon-backend/src/context.rs"
      via: "runtime entry points take BackendContext"
      pattern: "BackendContext"
    - from: "spoon-backend/src/scoop/buckets.rs"
      to: "spoon-backend/src/gitx.rs"
      via: "bucket sync keeps Git internal"
      pattern: "clone_repo"
    - from: "spoon-backend/src/scoop/runtime/execution.rs"
      to: "spoon-backend/src/ports.rs"
      via: "generic runtime effects and Spoon integrations stay split"
      pattern: "SystemPort"
---

<objective>
Move backend Scoop package and bucket operations onto the new backend seam.

Purpose: Satisfy the backend half of BNDR-01, BNDR-02, GIT-02, and GIT-03 while preserving the mixed OS/package boundary from D-04 through D-09.
Output: Context-driven backend Scoop and bucket APIs plus backend-owned contract tests.
</objective>

<execution_context>
@C:/Users/vision/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/vision/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-backend-seams-and-ownership/01-CONTEXT.md
@.planning/phases/01-backend-seams-and-ownership/01-RESEARCH.md
@AGENTS.md
@spoon-backend/src/context.rs
@spoon-backend/src/ports.rs
@spoon-backend/src/scoop/runtime/execution.rs
@spoon-backend/src/scoop/runtime/actions.rs
@spoon-backend/src/scoop/buckets.rs

<interfaces>
From spoon-backend/src/gitx.rs:
```rust
pub struct RepoSyncOutcome {
    pub head_commit: Option<String>,
    pub head_branch: Option<String>,
}
```

From spoon-backend/src/scoop/runtime/execution.rs:
```rust
pub trait ScoopRuntimeHost {
    fn ensure_user_path_entry(&self, path: &Path) -> Result<()>;
    fn ensure_process_path_entry(&self, path: &Path);
    fn apply_integrations<'a>(...) -> Pin<Box<dyn Future<Output = Result<BTreeMap<String, String>>> + 'a>>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert backend Scoop runtime entry points to BackendContext and split ports</name>
  <files>
spoon-backend/src/scoop/mod.rs
spoon-backend/src/scoop/runtime/execution.rs
spoon-backend/src/scoop/runtime/actions.rs
spoon-backend/src/scoop/tests/contracts.rs
spoon-backend/src/scoop/tests/mod.rs
  </files>
  <read_first>
.planning/phases/01-backend-seams-and-ownership/01-CONTEXT.md
.planning/phases/01-backend-seams-and-ownership/01-RESEARCH.md
spoon-backend/src/context.rs
spoon-backend/src/ports.rs
spoon-backend/src/scoop/mod.rs
spoon-backend/src/scoop/runtime/execution.rs
spoon-backend/src/scoop/runtime/actions.rs
spoon-backend/src/scoop/tests/mod.rs
  </read_first>
  <action>Update the backend Scoop runtime entry points to consume `BackendContext` and the split ports from Plan 01 per D-03, D-05, D-06, and D-09. `spoon-backend/src/scoop/runtime/execution.rs` must stop centering the public API on the old mixed `ScoopRuntimeHost`; replace that shape with backend-owned orchestration that calls `SystemPort` for PATH/home-dir work and `PackageIntegrationPort` for Spoon-owned integrations only. `spoon-backend/src/scoop/runtime/actions.rs` must take runtime inputs from context instead of separate `tool_root` and `proxy` arguments. In the same task, create and wire `spoon-backend/src/scoop/tests/contracts.rs` with a focused `scoop_action_contract_uses_context` test so Task 1 has a runnable Nyquist sample immediately after its edits land. Keep the mixed boundary from D-04 and D-06: generic runtime effects stay backend-owned, Spoon-specific config writes remain narrow app ports, and Phase 1 must not change persisted Scoop state or bucket data shapes.</action>
  <verify>
    <automated>cargo test -p spoon-backend scoop_action_contract_uses_context -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- `Select-String -Pattern 'BackendContext' spoon-backend/src/scoop/runtime/actions.rs` returns a match.
- `Select-String -Pattern 'SystemPort' spoon-backend/src/scoop/runtime/execution.rs` returns a match.
- `Select-String -Pattern 'PackageIntegrationPort' spoon-backend/src/scoop/runtime/execution.rs` returns a match.
- `Select-String -Pattern 'scoop_action_contract_uses_context' spoon-backend/src/scoop/tests/contracts.rs` returns a match.
- `Select-String -Pattern 'tool_root: &' spoon-backend/src/scoop/runtime/actions.rs` returns no matches.
- `Select-String -Pattern 'proxy: Option<&str>' spoon-backend/src/scoop/runtime/actions.rs` returns no matches.
  </acceptance_criteria>
  <done>Backend Scoop runtime actions run through explicit context and split ports, with no public runtime API depending on app-derived root or proxy arguments.</done>
</task>

<task type="auto">
  <name>Task 2: Move bucket sync contracts and tests behind backend-owned Git outcomes</name>
  <files>
spoon-backend/src/scoop/buckets.rs
spoon-backend/src/scoop/tests/contracts.rs
spoon-backend/src/scoop/tests/mod.rs
  </files>
  <read_first>
.planning/phases/01-backend-seams-and-ownership/01-CONTEXT.md
.planning/phases/01-backend-seams-and-ownership/01-RESEARCH.md
spoon-backend/src/context.rs
spoon-backend/src/scoop/buckets.rs
spoon-backend/src/gitx.rs
spoon-backend/src/scoop/tests/contracts.rs
spoon-backend/src/scoop/tests/mod.rs
  </read_first>
  <action>Update `spoon-backend/src/scoop/buckets.rs` so bucket clone and sync entry points take `BackendContext` and backend-owned request values instead of separate `tool_root` and `proxy` inputs, per D-03 and D-09. Keep `clone_repo` and `RepoSyncOutcome` internal to backend bucket behavior; do not introduce app-visible `gix` types or a frontend Git facade. Extend the `spoon-backend/src/scoop/tests/contracts.rs` module created in Task 1 with `bucket_sync_uses_backend_git_contract`, keep it wired from `spoon-backend/src/scoop/tests/mod.rs`, and keep the assertions focused on contract ownership rather than Phase 2 persisted-state cleanup.</action>
  <verify>
    <automated>cargo test -p spoon-backend bucket_sync_uses_backend_git_contract -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- `Select-String -Pattern 'BackendContext' spoon-backend/src/scoop/buckets.rs` returns a match.
- `Select-String -Pattern 'RepoSyncOutcome' spoon-backend/src/scoop/buckets.rs` returns a match.
- `Select-String -Pattern 'bucket_sync_uses_backend_git_contract' spoon-backend/src/scoop/tests/contracts.rs` returns a match.
- `Select-String -Pattern 'gix::' spoon-backend/src/scoop/buckets.rs` returns no matches.
  </acceptance_criteria>
  <done>Bucket clone and sync flows stay backend-owned, expose backend-level outcomes only, and are pinned by nearby contract tests.</done>
</task>

</tasks>

<verification>
Run `cargo test -p spoon-backend scoop_action_contract_uses_context -- --nocapture` and `cargo test -p spoon-backend bucket_sync_uses_backend_git_contract -- --nocapture` after both tasks.
</verification>

<success_criteria>
Backend Scoop runtime and bucket entry points consume explicit backend context, bucket Git progress stays backend-owned, and app-facing Git details do not leak out of `spoon-backend`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-seams-and-ownership/01-02-SUMMARY.md`
</output>

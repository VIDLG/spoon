---
phase: 01-backend-seams-and-ownership
plan: 6
type: execute
wave: 4
depends_on:
  - 01-04
  - 01-05
files_modified:
  - spoon/src/service/scoop/report.rs
  - spoon/src/cli/run.rs
  - spoon/src/view/tools/detail.rs
  - spoon/src/view/config.rs
  - spoon/src/packages/tool.rs
  - spoon/src/config/paths.rs
  - spoon/src/cli/json.rs
  - spoon/tests/cli/json_flow.rs
  - spoon/tests/tui/tui_tool_detail_flow.rs
autonomous: true
requirements:
  - BNDR-05
  - LAY-02
must_haves:
  truths:
    - "Tool detail, prefix, and config-derived runtime path views consume backend models or backend layout contracts instead of app-owned backend path helpers."
  artifacts:
    - path: "spoon/src/view/tools/detail.rs"
      provides: "Detail view built from backend package/runtime models"
      contains: "ToolDetailModel"
    - path: "spoon/src/view/config.rs"
      provides: "Config view that derives backend runtime paths via backend layout"
      contains: "ConfigModel"
    - path: "spoon/tests/tui/tui_tool_detail_flow.rs"
      provides: "UI regression coverage for backend-driven detail surfaces"
      contains: "tool_detail_uses_backend_models"
  key_links:
    - from: "spoon/src/view/tools/detail.rs"
      to: "spoon/src/service/scoop/report.rs"
      via: "detail surfaces consume backend package info/prefix data"
      pattern: "package_info"
    - from: "spoon/src/view/config.rs"
      to: "spoon-backend/src/layout.rs"
      via: "config view derives backend paths through backend layout"
      pattern: "RuntimeLayout"
    - from: "spoon/src/cli/json.rs"
      to: "spoon/src/view/config.rs"
      via: "JSON config output projects backend-driven view models"
      pattern: "ConfigModel"
---

<objective>
Finish the Phase 1 detail, prefix, and config surface conversion.

Purpose: Close the remaining detail/config/path seams from D-07 through D-11 without mixing in the final dependency cleanup.
Output: Backend-driven detail/config surfaces plus regression coverage for JSON and TUI detail views.
</objective>

<execution_context>
@C:/Users/vision/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/vision/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-backend-seams-and-ownership/01-CONTEXT.md
@.planning/phases/01-backend-seams-and-ownership/01-RESEARCH.md
@AGENTS.md
@spoon/src/service/scoop/report.rs
@spoon/src/cli/run.rs
@spoon/src/view/tools/detail.rs
@spoon/src/view/config.rs
@spoon/src/packages/tool.rs
@spoon/src/config/paths.rs
@spoon/tests/cli/json_flow.rs
@spoon/tests/tui/tui_tool_detail_flow.rs
@spoon-backend/src/layout.rs
@spoon-backend/src/status.rs

<interfaces>
From spoon/src/view/tools/detail.rs:
```rust
pub struct ToolDetailModel {
    pub title: String,
    pub rows: Vec<ToolDetailRow>,
}
```

From spoon/src/view/config.rs:
```rust
pub struct ConfigModel {
    pub root_path: String,
    pub derived_scoop_root: String,
    pub derived_managed_msvc_root: String,
    pub derived_managed_msvc_toolchain: String,
    pub derived_official_msvc_root: String,
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert prefix, detail, and report surfaces to backend query and outcome models</name>
  <files>
spoon/src/service/scoop/report.rs
spoon/src/cli/run.rs
spoon/src/view/tools/detail.rs
spoon/src/cli/json.rs
spoon/tests/cli/json_flow.rs
spoon/tests/tui/tui_tool_detail_flow.rs
  </files>
  <read_first>
.planning/phases/01-backend-seams-and-ownership/01-CONTEXT.md
.planning/phases/01-backend-seams-and-ownership/01-RESEARCH.md
spoon/src/service/scoop/report.rs
spoon/src/cli/run.rs
spoon/src/view/tools/detail.rs
spoon/src/cli/json.rs
spoon/tests/cli/json_flow.rs
spoon/tests/tui/tui_tool_detail_flow.rs
spoon-backend/src/layout.rs
spoon-backend/src/status.rs
  </read_first>
  <action>Finish the BNDR-05 conversion for non-status detail surfaces. Update `spoon/src/service/scoop/report.rs` and the related `spoon/src/cli/run.rs` call paths so `prefix`, package detail, and any remaining Scoop report output come from backend outcomes or backend query models instead of app-local `package_current_root`, `installed_package_states_filtered`, or app-side file reads. Update `spoon/src/view/tools/detail.rs` and `spoon/src/cli/json.rs` so detail rendering and JSON projections consume backend-derived package info and runtime facts only. Add or update `spoon/tests/cli/json_flow.rs` and `spoon/tests/tui/tui_tool_detail_flow.rs` with regression tests named `package_report_uses_backend_models` and `tool_detail_uses_backend_models`. Do not introduce Phase 2 persisted-state cleanup or app-side fallback parsing.</action>
  <verify>
    <automated>cargo test -p spoon --test tui_tool_detail_flow tool_detail_uses_backend_models -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- `Select-String -Pattern 'package_current_root' spoon/src/service/scoop/report.rs` returns no matches.
- `Select-String -Pattern 'installed_package_states_filtered' spoon/src/service/scoop/report.rs` returns no matches.
- `Select-String -Pattern 'tool_detail_uses_backend_models' spoon/tests/tui/tui_tool_detail_flow.rs` returns a match.
- `Select-String -Pattern 'package_report_uses_backend_models' spoon/tests/cli/json_flow.rs` returns a match.
  </acceptance_criteria>
  <done>Prefix, report, and tool-detail surfaces depend on backend query and outcome models instead of app-local state reconstruction helpers.</done>
</task>

<task type="auto">
  <name>Task 2: Convert config and runtime-path surfaces to backend layout semantics only</name>
  <files>
spoon/src/view/config.rs
spoon/src/packages/tool.rs
spoon/src/config/paths.rs
spoon/tests/cli/json_flow.rs
  </files>
  <read_first>
.planning/phases/01-backend-seams-and-ownership/01-CONTEXT.md
.planning/phases/01-backend-seams-and-ownership/01-RESEARCH.md
spoon/src/view/config.rs
spoon/src/packages/tool.rs
spoon/src/config/paths.rs
spoon/tests/cli/json_flow.rs
spoon-backend/src/layout.rs
spoon-backend/src/status.rs
  </read_first>
  <action>Update `spoon/src/view/config.rs` so backend runtime paths are derived via backend layout contracts (`RuntimeLayout` or a backend status snapshot) rather than `config::scoop_root_from`, `config::msvc_root_from`, `config::official_msvc_root_from`, or similar app-owned backend path helpers. Shrink `spoon/src/packages/tool.rs` and `spoon/src/config/paths.rs` so they keep app-owned config file semantics only and no longer act as the source of truth for backend runtime layout per D-10 and D-11. Extend `spoon/tests/cli/json_flow.rs` with `config_json_prints_structured_view_model` to pin the backend-driven config projection.</action>
  <verify>
    <automated>cargo test -p spoon --test json_flow config_json_prints_structured_view_model -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- `Select-String -Pattern 'RuntimeLayout' spoon/src/view/config.rs` returns a match.
- `Select-String -Pattern 'config::scoop_root_from' spoon/src/view/config.rs` returns no matches.
- `Select-String -Pattern 'config::msvc_root_from' spoon/src/view/config.rs` returns no matches.
- `Select-String -Pattern 'config::official_msvc_root_from' spoon/src/view/config.rs` returns no matches.
- `Select-String -Pattern 'config_json_prints_structured_view_model' spoon/tests/cli/json_flow.rs` returns a match.
  </acceptance_criteria>
  <done>Config and runtime-path surfaces use backend layout semantics only, and app config modules no longer double as backend layout authorities.</done>
</task>

</tasks>

<verification>
Run `cargo test -p spoon --test json_flow` and `cargo test -p spoon --test tui_tool_detail_flow` after both tasks.
</verification>

<success_criteria>
All Phase 1 app surfaces that display backend runtime data do so through backend contracts, and app-owned backend path helpers are no longer the source of truth for detail, prefix, or config views.
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-seams-and-ownership/01-06-SUMMARY.md`
</output>

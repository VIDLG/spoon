---
phase: 01-backend-seams-and-ownership
plan: 5
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - spoon/src/service/mod.rs
  - spoon/src/service/scoop/mod.rs
  - spoon/src/service/scoop/runtime.rs
  - spoon/src/service/scoop/actions.rs
  - spoon/src/service/scoop/bucket.rs
  - spoon/tests/cli/json_flow.rs
autonomous: true
requirements:
  - BNDR-01
  - BNDR-02
  - GIT-02
  - GIT-03
must_haves:
  truths:
    - "The Spoon app layer builds `BackendContext` requests once and routes Scoop runtime and package actions through backend contracts only."
    - "Bucket adapters map backend events and outcomes only, without app-local Git semantics or result reconstruction."
  artifacts:
    - path: "spoon/src/service/mod.rs"
      provides: "Shared app-side BackendContext builder"
      contains: "BackendContext"
    - path: "spoon/src/service/scoop/actions.rs"
      provides: "Thin app adapter for Scoop package actions"
      contains: "ScoopPackageOperationOutcome"
    - path: "spoon/src/service/scoop/bucket.rs"
      provides: "Thin app adapter for bucket flows"
      contains: "RepoSyncOutcome"
  key_links:
    - from: "spoon/src/service/mod.rs"
      to: "spoon-backend/src/context.rs"
      via: "app builds BackendContext once per request"
      pattern: "BackendContext"
    - from: "spoon/src/service/scoop/actions.rs"
      to: "spoon-backend/src/scoop/runtime/actions.rs"
      via: "package actions consume backend outcomes directly"
      pattern: "ScoopPackageOperationOutcome"
    - from: "spoon/src/service/scoop/bucket.rs"
      to: "spoon-backend/src/scoop/buckets.rs"
      via: "bucket flows consume backend events and repo outcomes only"
      pattern: "RepoSyncOutcome"
---

<objective>
Thin the Spoon Scoop adapters after the backend contracts land.

Purpose: Finish the app-shell half of BNDR-01, BNDR-02, GIT-02, and GIT-03 without changing backend state persistence or reintroducing app-side Git ownership.
Output: One shared app-side `BackendContext` builder plus thin Scoop runtime, action, and bucket adapters.
</objective>

<execution_context>
@C:/Users/vision/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/vision/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-backend-seams-and-ownership/01-CONTEXT.md
@.planning/phases/01-backend-seams-and-ownership/01-RESEARCH.md
@AGENTS.md
@spoon/src/service/mod.rs
@spoon/src/service/scoop/mod.rs
@spoon/src/service/scoop/runtime.rs
@spoon/src/service/scoop/actions.rs
@spoon/src/service/scoop/bucket.rs
@spoon/tests/cli/json_flow.rs
@spoon-backend/src/context.rs
@spoon-backend/src/scoop/runtime/actions.rs
@spoon-backend/src/scoop/buckets.rs

<interfaces>
From spoon-backend/src/context.rs:
```rust
pub struct BackendContext<P> {
    pub root: PathBuf,
    pub layout: RuntimeLayout,
    pub proxy: Option<String>,
    pub test_mode: bool,
    pub msvc_target_arch: String,
    pub msvc_command_profile: String,
    pub ports: P,
}
```

From spoon-backend/src/gitx.rs:
```rust
pub struct RepoSyncOutcome {
    pub head_commit: Option<String>,
    pub head_branch: Option<String>,
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add one shared BackendContext builder and thin package/runtime adapters</name>
  <files>
spoon/src/service/mod.rs
spoon/src/service/scoop/runtime.rs
spoon/src/service/scoop/actions.rs
spoon/tests/cli/json_flow.rs
  </files>
  <read_first>
.planning/phases/01-backend-seams-and-ownership/01-CONTEXT.md
.planning/phases/01-backend-seams-and-ownership/01-RESEARCH.md
spoon/src/service/mod.rs
spoon/src/service/scoop/runtime.rs
spoon/src/service/scoop/actions.rs
spoon/tests/cli/json_flow.rs
spoon-backend/src/context.rs
spoon-backend/src/scoop/runtime/actions.rs
  </read_first>
  <action>Add one helper in `spoon/src/service/mod.rs` that converts app config into `BackendContext` with the app-owned package ports per D-02 and D-06. Update `spoon/src/service/scoop/runtime.rs` and `spoon/src/service/scoop/actions.rs` so they call the new backend context-driven entry points, keep app-specific event mapping only, and stop calling `load_backend_config()` or deriving backend runtime paths locally. Remove app-local result reconstruction in package action code where backend outcomes already provide the same facts. Extend `spoon/tests/cli/json_flow.rs` with a regression named `install_json_prints_structured_package_action_results` that exercises the thin adapter path.</action>
  <verify>
    <automated>cargo test -p spoon --test json_flow install_json_prints_structured_package_action_results -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- `Select-String -Pattern 'BackendContext' spoon/src/service/mod.rs` returns a match.
- `Select-String -Pattern 'load_backend_config' spoon/src/service/scoop/actions.rs` returns no matches.
- `Select-String -Pattern 'load_backend_config' spoon/src/service/scoop/runtime.rs` returns no matches.
- `Select-String -Pattern 'package_current_root' spoon/src/service/scoop/actions.rs` returns no matches.
- `Select-String -Pattern 'install_json_prints_structured_package_action_results' spoon/tests/cli/json_flow.rs` returns a match.
  </acceptance_criteria>
  <done>The app Scoop runtime and package-action adapters are reduced to request construction and backend outcome mapping.</done>
</task>

<task type="auto">
  <name>Task 2: Thin the bucket adapter to backend events and repo outcomes only</name>
  <files>
spoon/src/service/scoop/mod.rs
spoon/src/service/scoop/bucket.rs
spoon/tests/cli/json_flow.rs
  </files>
  <read_first>
.planning/phases/01-backend-seams-and-ownership/01-CONTEXT.md
.planning/phases/01-backend-seams-and-ownership/01-RESEARCH.md
spoon/src/service/scoop/mod.rs
spoon/src/service/scoop/bucket.rs
spoon/tests/cli/json_flow.rs
spoon-backend/src/scoop/buckets.rs
spoon-backend/src/gitx.rs
  </read_first>
  <action>Update `spoon/src/service/scoop/bucket.rs` and any export wiring in `spoon/src/service/scoop/mod.rs` so bucket adapters call backend bucket entry points, map backend events, and return backend-level outcomes only per D-07 through D-09. Do not leave any `gix`-shaped types, app-local repo sync structs, or backend path derivation in the app bucket layer. Extend `spoon/tests/cli/json_flow.rs` with a regression named `bucket_json_uses_backend_repo_sync_outcome` that fails if the bucket path rebuilds Git semantics in `spoon/src`.</action>
  <verify>
    <automated>cargo test -p spoon --test json_flow bucket_json_uses_backend_repo_sync_outcome -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- `Select-String -Pattern 'load_backend_config' spoon/src/service/scoop/bucket.rs` returns no matches.
- `Select-String -Pattern 'gix::' spoon/src/service/scoop/bucket.rs` returns no matches.
- `Select-String -Pattern 'RepoSyncOutcome' spoon/src/service/scoop/bucket.rs` returns a match.
- `Select-String -Pattern 'bucket_json_uses_backend_repo_sync_outcome' spoon/tests/cli/json_flow.rs` returns a match.
  </acceptance_criteria>
  <done>The app bucket adapter speaks backend events and outcomes only, with no app-owned Git contract left behind.</done>
</task>

</tasks>

<verification>
Run `cargo test -p spoon --test json_flow install_json_prints_structured_package_action_results -- --nocapture` and `cargo test -p spoon --test json_flow bucket_json_uses_backend_repo_sync_outcome -- --nocapture` after both tasks.
</verification>

<success_criteria>
The app Scoop service layer constructs explicit backend requests, consumes backend outcomes directly, and no longer owns bucket or package Git semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-seams-and-ownership/01-05-SUMMARY.md`
</output>

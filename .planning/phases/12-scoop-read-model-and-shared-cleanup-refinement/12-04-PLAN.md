---
phase: 12-scoop-read-model-and-shared-cleanup-refinement
plan: 4
type: execute
wave: 3
depends_on:
  - 12-01
  - 12-02
  - 12-03
files_modified:
  - .planning/phases/12-scoop-read-model-and-shared-cleanup-refinement
autonomous: true
requirements:
  - SLEG-03
  - BECT-06
must_haves:
  truths:
    - "Phase 12 ends with explicit verification evidence."
    - "The read-model cleanup is documented clearly enough to hand off to the safety-net phase."
  artifacts:
    - path: ".planning/phases/12-scoop-read-model-and-shared-cleanup-refinement/12-VERIFICATION.md"
      provides: "phase verification record"
      contains: "actual commands run and residual risk"
---

<objective>
Verify and document the read-model cleanup phase.

Purpose: close Phase 12 with concrete evidence and a clean handoff to the safety-net refresh.
Output: summaries plus phase verification record.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Re-run representative regressions after the read-model cleanup</name>
  <files>
spoon/tests/cli/status_backend_flow.rs
spoon/tests/cli/scoop_flow.rs
  </files>
  <read_first>
spoon/tests/cli/status_backend_flow.rs
spoon/tests/cli/scoop_flow.rs
  </read_first>
  <action>Reconfirm the representative backend/app-shell regressions still hold after the DTO/read-model cleanup.</action>
  <verify>
    <automated>cargo test -p spoon --test status_backend_flow -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- Representative regressions still pass after the read-model cleanup.
  </acceptance_criteria>
  <done>The cleanup is grounded in verification rather than just structural preference.</done>
</task>

<task type="auto">
  <name>Task 2: Record summaries and phase verification</name>
  <files>
.planning/phases/12-scoop-read-model-and-shared-cleanup-refinement
  </files>
  <read_first>
.planning/phases/12-scoop-read-model-and-shared-cleanup-refinement/12-VALIDATION.md
  </read_first>
  <action>Write summaries and the phase verification record with the commands actually used and the remaining risk profile.</action>
  <verify>
    <automated>cargo check -p spoon-backend -p spoon</automated>
  </verify>
  <acceptance_criteria>
- Phase 12 is documented with explicit evidence and a clear handoff.
  </acceptance_criteria>
  <done>Phase 12 is closed as a verifiable cleanup phase.</done>
</task>

</tasks>

<verification>
Run `cargo test -p spoon --test status_backend_flow -- --nocapture` and `cargo check -p spoon-backend -p spoon`.
</verification>

<output>
After completion, create `.planning/phases/12-scoop-read-model-and-shared-cleanup-refinement/12-04-SUMMARY.md` and `12-VERIFICATION.md`
</output>

---
phase: 12-scoop-read-model-and-shared-cleanup-refinement
plan: 3
type: execute
wave: 2
depends_on:
  - 12-01
  - 12-02
files_modified:
  - spoon-backend/src/scoop/projection.rs
  - spoon-backend/src/scoop/info.rs
  - spoon-backend/src/scoop/query.rs
  - Cargo.toml
autonomous: true
requirements:
  - BECT-06
must_haves:
  truths:
    - "`projection.rs` is demoted further into an internal helper role."
    - "`schemars` is used only if it clearly improves the surviving outward contract."
  artifacts:
    - path: "spoon-backend/src/scoop/projection.rs"
      provides: "thinner internal helper pool"
      contains: "fewer dead or overly central helper exports"
---

<objective>
Demote projection helpers and evaluate narrow schema hardening.

Purpose: make helper layers less architecturally important and optionally harden the true outward contracts with targeted schema support.
Output: thinner projection helper surface and clearer surviving outward contracts.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead or needlessly central projection helpers</name>
  <files>
spoon-backend/src/scoop/projection.rs
spoon-backend/src/scoop/info.rs
spoon-backend/src/scoop/query.rs
  </files>
  <read_first>
spoon-backend/src/scoop/projection.rs
spoon-backend/src/scoop/info.rs
spoon-backend/src/scoop/query.rs
  </read_first>
  <action>Delete unused projection helpers and further reduce the sense that projection is a public architectural layer.</action>
  <verify>
    <automated>cargo check -p spoon-backend -p spoon</automated>
  </verify>
  <acceptance_criteria>
- Projection helpers are slimmer and more obviously internal.
  </acceptance_criteria>
  <done>The helper layer recedes into implementation detail.</done>
</task>

<task type="auto">
  <name>Task 2: Evaluate targeted `schemars` usage for surviving outward contracts</name>
  <files>
Cargo.toml
spoon-backend/src/scoop/info.rs
spoon-backend/src/scoop/query.rs
  </files>
  <read_first>
.planning/phases/12-scoop-read-model-and-shared-cleanup-refinement/12-CONTEXT.md
Cargo.toml
spoon-backend/src/scoop/info.rs
spoon-backend/src/scoop/query.rs
  </read_first>
  <action>If a small, justified set of surviving outward-facing read models clearly benefits from schema-backed contract hardening, introduce `schemars` narrowly; otherwise record the evaluation outcome and leave the crate out.</action>
  <verify>
    <automated>cargo test -p spoon-backend runtime_writes_canonical_scoop_state -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- `schemars` is either used narrowly with clear value or explicitly rejected for now with the code left cleaner anyway.
  </acceptance_criteria>
  <done>The phase resolves the schema-hardening question instead of leaving it vague.</done>
</task>

</tasks>

<verification>
Run `cargo check -p spoon-backend -p spoon` and `cargo test -p spoon-backend runtime_writes_canonical_scoop_state -- --nocapture`.
</verification>

<output>
After completion, create `.planning/phases/12-scoop-read-model-and-shared-cleanup-refinement/12-03-SUMMARY.md`
</output>

---
phase: 12-scoop-read-model-and-shared-cleanup-refinement
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - spoon-backend/src/scoop/query.rs
  - spoon-backend/src/status.rs
autonomous: true
requirements:
  - SLEG-03
must_haves:
  truths:
    - "Pure pass-through DTOs in the Scoop query/status surface are removed."
    - "Domain/state structures are reused by default unless a separate outward contract is justified."
  artifacts:
    - path: "spoon-backend/src/scoop/query.rs"
      provides: "cleaner Scoop query DTO layer"
      contains: "fewer pure pass-through structs"
---

<objective>
Delete pure pass-through DTOs in the Scoop query/status surface.

Purpose: reduce low-value structure duplication by reusing domain/state structures wherever a separate outward contract is not justified.
Output: leaner query/status data shapes.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Remove pure pass-through DTOs from query/status</name>
  <files>
spoon-backend/src/scoop/query.rs
spoon-backend/src/status.rs
  </files>
  <read_first>
.planning/phases/12-scoop-read-model-and-shared-cleanup-refinement/12-CONTEXT.md
.planning/phases/12-scoop-read-model-and-shared-cleanup-refinement/12-RESEARCH.md
spoon-backend/src/scoop/query.rs
spoon-backend/src/status.rs
  </read_first>
  <action>Delete or collapse pass-through DTOs such as duplicate bucket/package entry wrappers when the original domain/state structure can be used directly.</action>
  <verify>
    <automated>cargo check -p spoon-backend -p spoon</automated>
  </verify>
  <acceptance_criteria>
- Pure copy DTOs are removed from representative Scoop read surfaces.
  </acceptance_criteria>
  <done>Read-model duplication is lower and the surviving structs have clearer reasons to exist.</done>
</task>

<task type="auto">
  <name>Task 2: Reconfirm app/backend status integration after DTO cleanup</name>
  <files>
spoon/tests/cli/status_backend_flow.rs
  </files>
  <read_first>
spoon/tests/cli/status_backend_flow.rs
  </read_first>
  <action>Adjust any thin app-side status test expectations needed after DTO reuse so app/backend integration remains intact.</action>
  <verify>
    <automated>cargo test -p spoon --test status_backend_flow -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- App/backend status integration remains stable after DTO cleanup.
  </acceptance_criteria>
  <done>The DTO cleanup does not break status integration.</done>
</task>

</tasks>

<verification>
Run `cargo check -p spoon-backend -p spoon` and `cargo test -p spoon --test status_backend_flow -- --nocapture`.
</verification>

<output>
After completion, create `.planning/phases/12-scoop-read-model-and-shared-cleanup-refinement/12-01-SUMMARY.md`
</output>

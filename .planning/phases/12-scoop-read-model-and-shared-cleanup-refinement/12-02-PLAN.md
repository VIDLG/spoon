---
phase: 12-scoop-read-model-and-shared-cleanup-refinement
plan: 2
type: execute
wave: 1
depends_on:
  - 12-01
files_modified:
  - spoon-backend/src/scoop/query.rs
  - spoon-backend/src/scoop/info.rs
  - spoon-backend/src/status.rs
autonomous: true
requirements:
  - SLEG-03
must_haves:
  truths:
    - "Low-value derived fields are removed from read models unless they clearly strengthen the output contract."
    - "Counts and similarly trivial derived fields no longer survive by default."
  artifacts:
    - path: "spoon-backend/src/scoop/query.rs"
      provides: "derive-not-store enforcement in read models"
      contains: "fewer low-value derived fields"
---

<objective>
Apply derive-not-store discipline to Scoop read models.

Purpose: remove low-value counts and trivial derived fields that add noise without meaningful contract value.
Output: cleaner outward data shapes with less redundancy.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Remove low-value derived fields from Scoop read models</name>
  <files>
spoon-backend/src/scoop/query.rs
spoon-backend/src/scoop/info.rs
spoon-backend/src/status.rs
  </files>
  <read_first>
spoon-backend/src/scoop/query.rs
spoon-backend/src/scoop/info.rs
spoon-backend/src/status.rs
  </read_first>
  <action>Delete count and similar derived fields unless they provide clear external contract value that is worth preserving.</action>
  <verify>
    <automated>cargo check -p spoon-backend -p spoon</automated>
  </verify>
  <acceptance_criteria>
- Low-value count/derived fields are reduced or removed from representative read models.
  </acceptance_criteria>
  <done>The read model becomes quieter and more intentional.</done>
</task>

<task type="auto">
  <name>Task 2: Reconfirm representative app-shell Scoop flow output after field cleanup</name>
  <files>
spoon/tests/cli/scoop_flow.rs
  </files>
  <read_first>
spoon/tests/cli/scoop_flow.rs
  </read_first>
  <action>Adjust thin app-shell Scoop expectations only where needed after the field cleanup, without turning app tests into backend re-implementation.</action>
  <verify>
    <automated>cargo test -p spoon --test scoop_flow -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- App-shell Scoop flow remains stable after low-value field cleanup.
  </acceptance_criteria>
  <done>Field cleanup does not break user-facing thin-shell behavior.</done>
</task>

</tasks>

<verification>
Run `cargo check -p spoon-backend -p spoon` and `cargo test -p spoon --test scoop_flow -- --nocapture`.
</verification>

<output>
After completion, create `.planning/phases/12-scoop-read-model-and-shared-cleanup-refinement/12-02-SUMMARY.md`
</output>

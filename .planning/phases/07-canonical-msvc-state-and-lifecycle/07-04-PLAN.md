---
phase: 07-canonical-msvc-state-and-lifecycle
plan: 4
type: execute
wave: 4
depends_on:
  - 07-01-PLAN.md
  - 07-02-PLAN.md
  - 07-03-PLAN.md
files_modified:
  - spoon-backend/src/msvc/query.rs
  - spoon-backend/src/msvc/status.rs
  - spoon-backend/src/msvc/doctor.rs
  - spoon-backend/src/msvc/tests/context.rs
autonomous: true
requirements:
  - MSVC-02
  - MSVC-03
must_haves:
  truths:
    - "MSVC status/query/doctor now operate over canonical state plus evidence-backed reconciliation."
    - "Phase 7 ends with focused regressions around the new backend state machine."
  artifacts:
    - path: "spoon-backend/src/msvc/query.rs"
      provides: "MSVC read-model surface aligned to canonical state"
      contains: "canonical-state-first query/status projection"
---

<objective>
Align read models and diagnostics around the new canonical MSVC state machine and lock the result with focused regressions.

Purpose: make the new state/lifecycle design visible and safe across query/status/doctor paths.
Output: canonical-state-first MSVC query/status/doctor surfaces plus focused regression coverage.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Move MSVC query/status/doctor onto canonical-state-first read paths</name>
  <files>
spoon-backend/src/msvc/query.rs
spoon-backend/src/msvc/status.rs
spoon-backend/src/msvc/doctor.rs
spoon-backend/src/msvc/tests/context.rs
  </files>
  <read_first>
.planning/phases/07-canonical-msvc-state-and-lifecycle/07-CONTEXT.md
spoon-backend/src/msvc/query.rs
spoon-backend/src/msvc/status.rs
spoon-backend/src/msvc/tests/context.rs
  </read_first>
  <action>Refactor MSVC query/status/doctor paths so they read from canonical state first and use detection/validation as evidence-backed reconcile input rather than as the only source of truth.</action>
  <verify>
    <automated>cargo test -p spoon-backend --lib msvc::tests::context -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- Status/query/doctor align to canonical state plus evidence-backed reconciliation.
- Phase 7 still avoids implementing a full repair system.
  </acceptance_criteria>
  <done>The new canonical state machine is reflected in read paths, not just writes.</done>
</task>

<task type="auto">
  <name>Task 2: Finish Phase 7 with focused state-machine regressions</name>
  <files>
spoon-backend/src/msvc/tests/context.rs
spoon-backend/src/msvc/tests/root.rs
spoon-backend/src/msvc/tests/official.rs
  </files>
  <read_first>
spoon-backend/src/msvc/tests/context.rs
spoon-backend/src/msvc/tests/root.rs
spoon-backend/src/msvc/tests/official.rs
  </read_first>
  <action>Update or extend focused MSVC backend regressions so canonical state, lifecycle execution, and reconcile behavior are protected close to the logic that changed.</action>
  <verify>
    <automated>cargo check -p spoon-backend -p spoon</automated>
  </verify>
  <acceptance_criteria>
- Focused regressions exist for the new canonical-state/lifecycle behavior.
  </acceptance_criteria>
  <done>Phase 7 ends with a protected backend state machine rather than an unguarded rewrite.</done>
</task>

</tasks>

<verification>
Run `cargo test -p spoon-backend --lib msvc::tests::context -- --nocapture` and `cargo check -p spoon-backend -p spoon`.
</verification>

<output>
After completion, create `.planning/phases/07-canonical-msvc-state-and-lifecycle/07-04-SUMMARY.md`
</output>

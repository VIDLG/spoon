---
phase: 07-canonical-msvc-state-and-lifecycle
plan: 2
type: execute
wave: 2
depends_on:
  - 07-01-PLAN.md
files_modified:
  - spoon-backend/src/msvc/execute.rs
  - spoon-backend/src/msvc/state.rs
  - spoon-backend/src/msvc/tests/root.rs
  - spoon/tests/tui/tui_msvc_download_flow.rs
autonomous: true
requirements:
  - MSVC-03
must_haves:
  truths:
    - "Managed runtime execution runs under the shared MSVC lifecycle contract."
    - "Validation is part of the lifecycle story rather than a detached helper."
  artifacts:
    - path: "spoon-backend/src/msvc/execute.rs"
      provides: "Managed strategy execution under shared lifecycle semantics"
      contains: "shared stage progression plus managed-specific execution branch"
---

<objective>
Land the shared lifecycle contract on the managed MSVC strategy first.

Purpose: prove that the new canonical-state model can drive real backend lifecycle execution.
Output: managed install/update/uninstall/validate behavior aligned to the shared lifecycle contract.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Run managed execution and validation through the shared lifecycle contract</name>
  <files>
spoon-backend/src/msvc/execute.rs
spoon-backend/src/msvc/state.rs
spoon-backend/src/msvc/tests/root.rs
  </files>
  <read_first>
.planning/phases/07-canonical-msvc-state-and-lifecycle/07-CONTEXT.md
spoon-backend/src/msvc/execute.rs
spoon-backend/src/msvc/state.rs
spoon-backend/src/msvc/tests/root.rs
  </read_first>
  <action>Refactor managed execution and validation so they move through the shared MSVC lifecycle language, update canonical state appropriately, and stop behaving like an isolated legacy flow.</action>
  <verify>
    <automated>cargo test -p spoon-backend --lib msvc::tests::root -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- Managed install/update/uninstall/validate align to the shared lifecycle contract.
- Canonical state reflects managed lifecycle outcomes.
  </acceptance_criteria>
  <done>Managed runtime now behaves like a strategy branch under one MSVC state machine.</done>
</task>

<task type="auto">
  <name>Task 2: Keep app-visible managed progress behavior stable while lifecycle execution deepens</name>
  <files>
spoon/tests/tui/tui_msvc_download_flow.rs
spoon-backend/src/msvc/execute.rs
  </files>
  <read_first>
spoon/tests/tui/tui_msvc_download_flow.rs
spoon-backend/src/msvc/execute.rs
  </read_first>
  <action>Update or reinforce TUI-visible managed MSVC progress coverage so the deeper lifecycle/state integration does not regress the existing app contract.</action>
  <verify>
    <automated>cargo test -p spoon --test tui_msvc_download_flow -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- Managed progress behavior remains stable at the app shell after lifecycle/state integration.
  </acceptance_criteria>
  <done>Phase 7 deepens backend execution without regressing the managed app contract.</done>
</task>

</tasks>

<verification>
Run `cargo test -p spoon-backend --lib msvc::tests::root -- --nocapture` and `cargo test -p spoon --test tui_msvc_download_flow -- --nocapture`.
</verification>

<output>
After completion, create `.planning/phases/07-canonical-msvc-state-and-lifecycle/07-02-SUMMARY.md`
</output>

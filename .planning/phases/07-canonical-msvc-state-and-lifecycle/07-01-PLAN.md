---
phase: 07-canonical-msvc-state-and-lifecycle
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - spoon-backend/src/control_plane/schema/0001_control_plane.sql
  - spoon-backend/src/control_plane/sqlite.rs
  - spoon-backend/src/msvc/state.rs
  - spoon-backend/src/msvc/plan.rs
autonomous: true
requirements:
  - MSVC-02
must_haves:
  truths:
    - "MSVC canonical state lives in the existing SQLite control plane."
    - "New canonical state follows derive-not-store rather than persisting easy-but-derivable fields."
  artifacts:
    - path: "spoon-backend/src/msvc/state.rs"
      provides: "Canonical MSVC state model and store surface"
      contains: "shared envelope, lifecycle facts, runtime-specific detail"
---

<objective>
Add canonical MSVC state to the existing SQLite control plane with derive-not-store discipline.

Purpose: make MSVC a first-class backend state domain instead of leaving it on runtime-local JSON/state assumptions.
Output: SQLite schema and store APIs for canonical MSVC records.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Extend the SQLite control plane for canonical MSVC state and lifecycle facts</name>
  <files>
spoon-backend/src/control_plane/schema/0001_control_plane.sql
spoon-backend/src/control_plane/sqlite.rs
spoon-backend/src/msvc/state.rs
  </files>
  <read_first>
.planning/phases/07-canonical-msvc-state-and-lifecycle/07-CONTEXT.md
.planning/phases/07-canonical-msvc-state-and-lifecycle/07-RESEARCH.md
.planning/todos/pending/2026-04-01-audit-derive-not-store-fields.md
spoon-backend/src/control_plane/schema/0001_control_plane.sql
  </read_first>
  <action>Extend the SQLite control plane and add an MSVC state/store module that captures one canonical envelope, shared lifecycle facts, and runtime-specific detail for `managed` and `official` without persisting obviously derivable fields.</action>
  <verify>
    <automated>cargo test -p spoon-backend --lib msvc::tests::context -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- SQLite schema and backend store code support canonical MSVC state.
- New persisted fields follow the derive-not-store rule unless explicitly justified.
  </acceptance_criteria>
  <done>MSVC has a real control-plane state home inside SQLite.</done>
</task>

<task type="auto">
  <name>Task 2: Align seam-level MSVC contract types with canonical-state storage</name>
  <files>
spoon-backend/src/msvc/plan.rs
spoon-backend/src/msvc/state.rs
  </files>
  <read_first>
spoon-backend/src/msvc/plan.rs
spoon-backend/src/msvc/state.rs
  </read_first>
  <action>Refine the current seam-level MSVC contract types so they line up cleanly with the new canonical-state model instead of leaving Phase 7 with mismatched state and request concepts.</action>
  <verify>
    <automated>cargo check -p spoon-backend</automated>
  </verify>
  <acceptance_criteria>
- Canonical-state and seam contract types are coherent.
  </acceptance_criteria>
  <done>Phase 7 can build lifecycle execution directly against the canonical-state contract.</done>
</task>

</tasks>

<verification>
Run `cargo test -p spoon-backend --lib msvc::tests::context -- --nocapture` and `cargo check -p spoon-backend`.
</verification>

<output>
After completion, create `.planning/phases/07-canonical-msvc-state-and-lifecycle/07-01-SUMMARY.md`
</output>

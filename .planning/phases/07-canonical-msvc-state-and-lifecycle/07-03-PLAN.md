---
phase: 07-canonical-msvc-state-and-lifecycle
plan: 3
type: execute
wave: 3
depends_on:
  - 07-01-PLAN.md
files_modified:
  - spoon-backend/src/msvc/official.rs
  - spoon-backend/src/msvc/state.rs
  - spoon-backend/src/msvc/tests/official.rs
  - spoon/tests/cli/msvc_flow.rs
autonomous: true
requirements:
  - MSVC-02
  - MSVC-03
must_haves:
  truths:
    - "Official runtime participates in the same high-level MSVC lifecycle contract."
    - "Official detection/validation acts as evidence and reconcile source for canonical state."
  artifacts:
    - path: "spoon-backend/src/msvc/official.rs"
      provides: "Official strategy under the shared lifecycle/state model"
      contains: "evidence-backed reconciliation with canonical state"
---

<objective>
Integrate the official runtime strategy into the shared MSVC lifecycle and canonical-state model.

Purpose: prevent `official` from remaining architecturally outside the domain.
Output: official runtime behavior that updates and reconciles canonical state through evidence-backed lifecycle execution.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Rework official runtime flows to participate in the shared lifecycle/state model</name>
  <files>
spoon-backend/src/msvc/official.rs
spoon-backend/src/msvc/state.rs
spoon-backend/src/msvc/tests/official.rs
  </files>
  <read_first>
.planning/phases/07-canonical-msvc-state-and-lifecycle/07-CONTEXT.md
spoon-backend/src/msvc/official.rs
spoon-backend/src/msvc/state.rs
spoon-backend/src/msvc/tests/official.rs
  </read_first>
  <action>Refactor official install/update/uninstall/validate so they map onto the shared MSVC lifecycle contract and maintain canonical state using official detection/validation as evidence and reconcile input.</action>
  <verify>
    <automated>cargo test -p spoon-backend --lib msvc::tests::official -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- Official runtime uses the shared lifecycle language.
- Canonical state is updated and reconciled from official evidence rather than bypassed.
  </acceptance_criteria>
  <done>Official runtime is a strategy inside one MSVC domain, not a special-case side system.</done>
</task>

<task type="auto">
  <name>Task 2: Keep CLI-visible official flows stable while canonical-state ownership deepens</name>
  <files>
spoon/tests/cli/msvc_flow.rs
spoon-backend/src/msvc/official.rs
  </files>
  <read_first>
spoon/tests/cli/msvc_flow.rs
spoon-backend/src/msvc/official.rs
  </read_first>
  <action>Update or reinforce CLI-visible official MSVC regressions so the canonical-state/lifecycle integration preserves the app-visible contract.</action>
  <verify>
    <automated>cargo test -p spoon --test msvc_flow -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- Official CLI flows remain stable after canonical-state integration.
  </acceptance_criteria>
  <done>The official runtime remains user-visible in the same way while backend ownership becomes stronger.</done>
</task>

</tasks>

<verification>
Run `cargo test -p spoon-backend --lib msvc::tests::official -- --nocapture` and `cargo test -p spoon --test msvc_flow -- --nocapture`.
</verification>

<output>
After completion, create `.planning/phases/07-canonical-msvc-state-and-lifecycle/07-03-SUMMARY.md`
</output>

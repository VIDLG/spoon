---
phase: 02-canonical-scoop-state
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - spoon-backend/src/scoop/state/mod.rs
  - spoon-backend/src/scoop/state/model.rs
  - spoon-backend/src/scoop/state/store.rs
  - spoon-backend/src/scoop/mod.rs
  - spoon-backend/src/scoop/runtime/mod.rs
  - spoon-backend/src/scoop/tests/state.rs
  - spoon-backend/src/scoop/tests/mod.rs
autonomous: true
requirements:
  - SCST-01
  - SCST-04
must_haves:
  truths:
    - "There is exactly one backend-owned Scoop installed-package record type and it lives under `spoon-backend/src/scoop/state/`."
    - "The canonical installed-package record contains `bucket` and `architecture` and does not persist absolute paths or `current` roots."
  artifacts:
    - path: "spoon-backend/src/scoop/state/model.rs"
      provides: "Canonical Scoop installed-state model"
      contains: "pub struct InstalledPackageState"
    - path: "spoon-backend/src/scoop/state/store.rs"
      provides: "Canonical state persistence and enumeration APIs"
      contains: "pub async fn write_installed_state"
    - path: "spoon-backend/src/scoop/tests/state.rs"
      provides: "Canonical state contract tests"
      contains: "canonical_installed_state_roundtrips_bucket_and_architecture"
  key_links:
    - from: "spoon-backend/src/scoop/mod.rs"
      to: "spoon-backend/src/scoop/state/model.rs"
      via: "public Scoop facade re-exports the canonical installed-state model"
      pattern: "InstalledPackageState"
    - from: "spoon-backend/src/scoop/runtime/mod.rs"
      to: "spoon-backend/src/scoop/state/store.rs"
      via: "runtime module reads and writes canonical state through the state store"
      pattern: "write_installed_state"
---

<objective>
Introduce a dedicated Scoop state module around one canonical installed-package record before read/write consumers are migrated.

Purpose: satisfy the schema half of SCST-01 and SCST-04 by making `InstalledPackageState` the only installed-state contract and moving its ownership out of `runtime/`.
Output: `scoop/state/` module, canonical record fields including `bucket` and `architecture`, store APIs, and nearby contract tests.
</objective>

<execution_context>
@C:/Users/vision/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/vision/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-canonical-scoop-state/02-CONTEXT.md
@.planning/phases/02-canonical-scoop-state/02-RESEARCH.md
@AGENTS.md
@spoon-backend/src/scoop/mod.rs
@spoon-backend/src/scoop/runtime/mod.rs
@spoon-backend/src/scoop/runtime/installed_state.rs
@spoon-backend/src/scoop/package_state.rs
@spoon-backend/src/scoop/tests/mod.rs

<interfaces>
From spoon-backend/src/scoop/runtime/installed_state.rs:
```rust
pub struct InstalledPackageState {
    pub package: String,
    pub version: String,
    pub cache_size_bytes: Option<u64>,
    pub bins: Vec<String>,
    pub shortcuts: Vec<ShortcutEntry>,
    pub env_add_path: Vec<String>,
    pub env_set: BTreeMap<String, String>,
    pub persist: Vec<PersistEntry>,
    pub integrations: BTreeMap<String, String>,
    pub pre_uninstall: Vec<String>,
    pub uninstaller_script: Vec<String>,
    pub post_uninstall: Vec<String>,
}
```

From spoon-backend/src/scoop/package_state.rs:
```rust
pub struct ScoopPackageState {
    pub name: String,
    pub version: String,
    pub bucket: String,
    pub architecture: Option<String>,
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move canonical installed-state ownership into `scoop/state/` and enrich it with bucket and architecture</name>
  <files>
spoon-backend/src/scoop/state/mod.rs
spoon-backend/src/scoop/state/model.rs
spoon-backend/src/scoop/state/store.rs
spoon-backend/src/scoop/mod.rs
spoon-backend/src/scoop/runtime/mod.rs
  </files>
  <read_first>
.planning/phases/02-canonical-scoop-state/02-CONTEXT.md
.planning/phases/02-canonical-scoop-state/02-RESEARCH.md
spoon-backend/src/scoop/runtime/installed_state.rs
spoon-backend/src/scoop/package_state.rs
spoon-backend/src/scoop/mod.rs
spoon-backend/src/scoop/runtime/mod.rs
  </read_first>
  <action>Create `spoon-backend/src/scoop/state/mod.rs`, `spoon-backend/src/scoop/state/model.rs`, and `spoon-backend/src/scoop/state/store.rs`. Move `InstalledPackageState` ownership into `state/model.rs` and keep the public type name `InstalledPackageState`. Add concrete fields `bucket: String` and `architecture: Option<String>` to the struct. Keep the existing operational facts: `package`, `version`, `cache_size_bytes`, `bins`, `shortcuts`, `env_add_path`, `env_set`, `persist`, `integrations`, `pre_uninstall`, `uninstaller_script`, and `post_uninstall`. Do not add any absolute-path field, `current` field, or layout-derived root field. Move `read_installed_state`, `write_installed_state`, and `remove_installed_state` into `state/store.rs`, and add one typed enumeration API named `list_installed_states`. Re-export the new module from `spoon-backend/src/scoop/mod.rs` and point `spoon-backend/src/scoop/runtime/mod.rs` at the new store APIs so the runtime no longer owns installed-state persistence.</action>
  <verify>
    <automated>cargo test -p spoon-backend canonical_installed_state_roundtrips_bucket_and_architecture -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- `Select-String -Path spoon-backend/src/scoop/state/model.rs -Pattern 'pub struct InstalledPackageState'` returns a match.
- `Select-String -Path spoon-backend/src/scoop/state/model.rs -Pattern 'pub bucket: String'` returns a match.
- `Select-String -Path spoon-backend/src/scoop/state/model.rs -Pattern 'pub architecture: Option<String>'` returns a match.
- `Select-String -Path spoon-backend/src/scoop/state/model.rs -Pattern 'current'` returns no matches.
- `Select-String -Path spoon-backend/src/scoop/state/store.rs -Pattern 'pub async fn list_installed_states'` returns a match.
- `Select-String -Path spoon-backend/src/scoop/runtime/mod.rs -Pattern 'write_installed_state'` returns a match.
  </acceptance_criteria>
  <done>The canonical Scoop installed-state contract is single-owned under `scoop/state/`, includes `bucket` and `architecture`, and persists only non-derivable facts.</done>
</task>

<task type="auto">
  <name>Task 2: Add canonical state contract tests and wire them into Scoop test modules</name>
  <files>
spoon-backend/src/scoop/tests/state.rs
spoon-backend/src/scoop/tests/mod.rs
  </files>
  <read_first>
.planning/phases/02-canonical-scoop-state/02-CONTEXT.md
.planning/phases/02-canonical-scoop-state/02-RESEARCH.md
spoon-backend/src/scoop/tests/mod.rs
spoon-backend/src/scoop/state/model.rs
spoon-backend/src/scoop/state/store.rs
  </read_first>
  <action>Add `spoon-backend/src/scoop/tests/state.rs` and wire it from `spoon-backend/src/scoop/tests/mod.rs`. Create at least two tests: `canonical_installed_state_roundtrips_bucket_and_architecture`, which writes and reads a canonical installed-state record containing `bucket = "main"` and `architecture = Some("x64".to_string())`, and `canonical_state_persists_only_nonderivable_facts`, which asserts the serialized JSON contains `package`, `version`, `bucket`, and `architecture` but does not contain keys named `current`, `current_root`, `shims_root`, `apps_root`, or `tool_root`.</action>
  <verify>
    <automated>cargo test -p spoon-backend canonical_state_persists_only_nonderivable_facts -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- `Select-String -Path spoon-backend/src/scoop/tests/state.rs -Pattern 'canonical_installed_state_roundtrips_bucket_and_architecture'` returns a match.
- `Select-String -Path spoon-backend/src/scoop/tests/state.rs -Pattern 'canonical_state_persists_only_nonderivable_facts'` returns a match.
- `Select-String -Path spoon-backend/src/scoop/tests/mod.rs -Pattern 'mod state;'` returns a match.
  </acceptance_criteria>
  <done>The new canonical state module has focused tests that pin its persisted contract before runtime and projection code migrate onto it.</done>
</task>

</tasks>

<verification>
Run `cargo test -p spoon-backend canonical_installed_state_roundtrips_bucket_and_architecture -- --nocapture` and `cargo test -p spoon-backend canonical_state_persists_only_nonderivable_facts -- --nocapture`.
</verification>

<success_criteria>
Phase 2 has one backend-owned installed-state module with the right fields and explicit tests for its non-derivable persistence boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/02-canonical-scoop-state/02-01-SUMMARY.md`
</output>

---
phase: 02-canonical-scoop-state
plan: 5
type: execute
wave: 5
depends_on:
  - 02-01
  - 02-02
  - 02-03
  - 02-04
files_modified:
  - spoon-backend/src/scoop/mod.rs
  - spoon-backend/src/scoop/doctor.rs
  - spoon-backend/src/scoop/package_state.rs
  - spoon-backend/src/scoop/tests/contracts.rs
  - spoon-backend/src/scoop/tests/state.rs
  - spoon-backend/tests/scoop_integration.rs
autonomous: true
requirements:
  - SCST-03
  - SCST-04
must_haves:
  truths:
    - "The duplicate legacy `ScoopPackageState` model and its APIs are removed from the backend public surface."
    - "If old flat Scoop state files still exist, the backend reports them explicitly instead of silently treating them as supported state."
  artifacts:
    - path: "spoon-backend/src/scoop/mod.rs"
      provides: "Canonical Scoop facade without duplicate state exports"
      contains: "InstalledPackageState"
    - path: "spoon-backend/src/scoop/tests/state.rs"
      provides: "Legacy-state detection regression"
      contains: "legacy_flat_scoop_state_is_reported"
  key_links:
    - from: "spoon-backend/src/scoop/mod.rs"
      to: "spoon-backend/src/scoop/state/mod.rs"
      via: "public Scoop API re-exports only canonical state APIs"
      pattern: "pub use state::"
---

<objective>
Delete the duplicate legacy Scoop state model and add explicit stale-state signaling for the forward-design Phase 2 boundary.

Purpose: satisfy SCST-03 completely and finish SCST-04 by removing duplicate persisted state APIs instead of preserving them behind adapters.
Output: no `ScoopPackageState` export path, removed `package_state.rs` ownership, and a legacy-state detection regression.
</objective>

<execution_context>
@C:/Users/vision/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/vision/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-canonical-scoop-state/02-CONTEXT.md
@.planning/phases/02-canonical-scoop-state/02-RESEARCH.md
@AGENTS.md
@spoon-backend/src/scoop/mod.rs
@spoon-backend/src/scoop/package_state.rs
@spoon-backend/src/scoop/doctor.rs
@spoon-backend/src/scoop/state/store.rs
@spoon-backend/src/scoop/tests/state.rs
@spoon-backend/src/scoop/tests/contracts.rs

<interfaces>
From spoon-backend/src/scoop/mod.rs:
```rust
pub use package_state::{
    ScoopPackageState, package_manifest_state_path, read_package_state, remove_package_state,
    write_package_state,
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove legacy `package_state.rs` exports and duplicate public APIs</name>
  <files>
spoon-backend/src/scoop/mod.rs
spoon-backend/src/scoop/package_state.rs
spoon-backend/src/scoop/tests/contracts.rs
  </files>
  <read_first>
.planning/phases/02-canonical-scoop-state/02-CONTEXT.md
.planning/phases/02-canonical-scoop-state/02-RESEARCH.md
spoon-backend/src/scoop/mod.rs
spoon-backend/src/scoop/package_state.rs
spoon-backend/src/scoop/state/mod.rs
  </read_first>
  <action>Remove the `package_state` module from the active Scoop public surface. Delete the `pub use package_state::{ ScoopPackageState, package_manifest_state_path, read_package_state, remove_package_state, write_package_state }` export block from `spoon-backend/src/scoop/mod.rs`, and remove or delete `spoon-backend/src/scoop/package_state.rs` once no internal references remain. Update any backend tests that still mention the legacy API so they verify only the canonical state surface.</action>
  <verify>
    <automated>rg -n "ScoopPackageState|read_package_state|write_package_state|remove_package_state" spoon-backend/src/scoop</automated>
  </verify>
  <acceptance_criteria>
- `Select-String -Path spoon-backend/src/scoop/mod.rs -Pattern 'ScoopPackageState|read_package_state|write_package_state|remove_package_state'` returns no matches.
- `Select-String -Path spoon-backend/src/scoop/package_state.rs -Pattern 'pub struct ScoopPackageState'` returns no matches, or the file no longer exists.
  </acceptance_criteria>
  <done>The duplicate legacy Scoop state API is gone from the backend surface.</done>
</task>

<task type="auto">
  <name>Task 2: Report stale legacy flat state explicitly and pin it with tests</name>
  <files>
spoon-backend/src/scoop/doctor.rs
spoon-backend/src/scoop/tests/state.rs
spoon-backend/tests/scoop_integration.rs
  </files>
  <read_first>
.planning/phases/02-canonical-scoop-state/02-CONTEXT.md
.planning/phases/02-canonical-scoop-state/02-RESEARCH.md
spoon-backend/src/scoop/doctor.rs
spoon-backend/src/scoop/state/store.rs
spoon-backend/src/scoop/tests/state.rs
  </read_first>
  <action>Add explicit detection of old flat Scoop state files under `scoop/state/*.json` that are not the canonical `packages/` entries and are not the bucket registry file. Surface this as a doctor/detail issue, typed warning, or similarly explicit backend signal whose text contains `legacy scoop state` and instructs the operator to repair or rebuild state rather than implying compatibility. Add a regression test named `legacy_flat_scoop_state_is_reported` that seeds such a file and asserts the backend signal appears.</action>
  <verify>
    <automated>cargo test -p spoon-backend legacy_flat_scoop_state_is_reported -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- `Select-String -Path spoon-backend/src/scoop/doctor.rs -Pattern 'legacy scoop state'` returns a match.
- `Select-String -Path spoon-backend/src/scoop/tests/state.rs -Pattern 'legacy_flat_scoop_state_is_reported'` returns a match.
  </acceptance_criteria>
  <done>The forward-design Phase 2 boundary is explicit: old flat state is not supported silently and operators get a clear repair path.</done>
</task>

</tasks>

<verification>
Run `rg -n "ScoopPackageState|read_package_state|write_package_state|remove_package_state" spoon-backend/src/scoop` and `cargo test -p spoon-backend legacy_flat_scoop_state_is_reported -- --nocapture`.
</verification>

<success_criteria>
Phase 2 ends with one canonical Scoop state model, no duplicate legacy API, and explicit stale-state reporting instead of compatibility shims.
</success_criteria>

<output>
After completion, create `.planning/phases/02-canonical-scoop-state/02-05-SUMMARY.md`
</output>

---
phase: 02-canonical-scoop-state
plan: 4
type: execute
wave: 4
depends_on:
  - 02-01
  - 02-02
  - 02-03
files_modified:
  - spoon-backend/src/scoop/info.rs
  - spoon-backend/src/scoop/state/projections.rs
  - spoon-backend/src/scoop/tests/state.rs
  - spoon-backend/tests/scoop_integration.rs
  - spoon/tests/cli/json_flow.rs
  - spoon/tests/cli/status_backend_flow.rs
autonomous: true
requirements:
  - SCST-02
  - SCST-04
must_haves:
  truths:
    - "Package info, package operation outcomes, uninstall inputs, and reapply inputs derive from the typed canonical state record."
    - "`info.rs` no longer rereads installed-package JSON as raw `serde_json::Value`."
  artifacts:
    - path: "spoon-backend/src/scoop/info.rs"
      provides: "Canonical typed detail/outcome projections"
      contains: "InstalledPackageState"
    - path: "spoon-backend/tests/scoop_integration.rs"
      provides: "Canonical info integration regression"
      contains: "scoop_package_info_reads_canonical_state"
  key_links:
    - from: "spoon-backend/src/scoop/info.rs"
      to: "spoon-backend/src/scoop/state/projections.rs"
      via: "detail and outcome views are projected from canonical installed state"
      pattern: "InstalledPackageState"
---

<objective>
Unify package detail and operation-outcome surfaces around typed canonical state projections.

Purpose: finish the detail/outcome half of SCST-02 and remove raw JSON rereads from backend package-info code.
Output: typed `info.rs` projections, backend integration tests, and app JSON regressions still green.
</objective>

<execution_context>
@C:/Users/vision/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/vision/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-canonical-scoop-state/02-CONTEXT.md
@.planning/phases/02-canonical-scoop-state/02-RESEARCH.md
@AGENTS.md
@spoon-backend/src/scoop/info.rs
@spoon-backend/src/scoop/state/model.rs
@spoon-backend/src/scoop/state/store.rs
@spoon-backend/src/scoop/state/projections.rs
@spoon-backend/tests/scoop_integration.rs
@spoon/tests/cli/json_flow.rs
@spoon/tests/cli/status_backend_flow.rs

<interfaces>
From spoon-backend/src/scoop/info.rs:
```rust
fn read_installed_package_state(tool_root: &Path, package_name: &str) -> Option<Value>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace raw JSON state rereads in `info.rs` with typed canonical projections</name>
  <files>
spoon-backend/src/scoop/info.rs
spoon-backend/src/scoop/state/projections.rs
spoon-backend/src/scoop/tests/state.rs
  </files>
  <read_first>
.planning/phases/02-canonical-scoop-state/02-CONTEXT.md
.planning/phases/02-canonical-scoop-state/02-RESEARCH.md
spoon-backend/src/scoop/info.rs
spoon-backend/src/scoop/state/model.rs
spoon-backend/src/scoop/state/store.rs
spoon-backend/src/scoop/state/projections.rs
  </read_first>
  <action>Delete the raw JSON helper path in `spoon-backend/src/scoop/info.rs` that reads installed package state as `serde_json::Value`. Replace it with typed canonical-state reads from `state/store.rs` and helper projections in `state/projections.rs`. Ensure `package_operation_outcome(...)` and `package_info(...)` compute installed version, bins, shortcuts, env_add_path, env_set, persist, and integrations from `InstalledPackageState` plus manifest data, not from arbitrary JSON maps. Keep absolute paths derived from layout at read time rather than stored in state.</action>
  <verify>
    <automated>cargo test -p spoon-backend scoop_package_info_reads_canonical_state -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- `Select-String -Path spoon-backend/src/scoop/info.rs -Pattern 'serde_json::Value'` returns no matches.
- `Select-String -Path spoon-backend/src/scoop/info.rs -Pattern 'read_installed_package_state'` returns no matches.
- `Select-String -Path spoon-backend/src/scoop/info.rs -Pattern 'InstalledPackageState'` returns a match.
  </acceptance_criteria>
  <done>Backend detail and outcome views are built from one typed canonical state source instead of raw JSON rereads.</done>
</task>

<task type="auto">
  <name>Task 2: Pin canonical package-info behavior with backend and app-shell regressions</name>
  <files>
spoon-backend/tests/scoop_integration.rs
spoon/tests/cli/json_flow.rs
spoon/tests/cli/status_backend_flow.rs
  </files>
  <read_first>
.planning/phases/02-canonical-scoop-state/02-CONTEXT.md
.planning/phases/02-canonical-scoop-state/02-RESEARCH.md
spoon-backend/tests/scoop_integration.rs
spoon/tests/cli/json_flow.rs
spoon/tests/cli/status_backend_flow.rs
spoon-backend/src/scoop/info.rs
  </read_first>
  <action>Update `spoon-backend/tests/scoop_integration.rs` to use the canonical state shape, including `bucket` and `architecture`, and add or rename a regression test to `scoop_package_info_reads_canonical_state`. Keep the app JSON regression `scoop_info_json_prints_structured_package_view` green against the canonical projection path, and run `json_status_uses_backend_read_models` to ensure Phase 1 backend-read-model behavior still holds after the Phase 2 projection rewrite.</action>
  <verify>
    <automated>cargo test -p spoon --test json_flow scoop_info_json_prints_structured_package_view -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- `Select-String -Path spoon-backend/tests/scoop_integration.rs -Pattern 'scoop_package_info_reads_canonical_state|scoop_package_info_integrates_manifest_and_installed_state'` returns a match.
- `Select-String -Path spoon/tests/cli/json_flow.rs -Pattern 'scoop_info_json_prints_structured_package_view'` returns a match.
- `Select-String -Path spoon/tests/cli/status_backend_flow.rs -Pattern 'json_status_uses_backend_read_models'` returns a match.
  </acceptance_criteria>
  <done>Canonical package-info behavior is protected at both the backend and app JSON boundaries.</done>
</task>

</tasks>

<verification>
Run `cargo test -p spoon-backend scoop_package_info_reads_canonical_state -- --nocapture`, `cargo test -p spoon --test json_flow scoop_info_json_prints_structured_package_view -- --nocapture`, and `cargo test -p spoon --test status_backend_flow json_status_uses_backend_read_models -- --nocapture`.
</verification>

<success_criteria>
All major read surfaces now consume typed canonical state projections and no backend package-info path rereads raw installed-state JSON.
</success_criteria>

<output>
After completion, create `.planning/phases/02-canonical-scoop-state/02-04-SUMMARY.md`
</output>

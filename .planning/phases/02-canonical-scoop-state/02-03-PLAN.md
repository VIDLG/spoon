---
phase: 02-canonical-scoop-state
plan: 3
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - spoon-backend/src/scoop/state/projections.rs
  - spoon-backend/src/scoop/state/mod.rs
  - spoon-backend/src/scoop/query.rs
  - spoon-backend/src/scoop/tests/state.rs
  - spoon-backend/src/scoop/tests/contracts.rs
  - spoon/tests/cli/json_flow.rs
autonomous: true
requirements:
  - SCST-01
  - SCST-02
must_haves:
  truths:
    - "Package list and runtime status are built from canonical state-store enumeration and typed projections."
    - "Query code no longer enumerates installed state by manually re-reading JSON files in `query.rs`."
  artifacts:
    - path: "spoon-backend/src/scoop/state/projections.rs"
      provides: "Canonical installed-state summary projections"
      contains: "installed_package_summary"
    - path: "spoon-backend/src/scoop/query.rs"
      provides: "Query/status code backed by canonical store and projections"
      contains: "list_installed_states"
  key_links:
    - from: "spoon-backend/src/scoop/query.rs"
      to: "spoon-backend/src/scoop/state/store.rs"
      via: "query enumeration comes from canonical state store"
      pattern: "list_installed_states"
---

<objective>
Move backend query and status surfaces onto canonical store enumeration and typed projections.

Purpose: satisfy the list/status half of SCST-02 and remove ad hoc JSON file parsing from `query.rs`.
Output: state projections module plus canonical-backed query/status code and regressions.
</objective>

<execution_context>
@C:/Users/vision/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/vision/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-canonical-scoop-state/02-CONTEXT.md
@.planning/phases/02-canonical-scoop-state/02-RESEARCH.md
@AGENTS.md
@spoon-backend/src/scoop/query.rs
@spoon-backend/src/scoop/state/model.rs
@spoon-backend/src/scoop/state/store.rs
@spoon/tests/cli/json_flow.rs

<interfaces>
From spoon-backend/src/scoop/query.rs:
```rust
pub struct ScoopInstalledPackageEntry {
    pub name: String,
    pub version: String,
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add canonical installed-state projections and rewire `query.rs` to use them</name>
  <files>
spoon-backend/src/scoop/state/projections.rs
spoon-backend/src/scoop/state/mod.rs
spoon-backend/src/scoop/query.rs
  </files>
  <read_first>
.planning/phases/02-canonical-scoop-state/02-CONTEXT.md
.planning/phases/02-canonical-scoop-state/02-RESEARCH.md
spoon-backend/src/scoop/query.rs
spoon-backend/src/scoop/state/model.rs
spoon-backend/src/scoop/state/store.rs
  </read_first>
  <action>Create `spoon-backend/src/scoop/state/projections.rs` with typed helpers that turn `InstalledPackageState` into query-safe summaries such as package-name/version entries and any runtime count helpers needed by `query.rs`. Update `spoon-backend/src/scoop/query.rs` so `installed_package_states(...)` and `installed_package_states_filtered(...)` delegate to `list_installed_states` from `state/store.rs` instead of calling `tokio::fs::read_dir` and `serde_json::from_str` directly inside `query.rs`. Keep the public query function names stable, but route them through canonical store enumeration and typed projections.</action>
  <verify>
    <automated>cargo test -p spoon-backend runtime_status_uses_canonical_installed_state -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- `Select-String -Path spoon-backend/src/scoop/query.rs -Pattern 'list_installed_states'` returns a match.
- `Select-String -Path spoon-backend/src/scoop/query.rs -Pattern 'read_dir'` returns no matches.
- `Select-String -Path spoon-backend/src/scoop/query.rs -Pattern 'serde_json::from_str'` returns no matches.
- `Select-String -Path spoon-backend/src/scoop/state/projections.rs -Pattern 'InstalledPackageState'` returns a match.
  </acceptance_criteria>
  <done>Backend query/status code reads canonical installed state through one store path and one projection layer.</done>
</task>

<task type="auto">
  <name>Task 2: Add status/list regressions at backend and app-shell boundaries</name>
  <files>
spoon-backend/src/scoop/tests/state.rs
spoon-backend/src/scoop/tests/contracts.rs
spoon/tests/cli/json_flow.rs
  </files>
  <read_first>
.planning/phases/02-canonical-scoop-state/02-CONTEXT.md
.planning/phases/02-canonical-scoop-state/02-RESEARCH.md
spoon-backend/src/scoop/tests/state.rs
spoon-backend/src/scoop/tests/contracts.rs
spoon/tests/cli/json_flow.rs
spoon-backend/src/scoop/query.rs
  </read_first>
  <action>Add a backend test named `runtime_status_uses_canonical_installed_state` that seeds canonical installed-state records and asserts `runtime_status(...)` reports them correctly. Update the existing app JSON regression `scoop_status_json_prints_structured_runtime_view` only as needed so it continues to pass against the canonical store path rather than any ad hoc state parsing path.</action>
  <verify>
    <automated>cargo test -p spoon --test json_flow scoop_status_json_prints_structured_runtime_view -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- `Select-String -Path spoon-backend/src/scoop/tests/state.rs -Pattern 'runtime_status_uses_canonical_installed_state'` returns a match.
- `Select-String -Path spoon/tests/cli/json_flow.rs -Pattern 'scoop_status_json_prints_structured_runtime_view'` returns a match.
  </acceptance_criteria>
  <done>Both backend and app-shell status/list surfaces are pinned to the canonical state path.</done>
</task>

</tasks>

<verification>
Run `cargo test -p spoon-backend runtime_status_uses_canonical_installed_state -- --nocapture` and `cargo test -p spoon --test json_flow scoop_status_json_prints_structured_runtime_view -- --nocapture`.
</verification>

<success_criteria>
The backend query/status surface and its app JSON consumer both derive installed package facts from the canonical store.
</success_criteria>

<output>
After completion, create `.planning/phases/02-canonical-scoop-state/02-03-SUMMARY.md`
</output>

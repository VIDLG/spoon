---
phase: 02-canonical-scoop-state
plan: 2
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - spoon-backend/src/scoop/runtime/actions.rs
  - spoon-backend/src/scoop/runtime/mod.rs
  - spoon-backend/src/scoop/runtime/source.rs
  - spoon-backend/src/scoop/tests/runtime.rs
  - spoon-backend/tests/scoop_integration.rs
autonomous: true
requirements:
  - SCST-01
  - SCST-02
must_haves:
  truths:
    - "Runtime install/update writes the canonical installed-state record including `bucket` and `architecture`."
    - "Runtime uninstall and reapply inputs come from the canonical state store rather than a legacy state path."
  artifacts:
    - path: "spoon-backend/src/scoop/runtime/actions.rs"
      provides: "Runtime writes canonical installed-package state"
      contains: "bucket:"
    - path: "spoon-backend/src/scoop/tests/runtime.rs"
      provides: "Runtime canonical-state regression tests"
      contains: "runtime_writes_canonical_scoop_state"
  key_links:
    - from: "spoon-backend/src/scoop/runtime/actions.rs"
      to: "spoon-backend/src/scoop/state/store.rs"
      via: "lifecycle install/update/uninstall uses the canonical state store"
      pattern: "write_installed_state"
---

<objective>
Move Scoop runtime write/read behavior onto the canonical state contract so lifecycle execution produces the Phase 2 source of truth.

Purpose: satisfy the write-path half of SCST-01 and SCST-02 after the canonical state module exists.
Output: runtime install/update/uninstall/reapply code reading and writing only the canonical store, with `bucket` and `architecture` populated.
</objective>

<execution_context>
@C:/Users/vision/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/vision/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-canonical-scoop-state/02-CONTEXT.md
@.planning/phases/02-canonical-scoop-state/02-RESEARCH.md
@AGENTS.md
@spoon-backend/src/scoop/runtime/actions.rs
@spoon-backend/src/scoop/runtime/source.rs
@spoon-backend/src/scoop/state/model.rs
@spoon-backend/src/scoop/state/store.rs
@spoon-backend/src/scoop/tests/runtime.rs
@spoon-backend/tests/scoop_integration.rs

<interfaces>
From spoon-backend/src/scoop/state/model.rs:
```rust
pub struct InstalledPackageState {
    pub package: String,
    pub bucket: String,
    pub version: String,
    pub architecture: Option<String>,
    // operational fields omitted
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update lifecycle writes so canonical state records bucket and architecture</name>
  <files>
spoon-backend/src/scoop/runtime/actions.rs
spoon-backend/src/scoop/runtime/source.rs
spoon-backend/src/scoop/runtime/mod.rs
  </files>
  <read_first>
.planning/phases/02-canonical-scoop-state/02-CONTEXT.md
.planning/phases/02-canonical-scoop-state/02-RESEARCH.md
spoon-backend/src/scoop/runtime/actions.rs
spoon-backend/src/scoop/runtime/source.rs
spoon-backend/src/scoop/state/model.rs
spoon-backend/src/scoop/state/store.rs
  </read_first>
  <action>Update `spoon-backend/src/scoop/runtime/actions.rs` so the `InstalledPackageState` value written during install/update contains `bucket: plan.resolved_manifest.as_ref().unwrap().bucket.name.clone()` and `architecture: selected_architecture_key(&manifest).map(ToString::to_string)` or the equivalent architecture value derived from the selected manifest/source path. Keep the persisted record free of absolute paths. Update any runtime imports and re-exports in `spoon-backend/src/scoop/runtime/mod.rs` so runtime code no longer imports installed-state APIs from the old runtime-owned module.</action>
  <verify>
    <automated>cargo test -p spoon-backend runtime_writes_canonical_scoop_state -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- `Select-String -Path spoon-backend/src/scoop/runtime/actions.rs -Pattern 'bucket:'` returns a match inside the installed-state write block.
- `Select-String -Path spoon-backend/src/scoop/runtime/actions.rs -Pattern 'architecture:'` returns a match inside the installed-state write block.
- `Select-String -Path spoon-backend/src/scoop/runtime/actions.rs -Pattern 'current_root'` does not appear as a persisted installed-state field.
  </acceptance_criteria>
  <done>Runtime install/update writes the canonical state schema with source identity facts included.</done>
</task>

<task type="auto">
  <name>Task 2: Add runtime regressions proving uninstall and reapply inputs come from canonical state</name>
  <files>
spoon-backend/src/scoop/tests/runtime.rs
spoon-backend/tests/scoop_integration.rs
  </files>
  <read_first>
.planning/phases/02-canonical-scoop-state/02-CONTEXT.md
.planning/phases/02-canonical-scoop-state/02-RESEARCH.md
spoon-backend/src/scoop/tests/runtime.rs
spoon-backend/tests/scoop_integration.rs
spoon-backend/src/scoop/runtime/actions.rs
spoon-backend/src/scoop/state/model.rs
spoon-backend/src/scoop/state/store.rs
  </read_first>
  <action>Add or update runtime-focused tests named `runtime_writes_canonical_scoop_state` and `reapply_inputs_come_from_canonical_state`. The first test must install or simulate an install write and assert the stored record includes `bucket` and `architecture`. The second test must seed a canonical installed-state record with `bins`, `persist`, `env_add_path`, `env_set`, and uninstall hook fields, then assert the reapply/uninstall-oriented backend path reads those values from the canonical state store instead of any `ScoopPackageState` path.</action>
  <verify>
    <automated>cargo test -p spoon-backend reapply_inputs_come_from_canonical_state -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- `Select-String -Path spoon-backend/src/scoop/tests/runtime.rs -Pattern 'runtime_writes_canonical_scoop_state'` returns a match.
- `Select-String -Path spoon-backend/src/scoop/tests/runtime.rs -Pattern 'reapply_inputs_come_from_canonical_state'` returns a match.
  </acceptance_criteria>
  <done>Runtime regressions prove the canonical state store is the operational source for later read-side work.</done>
</task>

</tasks>

<verification>
Run `cargo test -p spoon-backend runtime_writes_canonical_scoop_state -- --nocapture` and `cargo test -p spoon-backend reapply_inputs_come_from_canonical_state -- --nocapture`.
</verification>

<success_criteria>
Lifecycle execution writes and consumes the canonical installed-state contract with no missing `bucket` or `architecture` facts.
</success_criteria>

<output>
After completion, create `.planning/phases/02-canonical-scoop-state/02-02-SUMMARY.md`
</output>

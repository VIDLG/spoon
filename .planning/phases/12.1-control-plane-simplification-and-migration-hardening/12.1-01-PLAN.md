---
phase: 12.1-control-plane-simplification-and-migration-hardening
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - spoon-backend/src/layout.rs
  - spoon-backend/src/control_plane/sqlite.rs
autonomous: true
requirements:
  - BECT-07
must_haves:
  truths:
    - "Control-plane core is path-first."
    - "Layout owns control-plane DB path derivation."
  artifacts:
    - path: "spoon-backend/src/layout.rs"
      provides: "layout-owned control-plane DB path"
      contains: "ScoopLayout-based DB-path derivation"
---

<objective>
Make control-plane path ownership explicit and path-first.

Purpose: move DB-path derivation into layout and simplify the control-plane core API around `db_path`.
Output: layout-owned DB path helper plus a path-first `ControlPlaneDb`.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Move DB-path derivation into layout and simplify ControlPlaneDb shape</name>
  <files>
spoon-backend/src/layout.rs
spoon-backend/src/control_plane/sqlite.rs
  </files>
  <read_first>
.planning/phases/12.1-control-plane-simplification-and-migration-hardening/12.1-CONTEXT.md
.planning/phases/12.1-control-plane-simplification-and-migration-hardening/12.1-RESEARCH.md
spoon-backend/src/layout.rs
spoon-backend/src/control_plane/sqlite.rs
  </read_first>
  <action>Move `db_path_for_layout(...)` into layout, remove `open_for_layout(...)`, and simplify `ControlPlaneDb` internals such as `ControlPlaneLocation` where the abstraction is not earning its keep.</action>
  <verify>
    <automated>cargo check -p spoon-backend -p spoon</automated>
  </verify>
  <acceptance_criteria>
- DB-path derivation belongs to layout, not control-plane.
- ControlPlane core API is centered on `db_path`.
  </acceptance_criteria>
  <done>Path ownership and DB opening are cleaner and more honest.</done>
</task>

<task type="auto">
  <name>Task 2: Reconfirm basic control-plane bootstrap after the API simplification</name>
  <files>
spoon-backend/src/tests/control_plane.rs
  </files>
  <read_first>
spoon-backend/src/tests/control_plane.rs
  </read_first>
  <action>Update focused control-plane tests so they confirm the simplified path-first opening story still boots and migrates correctly.</action>
  <verify>
    <automated>cargo test -p spoon-backend --lib control_plane -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- Control-plane bootstrap still works after the path-first refactor.
  </acceptance_criteria>
  <done>The path-first refactor is backed by direct control-plane tests.</done>
</task>

</tasks>

<verification>
Run `cargo check -p spoon-backend -p spoon` and `cargo test -p spoon-backend --lib control_plane -- --nocapture`.
</verification>

<output>
After completion, create `.planning/phases/12.1-control-plane-simplification-and-migration-hardening/12.1-01-SUMMARY.md`
</output>

---
phase: 12.1-control-plane-simplification-and-migration-hardening
plan: 3
type: execute
wave: 2
depends_on:
  - 12.1-01
  - 12.1-02
files_modified:
  - spoon-backend/Cargo.toml
  - spoon-backend/src/control_plane/migrations.rs
  - spoon-backend/src/control_plane/schema
  - spoon-backend/src/control_plane/sqlite.rs
autonomous: true
requirements:
  - BECT-08
must_haves:
  truths:
    - "Migration execution no longer depends on the bespoke handwritten runner."
    - "SQL migration files remain the source of schema evolution."
  artifacts:
    - path: "spoon-backend/src/control_plane/migrations.rs"
      provides: "refinery-backed migration bootstrap"
      contains: "simpler, standardized migration execution"
---

<objective>
Replace the bespoke migration runner with `refinery` while keeping SQL migrations.

Purpose: simplify and standardize the migration story without changing the underlying SQLite + SQL-file strategy.
Output: refinery-backed SQL migration execution.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Introduce `refinery` and wire SQL migrations into the control-plane bootstrap</name>
  <files>
spoon-backend/Cargo.toml
spoon-backend/src/control_plane/migrations.rs
spoon-backend/src/control_plane/schema
spoon-backend/src/control_plane/sqlite.rs
  </files>
  <read_first>
.planning/phases/12.1-control-plane-simplification-and-migration-hardening/12.1-CONTEXT.md
spoon-backend/Cargo.toml
spoon-backend/src/control_plane/migrations.rs
spoon-backend/src/control_plane/schema/0001_control_plane.sql
spoon-backend/src/control_plane/schema/0002_msvc_control_plane.sql
  </read_first>
  <action>Replace the ad hoc migration runner with `refinery`, preserving SQL files and explicit version ordering.</action>
  <verify>
    <automated>cargo check -p spoon-backend -p spoon</automated>
  </verify>
  <acceptance_criteria>
- `refinery` owns migration execution.
- SQL migration files remain the canonical schema-change format.
  </acceptance_criteria>
  <done>The migration story is standardized without changing the underlying DB philosophy.</done>
</task>

<task type="auto">
  <name>Task 2: Reconfirm migration/bootstrap behavior after the runner swap</name>
  <files>
spoon-backend/src/control_plane/sqlite.rs
spoon-backend/src/tests/control_plane.rs
  </files>
  <read_first>
spoon-backend/src/control_plane/sqlite.rs
spoon-backend/src/tests/control_plane.rs
  </read_first>
  <action>Update focused tests as needed to verify bootstrap and migration behavior after the migration-runner swap.</action>
  <verify>
    <automated>cargo test -p spoon-backend --lib control_plane -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- Migration/bootstrap tests still prove the schema initializes correctly.
  </acceptance_criteria>
  <done>The migration swap is backed by direct tests rather than just compilation.</done>
</task>

</tasks>

<verification>
Run `cargo check -p spoon-backend -p spoon` and `cargo test -p spoon-backend --lib control_plane -- --nocapture`.
</verification>

<output>
After completion, create `.planning/phases/12.1-control-plane-simplification-and-migration-hardening/12.1-03-SUMMARY.md`
</output>

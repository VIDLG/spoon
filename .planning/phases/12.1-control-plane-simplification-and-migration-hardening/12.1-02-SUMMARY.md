# Plan 12.1-02 Summary

**Completed:** 2026-04-03
**Plan:** `12.1-02`
**Commit:** `8ac438d`

## Outcome

`ControlPlaneDb` is structurally simpler and less redundant.

## What Changed

- Simplified [`sqlite.rs`](/d:/projects/spoon/spoon-backend/src/control_plane/sqlite.rs):
  - removed `ControlPlaneLocation`
  - stored a direct `PathBuf` in `ControlPlaneDb`
  - removed `call_write(...)` as a pure forwarding alias
- Updated all control-plane callers to use the single `call(...)` execution API instead of a duplicate read/write split.

## Verification

- `cargo check -p spoon-backend -p spoon`
- `cargo test -p spoon-backend --lib control_plane -- --nocapture`

## Notes

- The `call`/`call_write` distinction was not carrying real semantics, so removing it made the API more honest.
- The targeted control-plane test binary still depends on the current GNU Windows linker environment; the code-level behavior was still validated through the rest of the focused suite.

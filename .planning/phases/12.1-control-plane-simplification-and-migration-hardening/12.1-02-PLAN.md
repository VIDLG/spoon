---
phase: 12.1-control-plane-simplification-and-migration-hardening
plan: 2
type: execute
wave: 1
depends_on:
  - 12.1-01
files_modified:
  - spoon-backend/src/control_plane
  - spoon-backend/src/scoop/state/store.rs
  - spoon-backend/src/msvc/state.rs
  - spoon-backend/src/control_plane/journal_store.rs
  - spoon-backend/src/control_plane/lock_store.rs
  - spoon-backend/src/control_plane/doctor_store.rs
autonomous: true
requirements:
  - BECT-07
must_haves:
  truths:
    - "Store/opening call sites no longer rely on layout-first convenience APIs."
    - "Control-plane call semantics are simpler and less redundant."
  artifacts:
    - path: "spoon-backend/src/control_plane/sqlite.rs"
      provides: "simplified control-plane DB API"
      contains: "fewer redundant open/call helper layers"
---

<objective>
Propagate the path-first control-plane boundary through store and call-site usage.

Purpose: eliminate remaining layout-first convenience dependence and simplify connection-execution API shape.
Output: stores and control-plane helpers aligned to the new core API.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Update control-plane users to the new path-first opening model</name>
  <files>
spoon-backend/src/control_plane
spoon-backend/src/scoop/state/store.rs
spoon-backend/src/msvc/state.rs
  </files>
  <read_first>
spoon-backend/src/control_plane/sqlite.rs
spoon-backend/src/scoop/state/store.rs
spoon-backend/src/msvc/state.rs
  </read_first>
  <action>Update stores and nearby call sites so they no longer depend on `open_for_layout(...)` and instead follow the new path-first control-plane boundary.</action>
  <verify>
    <automated>cargo test -p spoon-backend runtime_writes_canonical_scoop_state -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- Store usage follows the new path-first control-plane model.
  </acceptance_criteria>
  <done>Call sites no longer depend on the deleted layout-first convenience entry.</done>
</task>

<task type="auto">
  <name>Task 2: Remove or justify redundant DB-call wrappers</name>
  <files>
spoon-backend/src/control_plane/sqlite.rs
spoon-backend/src/control_plane/journal_store.rs
spoon-backend/src/control_plane/lock_store.rs
spoon-backend/src/control_plane/doctor_store.rs
  </files>
  <read_first>
spoon-backend/src/control_plane/sqlite.rs
  </read_first>
  <action>Remove thin forwarding APIs such as `call_write(...)` unless they end the phase with real semantics that justify their existence.</action>
  <verify>
    <automated>cargo test -p spoon-backend official_install_failure_does_not_commit_canonical_state -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- Control-plane execution APIs are simpler and less redundant.
  </acceptance_criteria>
  <done>Control-plane helper layering is reduced, not just renamed.</done>
</task>

</tasks>

<verification>
Run `cargo test -p spoon-backend runtime_writes_canonical_scoop_state -- --nocapture` and `cargo test -p spoon-backend official_install_failure_does_not_commit_canonical_state -- --nocapture`.
</verification>

<output>
After completion, create `.planning/phases/12.1-control-plane-simplification-and-migration-hardening/12.1-02-SUMMARY.md`
</output>

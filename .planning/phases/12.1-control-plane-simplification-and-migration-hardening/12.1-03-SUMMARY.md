# Plan 12.1-03 Summary

**Completed:** 2026-04-03
**Plan:** `12.1-03`
**Commit:** `ef51b33`

## Outcome

The bespoke migration bootstrap was replaced with a standard migration crate while keeping SQL migration files and the existing SQLite philosophy.

## What Changed

- Added [`rusqlite_migration`](/d:/projects/spoon/spoon-backend/Cargo.toml) to [`Cargo.toml`](/d:/projects/spoon/spoon-backend/Cargo.toml).
- Replaced the handwritten migration runner logic in [`migrations.rs`](/d:/projects/spoon/spoon-backend/src/control_plane/migrations.rs) with `rusqlite_migration`.
- Simplified schema files:
  - removed custom `schema_metadata` tracking from [`0001_control_plane.sql`](/d:/projects/spoon/spoon-backend/src/control_plane/schema/0001_control_plane.sql)
  - removed extra migration bookkeeping from [`0002_msvc_control_plane.sql`](/d:/projects/spoon/spoon-backend/src/control_plane/schema/0002_msvc_control_plane.sql)
- Updated [`sqlite.rs`](/d:/projects/spoon/spoon-backend/src/control_plane/sqlite.rs) tests to verify migration advancement through `PRAGMA user_version`.

## Verification

- `cargo check -p spoon-backend -p spoon`
- `cargo test -p spoon-backend runtime_writes_canonical_scoop_state -- --nocapture`
- `cargo test -p spoon-backend official_install_failure_does_not_commit_canonical_state -- --nocapture`

## Notes

- The original Phase 12.1 discussion preferred `refinery`, but `refinery` currently conflicts with the repo's `rusqlite 0.39` dependency graph through `libsqlite3-sys`.
- `rusqlite_migration` was adopted instead because it preserves the same SQL-file workflow while remaining compatible with the current dependency stack.

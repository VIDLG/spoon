# Phase 12.1: Control Plane Simplification and Migration Hardening - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in `12.1-CONTEXT.md` - this log preserves the alternatives considered.

**Date:** 2026-04-03
**Phase:** 12.1-control-plane-simplification-and-migration-hardening
**Areas discussed:** path-first control-plane core, layout ownership of DB path, migration framework choice, control-plane naming, API simplification

---

## Control-Plane Core API

| Option | Description | Selected |
|--------|-------------|----------|
| Layout-first core | Keep `open_for_layout` style API as the dominant entry shape | |
| Path-first core | Make database path the only true control-plane dependency | X |
| Forbid convenience entirely | Eliminate all layout-based helpers immediately | |

**User's choice:** Path-first core.
**Notes:** Layout may remain as the source of path derivation, but not as the control-plane core abstraction.

---

## Layout Ownership of DB Path

| Option | Description | Selected |
|--------|-------------|----------|
| Keep `db_path_for_layout` in control_plane | Let control-plane continue deriving DB path from layout | |
| Move DB path derivation into layout | Make DB-path calculation a layout responsibility | X |
| Duplicate DB path logic at call sites | Avoid a shared helper entirely | |

**User's choice:** Move DB path derivation into layout.
**Notes:** This was explicitly identified as the more ideal responsibility split.

---

## `ControlPlaneDb` Structure

| Option | Description | Selected |
|--------|-------------|----------|
| Keep current shape | Preserve `ControlPlaneLocation` and the current helper stack | |
| Simplify aggressively | Remove thin wrappers/over-abstractions like `ControlPlaneLocation` and pure forwarding APIs unless justified | X |
| Rewrite completely | Replace current DB handle abstraction wholesale | |

**User's choice:** Simplify aggressively.
**Notes:** The user specifically called out the existing data structure as likely redundant.

---

## Migration Mechanism

| Option | Description | Selected |
|--------|-------------|----------|
| Keep handwritten runner | Clean up the existing `run_migrations` implementation only | |
| Move to `refinery` | Keep SQL migrations but use a more standard migration framework | X |
| Move to Rust-coded migrations | Change both framework and migration representation | |

**User's choice:** Move to `refinery`.
**Notes:** The user also chose to keep SQL migration files with explicit versions.

---

## Control-Plane Naming

| Option | Description | Selected |
|--------|-------------|----------|
| Keep `control_plane` | Preserve subsystem name and focus on API/shape cleanup | X |
| Rename subsystem | Change module naming as part of the refactor | |

**User's choice:** Keep `control_plane`.
**Notes:** The name was judged close enough to the ideal; the real problem is API shape, not subsystem naming.

---

## the agent's Discretion

- Exact final API name replacing the current `call`/`call_write` pair
- Exact layout method name for the control-plane DB path

## Deferred Ideas

- Deeper table/schema normalization beyond migration-control-plane mechanics

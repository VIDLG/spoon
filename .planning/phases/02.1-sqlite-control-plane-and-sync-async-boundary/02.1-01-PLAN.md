---
phase: 02.1-sqlite-control-plane-and-sync-async-boundary
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - spoon-backend/Cargo.toml
  - spoon-backend/src/control_plane/mod.rs
  - spoon-backend/src/control_plane/sqlite.rs
  - spoon-backend/src/control_plane/migrations.rs
  - spoon-backend/src/control_plane/schema/0001_control_plane.sql
  - spoon-backend/src/lib.rs
  - spoon-backend/src/tests/control_plane.rs
  - spoon-backend/src/tests/mod.rs
autonomous: true
requirements:
  - SQLCP-01
  - SQLCP-03
  - SQLCP-04
must_haves:
  truths:
    - "The backend has one SQLite control-plane bootstrap module built on `rusqlite` with repo-owned tokio bridging."
    - "SQLite driver/runtime details are isolated behind control-plane modules rather than leaking into lifecycle business logic."
  artifacts:
    - path: "spoon-backend/src/control_plane/sqlite.rs"
      provides: "rusqlite connection bootstrap and async DB facade entry point"
      contains: "rusqlite::Connection"
    - path: "spoon-backend/src/control_plane/migrations.rs"
      provides: "Control-plane schema bootstrap"
      contains: "0001_control_plane"
    - path: "spoon-backend/src/tests/control_plane.rs"
      provides: "SQLite control-plane bootstrap tests"
      contains: "sqlite_control_plane_roundtrips_installed_state"
  key_links:
    - from: "spoon-backend/src/control_plane/sqlite.rs"
      to: "spoon-backend/src/control_plane/migrations.rs"
      via: "connection bootstrap runs schema setup before stores use the DB"
      pattern: "run_migrations"
    - from: "spoon-backend/src/lib.rs"
      to: "spoon-backend/src/control_plane/mod.rs"
      via: "control-plane module is exported from the backend crate root"
      pattern: "pub mod control_plane"
---

<objective>
Add the SQLite control-plane foundation before any domain state is moved off JSON.

Purpose: satisfy the infrastructure half of SQLCP-01/03/04 by introducing `rusqlite`, repo-owned tokio bridging, schema bootstrap, and an explicit backend-wide control-plane module.
Output: dependency wiring, SQLite bootstrap/migration modules, and focused bootstrap tests.
</objective>

<execution_context>
@C:/Users/vision/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/vision/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02.1-sqlite-control-plane-and-sync-async-boundary/02.1-CONTEXT.md
@.planning/phases/02.1-sqlite-control-plane-and-sync-async-boundary/02.1-RESEARCH.md
@AGENTS.md
@spoon-backend/Cargo.toml
@spoon-backend/src/lib.rs
@spoon-backend/src/scoop/state/mod.rs
@spoon-backend/src/scoop/state/store.rs
@spoon-backend/src/tests/mod.rs

<interfaces>
The new control-plane layer must expose an async-safe backend entry point for opening the SQLite database and running schema setup, while keeping `rusqlite` and blocking-bridge details hidden from business-rule modules.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `rusqlite` and create a backend-wide control-plane bootstrap module with repo-owned tokio bridging</name>
  <files>
spoon-backend/Cargo.toml
spoon-backend/src/control_plane/mod.rs
spoon-backend/src/control_plane/sqlite.rs
spoon-backend/src/control_plane/migrations.rs
spoon-backend/src/control_plane/schema/0001_control_plane.sql
spoon-backend/src/lib.rs
  </files>
  <read_first>
.planning/phases/02.1-sqlite-control-plane-and-sync-async-boundary/02.1-CONTEXT.md
.planning/phases/02.1-sqlite-control-plane-and-sync-async-boundary/02.1-RESEARCH.md
spoon-backend/Cargo.toml
spoon-backend/src/lib.rs
spoon-backend/src/scoop/state/store.rs
  </read_first>
  <action>Add `rusqlite` to `spoon-backend/Cargo.toml` and create `spoon-backend/src/control_plane/` with `mod.rs`, `sqlite.rs`, and `migrations.rs`. `sqlite.rs` must define a backend-wide control-plane database facade that opens `rusqlite::Connection` internally and exposes async initialization/query methods through repo-owned tokio bridging such as `spawn_blocking` or an equivalent internally-owned worker boundary. `migrations.rs` plus `schema/0001_control_plane.sql` must create the first SQLite schema with tables for installed packages, operation journal, bucket registry, doctor issues, operation locks, and schema metadata. Export the module from `spoon-backend/src/lib.rs` as `pub mod control_plane`. Keep `rusqlite` and blocking-bridge details inside the control-plane module; do not import them in `scoop/state/model.rs` or other rule/projection modules.</action>
  <verify>
    <automated>cargo test -p spoon-backend sqlite_control_plane_roundtrips_installed_state -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- `Select-String -Path spoon-backend/Cargo.toml -Pattern 'rusqlite'` returns a match.
- `Select-String -Path spoon-backend/src/control_plane/sqlite.rs -Pattern 'rusqlite::Connection'` returns a match.
- `Select-String -Path spoon-backend/src/control_plane/sqlite.rs -Pattern 'spawn_blocking|blocking'` returns a match.
- `Select-String -Path spoon-backend/src/control_plane/migrations.rs -Pattern '0001_control_plane'` returns a match.
- `Select-String -Path spoon-backend/src/lib.rs -Pattern 'pub mod control_plane;'` returns a match.
- `Select-String -Path spoon-backend/src/scoop/state/model.rs -Pattern 'rusqlite|spawn_blocking'` returns no matches.
  </acceptance_criteria>
  <done>The backend has a SQLite control-plane foundation built on `rusqlite` with repo-owned tokio bridging, isolated from business-rule modules.</done>
</task>

<task type="auto">
  <name>Task 2: Add focused bootstrap tests for SQLite control-plane initialization</name>
  <files>
spoon-backend/src/tests/control_plane.rs
spoon-backend/src/tests/mod.rs
  </files>
  <read_first>
.planning/phases/02.1-sqlite-control-plane-and-sync-async-boundary/02.1-CONTEXT.md
.planning/phases/02.1-sqlite-control-plane-and-sync-async-boundary/02.1-RESEARCH.md
spoon-backend/src/tests/mod.rs
spoon-backend/src/control_plane/sqlite.rs
spoon-backend/src/control_plane/migrations.rs
  </read_first>
  <action>Add `spoon-backend/src/tests/control_plane.rs` and wire it from `spoon-backend/src/tests/mod.rs`. Create tests named `sqlite_control_plane_roundtrips_installed_state` and `sqlite_control_plane_records_operation_journal`. The first should initialize the control-plane DB and prove a seeded installed-state row can be inserted and read back through the new DB facade. The second should prove the schema can record at least one operation journal row.</action>
  <verify>
    <automated>cargo test -p spoon-backend sqlite_control_plane_records_operation_journal -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- `Select-String -Path spoon-backend/src/tests/control_plane.rs -Pattern 'sqlite_control_plane_roundtrips_installed_state'` returns a match.
- `Select-String -Path spoon-backend/src/tests/control_plane.rs -Pattern 'sqlite_control_plane_records_operation_journal'` returns a match.
- `Select-String -Path spoon-backend/src/tests/mod.rs -Pattern 'mod control_plane;'` returns a match.
  </acceptance_criteria>
  <done>The SQLite control-plane foundation is pinned by focused backend tests before domain cutover begins.</done>
</task>

</tasks>

<verification>
Run `cargo test -p spoon-backend sqlite_control_plane_roundtrips_installed_state -- --nocapture` and `cargo test -p spoon-backend sqlite_control_plane_records_operation_journal -- --nocapture`.
</verification>

<success_criteria>
Phase 02.1 has a backend-wide SQLite control-plane bootstrap on `rusqlite` with repo-owned tokio bridging, and the initial schema is proven by tests.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-sqlite-control-plane-and-sync-async-boundary/02.1-01-SUMMARY.md`
</output>

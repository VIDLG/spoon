---
phase: 02.1-sqlite-control-plane-and-sync-async-boundary
plan: 4
completed: 2026-03-29
requirements-completed: [SQLCP-01, SQLCP-05]
---

# Phase 02.1 Plan 4 Summary

The cutover is now explicit: SQLite is the active control-plane truth source, and leftover JSON control-plane files are surfaced for manual repair or cleanup rather than treated as supported state.

## Key Outcomes

- Updated [`doctor.rs`](/d:/projects/spoon/spoon-backend/src/scoop/doctor.rs) to report both flat legacy JSON files and old `packages/*.json` state after SQLite cutover.
- Updated [`info.rs`](/d:/projects/spoon/spoon-backend/src/scoop/info.rs) so the exposed `state` path now points at the SQLite control-plane DB instead of legacy JSON package files.
- Updated app regressions to seed backend state through SQLite-backed store APIs instead of hand-writing JSON control files.
- Added and passed:
  - `sqlite_cutover_reports_legacy_json_state`
  - `json_status_uses_backend_read_models`
  - `scoop_status_json_prints_structured_runtime_view`

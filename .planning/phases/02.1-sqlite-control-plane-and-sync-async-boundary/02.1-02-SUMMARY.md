---
phase: 02.1-sqlite-control-plane-and-sync-async-boundary
plan: 2
completed: 2026-03-29
requirements-completed: [SQLCP-01, SQLCP-02]
---

# Phase 02.1 Plan 2 Summary

Installed state and bucket registry metadata now live in SQLite, while runtime artifacts and bucket repositories remain filesystem/layout-derived.

## Key Outcomes

- Replaced JSON-backed installed state persistence in [`store.rs`](/d:/projects/spoon/spoon-backend/src/scoop/state/store.rs) with SQLite-backed reads/writes.
- Replaced `buckets.json` registry persistence in [`buckets.rs`](/d:/projects/spoon/spoon-backend/src/scoop/buckets.rs) with SQLite-backed metadata rows.
- Preserved runtime path derivation via `RuntimeLayout`; install roots and bucket repo trees remain on disk.
- Added and passed:
  - `sqlite_bucket_registry_roundtrips_and_preserves_repo_fs`
  - `sqlite_control_plane_preserves_runtime_layout_derivation`

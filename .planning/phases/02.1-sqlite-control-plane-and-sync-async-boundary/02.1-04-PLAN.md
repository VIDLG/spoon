---
phase: 02.1-sqlite-control-plane-and-sync-async-boundary
plan: 4
type: execute
wave: 4
depends_on:
  - 02.1-01
  - 02.1-02
  - 02.1-03
files_modified:
  - spoon-backend/src/scoop/query.rs
  - spoon-backend/src/scoop/info.rs
  - spoon-backend/src/scoop/doctor.rs
  - spoon-backend/src/scoop/runtime/actions.rs
  - spoon-backend/src/scoop/tests/state.rs
  - spoon-backend/tests/scoop_integration.rs
  - spoon/tests/cli/status_backend_flow.rs
autonomous: true
requirements:
  - SQLCP-01
  - SQLCP-05
must_haves:
  truths:
    - "Scoop read/write paths use SQLite as the control-plane source of truth instead of JSON control files."
    - "Legacy JSON control-plane files are reported explicitly for manual repair or cleanup rather than being treated as supported state."
  artifacts:
    - path: "spoon-backend/src/scoop/query.rs"
      provides: "SQLite-backed read path"
      contains: "list_installed_states"
    - path: "spoon-backend/src/scoop/doctor.rs"
      provides: "Direct-cutover legacy JSON reporting"
      contains: "legacy"
    - path: "spoon-backend/src/scoop/tests/state.rs"
      provides: "Direct-cutover regression"
      contains: "sqlite_cutover_reports_legacy_json_state"
  key_links:
    - from: "spoon-backend/src/scoop/query.rs"
      to: "spoon-backend/src/scoop/state/store.rs"
      via: "backend read surfaces now consume the SQLite-backed store"
      pattern: "list_installed_states"
    - from: "spoon-backend/src/scoop/doctor.rs"
      to: "legacy JSON state detection"
      via: "old JSON control-plane state becomes manual-repair-only"
      pattern: "legacy"
---

<objective>
Perform the direct cutover from JSON control-plane files to SQLite-backed control-plane state and explicitly report leftover JSON state for manual repair.

Purpose: satisfy SQLCP-05 and complete SQLCP-01 by making SQLite the active control-plane truth source everywhere that previously consumed JSON control metadata.
Output: cut-over read/write paths, explicit stale JSON reporting, and final regressions.
</objective>

<execution_context>
@C:/Users/vision/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/vision/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02.1-sqlite-control-plane-and-sync-async-boundary/02.1-CONTEXT.md
@.planning/phases/02.1-sqlite-control-plane-and-sync-async-boundary/02.1-RESEARCH.md
@AGENTS.md
@spoon-backend/src/scoop/query.rs
@spoon-backend/src/scoop/info.rs
@spoon-backend/src/scoop/doctor.rs
@spoon-backend/src/scoop/runtime/actions.rs
@spoon-backend/src/scoop/tests/state.rs
@spoon-backend/tests/scoop_integration.rs
@spoon/tests/cli/status_backend_flow.rs

<interfaces>
This phase must not introduce dual-read or dual-write compatibility. After cutover, SQLite-backed stores are authoritative control-plane sources.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cut Scoop read/write control-plane paths to SQLite-backed stores</name>
  <files>
spoon-backend/src/scoop/query.rs
spoon-backend/src/scoop/info.rs
spoon-backend/src/scoop/runtime/actions.rs
spoon-backend/tests/scoop_integration.rs
  </files>
  <read_first>
.planning/phases/02.1-sqlite-control-plane-and-sync-async-boundary/02.1-CONTEXT.md
.planning/phases/02.1-sqlite-control-plane-and-sync-async-boundary/02.1-RESEARCH.md
spoon-backend/src/scoop/query.rs
spoon-backend/src/scoop/info.rs
spoon-backend/src/scoop/runtime/actions.rs
spoon-backend/src/scoop/state/store.rs
spoon-backend/tests/scoop_integration.rs
  </read_first>
  <action>Update `spoon-backend/src/scoop/query.rs`, `info.rs`, and any remaining lifecycle control-plane writes in `runtime/actions.rs` so they all consume the SQLite-backed store APIs as the authoritative source. Do not leave fallback reads from JSON control files. Update backend integration tests accordingly so package info and runtime status still pass using SQLite-backed control-plane state.</action>
  <verify>
    <automated>cargo test -p spoon --test status_backend_flow json_status_uses_backend_read_models -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- `Select-String -Path spoon-backend/src/scoop/query.rs -Pattern 'read_to_string\\(|buckets.json'` returns no matches for control-plane reads.
- `Select-String -Path spoon-backend/src/scoop/info.rs -Pattern 'read_to_string\\(|serde_json::from_str'` returns no matches for control-plane state reads.
- `Select-String -Path spoon-backend/tests/scoop_integration.rs -Pattern 'sqlite'` or updated assertions around the new control-plane path returns a match.
  </acceptance_criteria>
  <done>The backend control plane is now SQLite-backed end-to-end for active Scoop read/write metadata paths.</done>
</task>

<task type="auto">
  <name>Task 2: Report legacy JSON control-plane state explicitly for manual cleanup</name>
  <files>
spoon-backend/src/scoop/doctor.rs
spoon-backend/src/scoop/tests/state.rs
spoon/tests/cli/status_backend_flow.rs
  </files>
  <read_first>
.planning/phases/02.1-sqlite-control-plane-and-sync-async-boundary/02.1-CONTEXT.md
.planning/phases/02.1-sqlite-control-plane-and-sync-async-boundary/02.1-RESEARCH.md
spoon-backend/src/scoop/doctor.rs
spoon-backend/src/scoop/tests/state.rs
spoon/tests/cli/status_backend_flow.rs
  </read_first>
  <action>Update `spoon-backend/src/scoop/doctor.rs` and the relevant regression tests so leftover JSON control-plane files are treated as explicit manual-repair signals after cutover. The surfaced message must make clear that SQLite is now authoritative and that old JSON control-plane files must be repaired, migrated manually, or deleted. Add a regression named `sqlite_cutover_reports_legacy_json_state` and keep `json_status_uses_backend_read_models` green so the app still consumes backend read models correctly after the storage pivot.</action>
  <verify>
    <automated>cargo test -p spoon-backend sqlite_cutover_reports_legacy_json_state -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- `Select-String -Path spoon-backend/src/scoop/tests/state.rs -Pattern 'sqlite_cutover_reports_legacy_json_state'` returns a match.
- `Select-String -Path spoon-backend/src/scoop/doctor.rs -Pattern 'SQLite|manual cleanup|repair'` returns a match.
- `Select-String -Path spoon/tests/cli/status_backend_flow.rs -Pattern 'json_status_uses_backend_read_models'` returns a match.
  </acceptance_criteria>
  <done>The direct-cutover policy is explicit: leftover JSON control-plane state is no longer a supported source and is surfaced for manual repair.</done>
</task>

</tasks>

<verification>
Run `cargo test -p spoon-backend sqlite_cutover_reports_legacy_json_state -- --nocapture` and `cargo test -p spoon --test status_backend_flow json_status_uses_backend_read_models -- --nocapture`.
</verification>

<success_criteria>
SQLite becomes the control-plane truth source without compatibility fallback, and stale JSON state is surfaced explicitly for operator repair.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-sqlite-control-plane-and-sync-async-boundary/02.1-04-SUMMARY.md`
</output>

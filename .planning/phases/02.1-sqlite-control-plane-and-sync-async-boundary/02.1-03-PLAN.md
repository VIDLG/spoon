---
phase: 02.1-sqlite-control-plane-and-sync-async-boundary
plan: 3
type: execute
wave: 3
depends_on:
  - 02.1-01
files_modified:
  - spoon-backend/src/control_plane/journal_store.rs
  - spoon-backend/src/control_plane/lock_store.rs
  - spoon-backend/src/scoop/doctor.rs
  - spoon-backend/src/scoop/runtime/actions.rs
  - spoon-backend/src/tests/control_plane.rs
autonomous: true
requirements:
  - SQLCP-01
  - SQLCP-04
must_haves:
  truths:
    - "Operation journal, lock state, and doctor/repair metadata live in SQLite control-plane stores."
    - "Lifecycle code consumes async store facades rather than embedding driver calls or blocking bridges directly."
  artifacts:
    - path: "spoon-backend/src/control_plane/journal_store.rs"
      provides: "SQLite-backed journal/checkpoint store"
      contains: "begin_operation"
    - path: "spoon-backend/src/control_plane/lock_store.rs"
      provides: "SQLite-backed in-flight operation/lock state"
      contains: "acquire_lock"
    - path: "spoon-backend/src/scoop/doctor.rs"
      provides: "SQLite-backed doctor/repair metadata integration"
      contains: "doctor"
  key_links:
    - from: "spoon-backend/src/scoop/runtime/actions.rs"
      to: "spoon-backend/src/control_plane/journal_store.rs"
      via: "lifecycle code records operation progress through store facades"
      pattern: "begin_operation"
---

<objective>
Add SQLite-backed journal, lock, and doctor metadata so control-plane state goes beyond installed-package rows.

Purpose: satisfy the metadata half of SQLCP-01 and enforce SQLCP-04 by routing control metadata through dedicated stores instead of mixing DB details into lifecycle code.
Output: journal/lock stores, doctor metadata integration, and focused backend regressions.
</objective>

<execution_context>
@C:/Users/vision/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/vision/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02.1-sqlite-control-plane-and-sync-async-boundary/02.1-CONTEXT.md
@.planning/phases/02.1-sqlite-control-plane-and-sync-async-boundary/02.1-RESEARCH.md
@AGENTS.md
@spoon-backend/src/control_plane/sqlite.rs
@spoon-backend/src/scoop/doctor.rs
@spoon-backend/src/scoop/runtime/actions.rs
@spoon-backend/src/tests/control_plane.rs

<interfaces>
Journal and lock stores must expose async facade methods returning backend domain types or simple store DTOs. Business-rule modules should not mention `rusqlite::Connection` or manage blocking boundaries directly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SQLite journal and lock stores and route lifecycle progress through them</name>
  <files>
spoon-backend/src/control_plane/journal_store.rs
spoon-backend/src/control_plane/lock_store.rs
spoon-backend/src/scoop/runtime/actions.rs
spoon-backend/src/tests/control_plane.rs
  </files>
  <read_first>
.planning/phases/02.1-sqlite-control-plane-and-sync-async-boundary/02.1-CONTEXT.md
.planning/phases/02.1-sqlite-control-plane-and-sync-async-boundary/02.1-RESEARCH.md
spoon-backend/src/control_plane/sqlite.rs
spoon-backend/src/scoop/runtime/actions.rs
spoon-backend/src/tests/control_plane.rs
  </read_first>
  <action>Create `spoon-backend/src/control_plane/journal_store.rs` and `spoon-backend/src/control_plane/lock_store.rs`. `journal_store.rs` must provide async facade methods for beginning an operation, appending lifecycle stage checkpoints, and marking completion/failure. `lock_store.rs` must provide async facade methods for acquiring and releasing in-flight operation locks. Update `spoon-backend/src/scoop/runtime/actions.rs` only enough to route lifecycle progress through these store facades; do not embed `rusqlite::Connection` or repo-owned blocking bridge calls directly in lifecycle orchestration code.</action>
  <verify>
    <automated>cargo test -p spoon-backend sqlite_control_plane_records_operation_journal -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- `Select-String -Path spoon-backend/src/control_plane/journal_store.rs -Pattern 'begin_operation'` returns a match.
- `Select-String -Path spoon-backend/src/control_plane/lock_store.rs -Pattern 'acquire_lock'` returns a match.
- `Select-String -Path spoon-backend/src/scoop/runtime/actions.rs -Pattern 'rusqlite::Connection|spawn_blocking'` returns no matches.
  </acceptance_criteria>
  <done>Lifecycle code records journal and lock metadata through async store facades without leaking DB-driver details.</done>
</task>

<task type="auto">
  <name>Task 2: Persist doctor/repair metadata in SQLite-backed control-plane stores</name>
  <files>
spoon-backend/src/scoop/doctor.rs
spoon-backend/src/tests/control_plane.rs
  </files>
  <read_first>
.planning/phases/02.1-sqlite-control-plane-and-sync-async-boundary/02.1-CONTEXT.md
.planning/phases/02.1-sqlite-control-plane-and-sync-async-boundary/02.1-RESEARCH.md
spoon-backend/src/scoop/doctor.rs
spoon-backend/src/control_plane/sqlite.rs
spoon-backend/src/tests/control_plane.rs
  </read_first>
  <action>Extend the control-plane schema and code so doctor/repair metadata is persisted in SQLite instead of being only ephemeral logic over filesystem scans. Update `spoon-backend/src/scoop/doctor.rs` to read/write these SQLite-backed doctor findings or repair markers while still checking the filesystem for runtime evidence. Add or update a regression proving doctor metadata round-trips through SQLite and remains visible to the backend doctor path.</action>
  <verify>
    <automated>cargo test -p spoon-backend sqlite_store_facade_hides_driver_details -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- `Select-String -Path spoon-backend/src/scoop/doctor.rs -Pattern 'control_plane|repair|doctor'` returns a match for SQLite-backed metadata handling.
- `Select-String -Path spoon-backend/src/tests/control_plane.rs -Pattern 'sqlite_store_facade_hides_driver_details'` returns a match.
  </acceptance_criteria>
  <done>Doctor and repair metadata become part of the SQLite control plane instead of ad hoc ephemeral state.</done>
</task>

</tasks>

<verification>
Run `cargo test -p spoon-backend sqlite_control_plane_records_operation_journal -- --nocapture` and `cargo test -p spoon-backend sqlite_store_facade_hides_driver_details -- --nocapture`.
</verification>

<success_criteria>
Journal, lock, and doctor metadata are persisted in SQLite behind store facades, preserving the sync-core / async-edge boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-sqlite-control-plane-and-sync-async-boundary/02.1-03-SUMMARY.md`
</output>

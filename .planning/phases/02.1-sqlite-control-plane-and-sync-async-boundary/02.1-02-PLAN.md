---
phase: 02.1-sqlite-control-plane-and-sync-async-boundary
plan: 2
type: execute
wave: 2
depends_on:
  - 02.1-01
files_modified:
  - spoon-backend/src/scoop/state/store.rs
  - spoon-backend/src/scoop/buckets.rs
  - spoon-backend/src/scoop/query.rs
  - spoon-backend/src/scoop/tests/state.rs
  - spoon-backend/src/scoop/tests/buckets.rs
autonomous: true
requirements:
  - SQLCP-01
  - SQLCP-02
must_haves:
  truths:
    - "Canonical installed state and bucket registry metadata are persisted in SQLite, not JSON files."
    - "Runtime roots, install directories, and bucket repositories remain filesystem/layout-derived."
  artifacts:
    - path: "spoon-backend/src/scoop/state/store.rs"
      provides: "SQLite-backed installed-state store facade"
      contains: "rusqlite"
    - path: "spoon-backend/src/scoop/buckets.rs"
      provides: "SQLite-backed bucket registry metadata path"
      contains: "bucket registry"
  key_links:
    - from: "spoon-backend/src/scoop/query.rs"
      to: "spoon-backend/src/scoop/state/store.rs"
      via: "query/status reads canonical installed state from SQLite-backed store"
      pattern: "list_installed_states"
    - from: "spoon-backend/src/scoop/buckets.rs"
      to: "spoon-backend/src/control_plane/sqlite.rs"
      via: "bucket registry metadata now flows through SQLite control plane"
      pattern: "control_plane"
---

<objective>
Move canonical installed state and bucket registry metadata into SQLite while preserving filesystem runtime artifacts.

Purpose: satisfy SQLCP-01 and SQLCP-02 by replacing JSON control-plane storage with SQLite for the two most visible metadata domains.
Output: SQLite-backed installed-state store, SQLite-backed bucket registry metadata, and regressions proving runtime artifacts still come from filesystem/layout.
</objective>

<execution_context>
@C:/Users/vision/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/vision/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02.1-sqlite-control-plane-and-sync-async-boundary/02.1-CONTEXT.md
@.planning/phases/02.1-sqlite-control-plane-and-sync-async-boundary/02.1-RESEARCH.md
@AGENTS.md
@spoon-backend/src/scoop/state/store.rs
@spoon-backend/src/scoop/state/model.rs
@spoon-backend/src/scoop/query.rs
@spoon-backend/src/scoop/buckets.rs
@spoon-backend/src/scoop/tests/state.rs
@spoon-backend/src/scoop/tests/buckets.rs

<interfaces>
Installed-state and bucket-registry store APIs should remain async facade methods that return domain types and should not expose `rusqlite` or raw row handles to callers.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace JSON installed-state persistence with a SQLite-backed store facade</name>
  <files>
spoon-backend/src/scoop/state/store.rs
spoon-backend/src/scoop/query.rs
spoon-backend/src/scoop/tests/state.rs
  </files>
  <read_first>
.planning/phases/02.1-sqlite-control-plane-and-sync-async-boundary/02.1-CONTEXT.md
.planning/phases/02.1-sqlite-control-plane-and-sync-async-boundary/02.1-RESEARCH.md
spoon-backend/src/scoop/state/store.rs
spoon-backend/src/scoop/state/model.rs
spoon-backend/src/scoop/query.rs
spoon-backend/src/scoop/tests/state.rs
  </read_first>
  <action>Rewrite `spoon-backend/src/scoop/state/store.rs` so `read_installed_state`, `write_installed_state`, `remove_installed_state`, and `list_installed_states` use the SQLite control plane instead of `tokio::fs` JSON files under `layout.scoop.package_state_root`. Keep the API returning `InstalledPackageState` domain values and keep `RuntimeLayout` in the method signatures only for deriving runtime paths, not for selecting JSON files. Update `spoon-backend/src/scoop/query.rs` and state tests so all installed-state reads now come through the SQLite-backed store.</action>
  <verify>
    <automated>cargo test -p spoon-backend sqlite_control_plane_roundtrips_installed_state -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- `Select-String -Path spoon-backend/src/scoop/state/store.rs -Pattern 'rusqlite'` returns a match.
- `Select-String -Path spoon-backend/src/scoop/state/store.rs -Pattern 'tokio::fs::read_to_string|serde_json::to_string_pretty'` returns no matches.
- `Select-String -Path spoon-backend/src/scoop/query.rs -Pattern 'list_installed_states'` returns a match.
  </acceptance_criteria>
  <done>Canonical installed-state control metadata now lives in SQLite behind the existing async store facade.</done>
</task>

<task type="auto">
  <name>Task 2: Replace bucket registry JSON metadata with SQLite-backed registry rows while keeping repos on disk</name>
  <files>
spoon-backend/src/scoop/buckets.rs
spoon-backend/src/scoop/tests/buckets.rs
spoon-backend/src/scoop/tests/state.rs
  </files>
  <read_first>
.planning/phases/02.1-sqlite-control-plane-and-sync-async-boundary/02.1-CONTEXT.md
.planning/phases/02.1-sqlite-control-plane-and-sync-async-boundary/02.1-RESEARCH.md
spoon-backend/src/scoop/buckets.rs
spoon-backend/src/control_plane/sqlite.rs
spoon-backend/src/scoop/tests/buckets.rs
  </read_first>
  <action>Update `spoon-backend/src/scoop/buckets.rs` so bucket registry metadata (`load_buckets_from_registry`, `upsert_bucket_to_registry`, `remove_bucket_from_registry_record`, `sync_main_bucket_registry`) is stored in SQLite rather than `scoop/state/buckets.json`. Keep bucket repositories on disk under the existing layout roots and keep manifest resolution pointing at bucket checkout directories. Add regressions proving SQLite bucket registry metadata round-trips correctly while the bucket repository itself still lives on the filesystem.</action>
  <verify>
    <automated>cargo test -p spoon-backend sqlite_bucket_registry_roundtrips_and_preserves_repo_fs -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- `Select-String -Path spoon-backend/src/scoop/buckets.rs -Pattern 'buckets.json'` returns no matches in active bucket-registry persistence paths.
- `Select-String -Path spoon-backend/src/scoop/tests/buckets.rs -Pattern 'sqlite_bucket_registry_roundtrips_and_preserves_repo_fs'` returns a match.
- `Select-String -Path spoon-backend/src/scoop/buckets.rs -Pattern 'scoop_buckets_root'` returns a match, proving repo checkout paths still come from filesystem layout.
  </acceptance_criteria>
  <done>Bucket registration/sync metadata is moved into SQLite without moving bucket repos themselves out of the filesystem.</done>
</task>

</tasks>

<verification>
Run `cargo test -p spoon-backend sqlite_control_plane_roundtrips_installed_state -- --nocapture` and `cargo test -p spoon-backend sqlite_bucket_registry_roundtrips_and_preserves_repo_fs -- --nocapture`.
</verification>

<success_criteria>
Installed state and bucket registry metadata are SQLite-backed, while runtime artifacts and bucket checkouts remain on disk.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-sqlite-control-plane-and-sync-async-boundary/02.1-02-SUMMARY.md`
</output>

---
phase: 06-msvc-seams-and-ownership-completion
plan: 2
type: execute
wave: 1
depends_on:
  - 06-01-PLAN.md
files_modified:
  - spoon/src/service/msvc/mod.rs
  - spoon/src/service/mod.rs
  - spoon-backend/src/msvc/mod.rs
autonomous: true
requirements:
  - MSVC-01
  - MSVC-04
must_haves:
  truths:
    - "The app layer expresses runtime preference and translates results, but does not orchestrate managed/official internals."
    - "Live MSVC app paths delegate through backend-owned requests/results and explicit context."
  artifacts:
    - path: "spoon/src/service/msvc/mod.rs"
      provides: "Thin-shell app-side MSVC adapter"
      contains: "request construction, runtime preference, result/event translation"
---

<objective>
Thin the app-facing MSVC surface so the app expresses preference and presentation only, while the backend owns internal orchestration.

Purpose: finish the seam ownership half of Phase 6 on live app paths.
Output: app-side MSVC adapters that stop leaking runtime-specific orchestration.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Collapse app-side MSVC adapters toward request/result translation only</name>
  <files>
spoon/src/service/msvc/mod.rs
spoon/src/service/mod.rs
spoon-backend/src/msvc/mod.rs
  </files>
  <read_first>
.planning/phases/06-msvc-seams-and-ownership-completion/06-CONTEXT.md
spoon/src/service/msvc/mod.rs
spoon/src/service/mod.rs
spoon-backend/src/msvc/mod.rs
  </read_first>
  <action>Refactor the app-side MSVC service layer so it only builds backend context/requests, expresses runtime preference where required, and translates backend events/results without retaining runtime-specific orchestration logic.</action>
  <verify>
    <automated>cargo test -p spoon --test msvc_flow -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- App-side MSVC service code reads as a thin adapter rather than a second orchestration layer.
- Runtime preference remains expressible without leaking internal sequencing decisions to the app.
  </acceptance_criteria>
  <done>MSVC app/backend seam matches the thin-shell architecture already established for Scoop.</done>
</task>

<task type="auto">
  <name>Task 2: Align shared translation helpers with the thinner MSVC seam</name>
  <files>
spoon/src/service/mod.rs
spoon/src/service/msvc/mod.rs
spoon-backend/src/msvc/mod.rs
  </files>
  <read_first>
spoon/src/service/mod.rs
spoon/src/service/msvc/mod.rs
spoon-backend/src/event.rs
  </read_first>
  <action>Adjust any shared service-layer helpers needed so MSVC request/result/event translation stays consistent with the backend-owned seam and does not regress app output behavior.</action>
  <verify>
    <automated>cargo check -p spoon-backend -p spoon</automated>
  </verify>
  <acceptance_criteria>
- Shared service helpers remain compatible with the thinner MSVC adapter model.
- The seam change does not require the app to reconstruct backend semantics.
  </acceptance_criteria>
  <done>The live app path delegates MSVC behavior through backend-owned contracts cleanly.</done>
</task>

</tasks>

<verification>
Run `cargo test -p spoon --test msvc_flow -- --nocapture` and `cargo check -p spoon-backend -p spoon`.
</verification>

<output>
After completion, create `.planning/phases/06-msvc-seams-and-ownership-completion/06-02-SUMMARY.md`
</output>

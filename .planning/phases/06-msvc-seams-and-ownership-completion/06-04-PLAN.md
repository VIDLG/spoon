---
phase: 06-msvc-seams-and-ownership-completion
plan: 4
type: execute
wave: 3
depends_on:
  - 06-01-PLAN.md
  - 06-02-PLAN.md
  - 06-03-PLAN.md
files_modified:
  - spoon-backend/src/msvc/mod.rs
  - spoon-backend/src/event.rs
  - spoon-backend/src/msvc/tests/context.rs
  - spoon/tests/tui/tui_msvc_download_flow.rs
autonomous: true
requirements:
  - MSVC-01
  - MSVC-04
must_haves:
  truths:
    - "MSVC seam changes are protected by focused regressions and contract-oriented checks."
    - "Phase 6 ends with explicit execution/lifecycle contracts ready for Phase 7 rather than implicit drift."
  artifacts:
    - path: "spoon-backend/src/msvc/mod.rs"
      provides: "Explicit MSVC contract surface prepared for later lifecycle work"
      contains: "clear stage/preference/result shape suitable for Phase 7"
---

<objective>
Lock the new MSVC seam with focused regressions and formalize the contract shape that Phase 7 will execute against.

Purpose: finish Phase 6 with a safe and explicit boundary instead of only a code shuffle.
Output: protected seam and clear contract ready for canonical-state/lifecycle work.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Formalize the Phase-6-level MSVC execution contract surface</name>
  <files>
spoon-backend/src/msvc/mod.rs
spoon-backend/src/event.rs
spoon-backend/src/msvc/tests/context.rs
  </files>
  <read_first>
.planning/phases/06-msvc-seams-and-ownership-completion/06-CONTEXT.md
spoon-backend/src/msvc/mod.rs
spoon-backend/src/event.rs
spoon-backend/src/msvc/tests/context.rs
  </read_first>
  <action>Make the request/result/stage/preference surface explicit enough that Phase 7 can build canonical state and lifecycle execution on top of it without reopening seam questions.</action>
  <verify>
    <automated>cargo test -p spoon-backend --lib msvc::tests::context -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- MSVC seam contracts are explicit enough to anchor later lifecycle work.
- The change does not require event-contract redesign beyond what Phase 6 needs.
  </acceptance_criteria>
  <done>Phase 7 has a stable contract target rather than an implied one.</done>
</task>

<task type="auto">
  <name>Task 2: Protect app-visible MSVC progress behavior after seam cleanup</name>
  <files>
spoon/tests/tui/tui_msvc_download_flow.rs
spoon-backend/src/msvc/mod.rs
spoon-backend/src/event.rs
  </files>
  <read_first>
spoon/tests/tui/tui_msvc_download_flow.rs
spoon-backend/src/msvc/mod.rs
spoon-backend/src/event.rs
  </read_first>
  <action>Update or reinforce MSVC app-shell regressions so seam cleanup preserves the expected app-visible progress/result behavior while internal ownership becomes clearer.</action>
  <verify>
    <automated>cargo test -p spoon --test tui_msvc_download_flow -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- The seam rewrite keeps the TUI-visible MSVC progress contract stable.
- Phase 6 ends with regressions that make later MSVC lifecycle work safer.
  </acceptance_criteria>
  <done>MSVC seam work is guarded by targeted backend/app regressions.</done>
</task>

</tasks>

<verification>
Run `cargo test -p spoon-backend --lib msvc::tests::context -- --nocapture`, `cargo test -p spoon --test tui_msvc_download_flow -- --nocapture`, and `cargo check -p spoon-backend -p spoon`.
</verification>

<output>
After completion, create `.planning/phases/06-msvc-seams-and-ownership-completion/06-04-SUMMARY.md`
</output>

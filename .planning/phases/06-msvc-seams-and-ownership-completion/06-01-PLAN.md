---
phase: 06-msvc-seams-and-ownership-completion
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - spoon-backend/src/msvc/mod.rs
  - spoon-backend/src/msvc/detect.rs
  - spoon-backend/src/msvc/plan.rs
  - spoon-backend/src/msvc/execute.rs
  - spoon-backend/src/msvc/query.rs
autonomous: true
requirements:
  - MSVC-01
must_haves:
  truths:
    - "MSVC reads as one backend domain with explicit seams rather than a large monolithic managed controller."
    - "`managed` and `official` are represented as runtime strategies within one domain model."
  artifacts:
    - path: "spoon-backend/src/msvc/mod.rs"
      provides: "MSVC public domain surface with explicit module ownership"
      contains: "strategy-aware public exports and reduced monolithic control logic"
---

<objective>
Define the MSVC backend domain surface around explicit module responsibilities and runtime strategies.

Purpose: establish the correct seam/model skeleton before deeper canonical-state and lifecycle rewrites begin.
Output: backend MSVC module boundaries that clearly separate detect/plan/execute/query responsibilities and runtime strategy shape.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Carve MSVC into explicit domain modules and reduce `mod.rs` to public surface ownership</name>
  <files>
spoon-backend/src/msvc/mod.rs
spoon-backend/src/msvc/detect.rs
spoon-backend/src/msvc/plan.rs
spoon-backend/src/msvc/execute.rs
spoon-backend/src/msvc/query.rs
  </files>
  <read_first>
.planning/phases/06-msvc-seams-and-ownership-completion/06-CONTEXT.md
.planning/phases/06-msvc-seams-and-ownership-completion/06-RESEARCH.md
spoon-backend/src/msvc/mod.rs
spoon-backend/src/msvc/official.rs
spoon-backend/src/msvc/status.rs
  </read_first>
  <action>Restructure the backend MSVC domain so `mod.rs` becomes a thinner public surface, with explicit modules for detection, planning, execution coordination, and query/state projection boundaries.</action>
  <verify>
    <automated>cargo check -p spoon-backend</automated>
  </verify>
  <acceptance_criteria>
- The backend MSVC module layout reflects the `detect / plan / execute / state-query` shape.
- `mod.rs` no longer reads like the one giant owner of all managed-path behavior.
  </acceptance_criteria>
  <done>MSVC backend seams exist in code layout rather than only in planning language.</done>
</task>

<task type="auto">
  <name>Task 2: Introduce explicit runtime-strategy modeling for managed vs official</name>
  <files>
spoon-backend/src/msvc/mod.rs
spoon-backend/src/msvc/detect.rs
spoon-backend/src/msvc/plan.rs
spoon-backend/src/msvc/query.rs
  </files>
  <read_first>
spoon-backend/src/msvc/mod.rs
spoon-backend/src/msvc/official.rs
spoon-backend/src/msvc/status.rs
spoon-backend/src/msvc/rules.rs
  </read_first>
  <action>Make the backend domain explicitly express `managed` and `official` as runtime strategies within one MSVC model instead of leaving them as loosely related parallel surfaces.</action>
  <verify>
    <automated>cargo test -p spoon-backend --lib msvc::tests::context -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- Backend code clearly expresses one MSVC domain with two runtime strategies.
- No new app-facing parallel runtime-product split is introduced.
  </acceptance_criteria>
  <done>The MSVC domain model is unified enough that later phases can build canonical state on top of it.</done>
</task>

</tasks>

<verification>
Run `cargo check -p spoon-backend` and `cargo test -p spoon-backend --lib msvc::tests::context -- --nocapture`.
</verification>

<output>
After completion, create `.planning/phases/06-msvc-seams-and-ownership-completion/06-01-SUMMARY.md`
</output>

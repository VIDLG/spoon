---
phase: 06-msvc-seams-and-ownership-completion
plan: 3
type: execute
wave: 2
depends_on:
  - 06-01-PLAN.md
  - 06-02-PLAN.md
files_modified:
  - spoon-backend/src/msvc/status.rs
  - spoon-backend/src/msvc/rules.rs
  - spoon-backend/src/msvc/paths.rs
  - spoon-backend/src/msvc/detect.rs
  - spoon-backend/src/msvc/query.rs
autonomous: true
requirements:
  - MSVC-01
  - MSVC-04
must_haves:
  truths:
    - "MSVC status/detail/detect/query behavior no longer relies on an accidental mix of module-local helpers and monolithic exports."
    - "Shared domain concepts needed by both runtime strategies are explicit and reusable."
  artifacts:
    - path: "spoon-backend/src/msvc/status.rs"
      provides: "Unified MSVC read-model projection over explicit domain helpers"
      contains: "status/query logic consistent with one MSVC domain"
---

<objective>
Move mixed detect/query/status logic onto the new MSVC domain boundaries and make shared concepts explicit.

Purpose: ensure the MSVC seam is reflected in real backend read paths, not just file names.
Output: status and supporting domain helpers that read coherently as part of one MSVC domain.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Re-home status/query logic onto explicit detect and query helpers</name>
  <files>
spoon-backend/src/msvc/status.rs
spoon-backend/src/msvc/detect.rs
spoon-backend/src/msvc/query.rs
  </files>
  <read_first>
spoon-backend/src/msvc/status.rs
spoon-backend/src/msvc/mod.rs
spoon-backend/src/msvc/official.rs
  </read_first>
  <action>Refactor MSVC status/detail/query code so it consumes explicit detect/query helpers and shared domain concepts rather than depending on a loose mix of monolithic exports.</action>
  <verify>
    <automated>cargo test -p spoon-backend --lib msvc::tests::root -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- MSVC status/query code reflects the new domain shape.
- Shared managed/official facts are easier to identify and reuse.
  </acceptance_criteria>
  <done>Read-model code is aligned with the seam-first MSVC architecture.</done>
</task>

<task type="auto">
  <name>Task 2: Keep app-visible MSVC status behavior stable while the backend read path changes</name>
  <files>
spoon-backend/src/msvc/status.rs
spoon/tests/cli/msvc_flow.rs
  </files>
  <read_first>
spoon/tests/cli/msvc_flow.rs
spoon-backend/src/msvc/status.rs
  </read_first>
  <action>Update or extend status-facing regressions only as needed so the seam cleanup preserves the visible app contract while the underlying backend read path becomes cleaner.</action>
  <verify>
    <automated>cargo test -p spoon --test msvc_flow msvc_status_lists_managed_and_official_runtime_state -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
- MSVC status continues to present both runtime strategies through one backend-owned read model.
  </acceptance_criteria>
  <done>The seam cleanup does not regress user-visible MSVC status output.</done>
</task>

</tasks>

<verification>
Run `cargo test -p spoon-backend --lib msvc::tests::root -- --nocapture` and `cargo test -p spoon --test msvc_flow msvc_status_lists_managed_and_official_runtime_state -- --nocapture`.
</verification>

<output>
After completion, create `.planning/phases/06-msvc-seams-and-ownership-completion/06-03-SUMMARY.md`
</output>

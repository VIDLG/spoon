---
phase: 12.2-scoop-surface-and-actions-decomposition
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - spoon-backend/src/scoop/actions.rs
  - spoon-backend/src/scoop/actions
  - spoon-backend/src/scoop/mod.rs
requirements:
  - SLEG-02
must_haves:
  truths:
    - "The Scoop action coordinator remains at the domain root."
    - "Large action orchestration is no longer concentrated in one file."
---

## Objective

Turn the current root `actions.rs` into a clearer `actions/` module family while keeping the root coordinator role intact.

## Tasks

1. Split install/update and uninstall/reapply orchestration into focused submodules.
2. Keep `scoop/actions` as the named domain entry layer rather than pushing everything into `lifecycle`.
3. Coordinate the split with any new `extract/info/buckets` module boundaries so root exports stay coherent.

## Verification

- `cargo check -p spoon-backend -p spoon`
- `cargo test -p spoon-backend scoop_action_contract_uses_context -- --nocapture`

## Output

Create `12.2-02-SUMMARY.md` after completion.

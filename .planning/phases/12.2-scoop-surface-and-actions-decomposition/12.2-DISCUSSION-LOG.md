# Phase 12.2: Scoop Surface and Actions Decomposition - Discussion Log

> **Audit trail only.** Do not use as planning input.
> Decisions are captured in `12.2-CONTEXT.md`.

**Date:** 2026-04-04
**Phase:** 12.2-scoop-surface-and-actions-decomposition
**Areas discussed:** surface decomposition, actions decomposition, lifecycle thin shells

---

## Surface Decomposition

| Option | Description | Selected |
|--------|-------------|----------|
| Keep one `surface.rs` | Continue collecting all command-surface logic in one file | |
| Split `surface.rs` into focused submodules | Separate shim logic, shortcut logic, and reapply orchestration | X |
| Move everything into `lifecycle/surface.rs` | Collapse host-side surface helpers into lifecycle | |

**User direction:** Split `surface.rs`.

---

## Actions Decomposition

| Option | Description | Selected |
|--------|-------------|----------|
| Keep one root `actions.rs` | Leave the coordinator as one large file | |
| Move everything into `lifecycle/` | Treat action orchestration as lifecycle-only code | |
| Keep root coordinator but split into `actions/` modules | Preserve root role while decomposing internals | X |

**User direction:** `actions` still needs decomposition, but not by pushing everything into `lifecycle`.

---

## Lifecycle Thin Layers

| Option | Description | Selected |
|--------|-------------|----------|
| Keep all lifecycle files | Preserve current stage topology even if thin | |
| Remove lifecycle entirely | Collapse all stage modules back into actions | |
| Keep lifecycle only where stage semantics remain | Collapse adapter shells, keep meaningful phase modules | X |

**User direction:** If lifecycle files are only thin wrappers, they should not survive by default.

---

## Current Best Reading

- `surface.rs` is too mixed to stay one file.
- `actions.rs` is still the correct coordinator, but its internals should be modularized.
- `lifecycle` should be judged by semantic value, not by tradition.

---

*Phase: 12.2-scoop-surface-and-actions-decomposition*

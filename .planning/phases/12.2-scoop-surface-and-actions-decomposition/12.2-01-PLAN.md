---
phase: 12.2-scoop-surface-and-actions-decomposition
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - spoon-backend/src/scoop/host/surface.rs
  - spoon-backend/src/scoop/host/mod.rs
  - spoon-backend/src/scoop/host
  - spoon-backend/src/scoop/extract.rs
  - spoon-backend/src/scoop/info.rs
  - spoon-backend/src/scoop/buckets.rs
requirements:
  - SLEG-02
must_haves:
  truths:
    - "`surface.rs` no longer acts as one large mixed file."
    - "`extract.rs`, `info.rs`, and `buckets.rs` all move toward smaller, clearer modules."
  artifacts:
    - path: "spoon-backend/src/scoop/host"
      provides: "split command-surface helpers"
---

## Objective

Decompose the oversized Scoop host-side surface layer into smaller modules with clearer responsibility boundaries.

## Tasks

1. Split `host/surface.rs` so shim logic, shortcut logic, and reapply orchestration stop living in one file.
2. Split `extract.rs` so archive handling, materialization, and current-entry maintenance stop living in one file.
3. Split `info.rs` and `buckets.rs` if their current mixed concerns can be separated cleanly without behavior change.
4. Update re-exports and imports without changing behavior.

## Verification

- `cargo check -p spoon-backend -p spoon`

## Output

Create `12.2-01-SUMMARY.md` after completion.
